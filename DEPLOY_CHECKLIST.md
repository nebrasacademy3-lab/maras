# قائمة نشر مراس العلم

## Railway — الويب وواجهات API

1. فك الحزمة وارفعها إلى مستودع خاص، ثم أنشئ خدمة Railway واجعل Root Directory هو `backend`.
2. أضف PostgreSQL واربط `DATABASE_URL` بالخدمة.
3. أضف Volume واربطه بالمسار `/data`.
4. انسخ متغيرات `backend/.env.example` إلى Railway Variables، واستبدل أسرار الأمثلة بقيم عشوائية مستقلة.
5. اضبط `NEXT_PUBLIC_SITE_URL` و`APP_URL` على نطاق HTTPS النهائي، و`UPLOAD_DIR=/data/uploads`.
6. اترك `RUN_DB_MIGRATIONS=true` و`AUTO_SEED_CATALOG=true` و`CATALOG_SEED_MODE=core` في أول تشغيل.
7. تأكد أن `/api/health` يعيد `ok: true` و`database: ready`.
8. أنشئ أول حساب إدارة بالطريقة الموثقة في `backend/README.md`، ثم غيّر/دوّر الرمز الإداري بعد الإعداد إن لزم.

## الخدمات الخارجية

- Tap: اضبط `TAP_SECRET_KEY` و`TAP_WEBHOOK_SECRET` واجعل Webhook على `/api/webhooks/tap`، ثم اختبر Tap Sandbox قبل استقبال مدفوعات حقيقية.
- Resend: اضبط `RESEND_API_KEY` و`EMAIL_FROM` حتى تصل روابط استعادة كلمة المرور.
- المساعد الخارجي اختياري؛ يبقى محرك المنصة المحلي فعالًا دون مفتاح.

## Expo — Android وiOS

1. داخل `mobile` انسخ `.env.example` إلى `.env` واضبط `EXPO_PUBLIC_API_URL` على نطاق Railway الآمن.
2. اترك `EXPO_PUBLIC_STORE_MODE=reader` لنسخ المتاجر.
3. شغّل `npm ci` ثم `npm run typecheck` و`npm run lint`.
4. اربط مشروع EAS ثم شغّل ملفات البناء الموجودة في `eas.json`.
5. بناء APK/AAB أو IPA موقع وإرساله يحتاج حساب Expo وشهادات Apple/Google المملوكة لك.

## اختبار القبول بعد النشر

- تسجيل طالب جديد، تسجيل الدخول والخروج، إكمال الملف، والاستعادة عبر البريد.
- إضافة مادة/وحدة/درس وإشعار من الإدارة والتأكد من ظهوره دون تحديث يدوي في الويب والتطبيق.
- رفع شعار وغلاف وفيديو، ثم التحقق من ظهور المادة «قريبًا» حتى تكتمل كل الفيديوهات والدرس التجريبي.
- إرسال طلب مادة بمرفقات، فتحه من المشرف، تغيير حالته، ومتابعته من الطالب.
- فتح تذكرة دعم والرد عليها من الإدارة/المشرف مع مرفق.
- Tap Sandbox ثم التأكد من منح الوصول بعد Webhook فقط.
- اختبار الثيم الفاتح والليلي وكل أحجام الخط على هاتف وجهاز لوحي ومتصفحات سطح المكتب.
- إعداد نسخ احتياطية مشفرة لقاعدة PostgreSQL وVolume واختبار استعادتها.
