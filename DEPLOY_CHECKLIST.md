# قائمة نشر مراس العلم

## Railway — الويب وواجهات API

| الخطوة | التحقق المطلوب |
|---|---|
| 1 | ارفع الحزمة إلى مستودع خاص، وأنشئ خدمة Railway يكون Root Directory فيها `backend`. |
| 2 | أضف PostgreSQL واربط `DATABASE_URL` بالخدمة. |
| 3 | أضف Volume دائمًا بالمسار `/data` واضبط `UPLOAD_DIR=/data/uploads`. |
| 4 | انسخ مفاتيح `backend/.env.example` إلى Railway Variables واستبدل جميع قيم الأمثلة بأسرار عشوائية مستقلة. |
| 5 | اضبط `APP_URL` و`NEXT_PUBLIC_SITE_URL` على نطاق HTTPS النهائي. إذا نُشر Expo Web على نطاق مختلف، اضبط `EXPO_WEB_ORIGIN` على أصله الدقيق. |
| 6 | اترك `RUN_DB_MIGRATIONS=true` و`AUTO_SEED_CATALOG=true` و`CATALOG_SEED_MODE=core` في أول تشغيل. |
| 7 | راقب سجل الإقلاع حتى اكتمال الهجرات والبذور، ثم تأكد أن `/api/health` يعيد `ok: true` و`database: ready`. |
| 8 | أنشئ أول حساب إدارة بالطريقة الموثقة في `backend/README.md`، ثم دوّر رمز الإعداد إذا استُخدم مؤقتًا. |
| 9 | أنشئ خدمة Cron من الشيفرة نفسها وشغّل `./scripts/dispatch-notifications.sh` كل دقيقة أو خمس دقائق لتسليم Push المجدول. |

## الخدمات الخارجية

| الخدمة | ما يجب ضبطه واختباره |
|---|---|
| Tap | اضبط `TAP_SECRET_KEY` و`TAP_WEBHOOK_SECRET` واجعل Webhook على `/api/webhooks/tap`، ثم نفّذ Tap Sandbox من الدفع حتى `CAPTURED`. |
| Resend | اضبط `RESEND_API_KEY` و`EMAIL_FROM` واختبر وصول رابط استعادة كلمة المرور. |
| المساعد | اضبط `OPENAI_API_KEY` و`OPENAI_API_URL` و`ASSISTANT_MODEL`. يقبل العنوان `/v1` أو مسار الإكمال كاملًا، ويبقى المحرك المحلي فعالًا عند غياب المفتاح. |
| Expo Push | اربط مشروع EAS وضع `EXPO_PUBLIC_EAS_PROJECT_ID` الحقيقي، ثم اختبر تسجيل الرمز ووصول إشعار على جهاز فعلي. |

## Expo — Android وiOS

| الخطوة | التحقق المطلوب |
|---|---|
| 1 | داخل `mobile` انسخ `.env.example` إلى `.env` واضبط `EXPO_PUBLIC_API_URL` على نطاق Railway HTTPS الفعلي. |
| 2 | اترك `EXPO_PUBLIC_STORE_MODE=reader` لنسخ المتاجر، وضع معرف EAS الحقيقي. |
| 3 | شغّل `npm ci` ثم `npm run doctor` و`npm run typecheck` و`npm run lint`. |
| 4 | صدّر الويب وAndroid وiOS كلًا على حدة للتحقق، ثم نفّذ EAS Build من `eas.json`. |
| 5 | استخدم حساب Expo وشهادات Apple/Google المملوكة لك لبناء AAB/IPA موقعين وإرسالهما للمتاجر. |

## اختبار القبول بعد النشر

| السيناريو | معيار القبول |
|---|---|
| الحساب | تسجيل طالب، الدخول والخروج، إكمال الملف، واستعادة كلمة المرور بالبريد. |
| الإدارة | إضافة جامعة/تخصص/مادة/وحدة/درس، وإخفاؤها ثم إعادة نشرها، والتحقق من ظهور التغيير دون تحديث يدوي. |
| البرامج الرسمية | تشغيل «تحقق واربط البرامج» لجامعة معروفة ومراجعة المصدر والتاريخ وسجل التدقيق. |
| النشر المرحلي | رفع فيديو لدرس واحد ونشره، ثم التأكد من إمكانية الاشتراك وظهور الدروس الأخرى «قريبًا» ضمن الصلاحية نفسها. |
| الرفع | رفع شعار وغلاف وفيديو، وطلب مادة حتى الحدود المعلنة، وتذكرة دعم بمرفق، مع ظهور النسبة والسرعة والإلغاء. |
| الإعلانات | إنشاء إعلان عام وموجّه ومجدول، والتحقق من الويب وExpo وصندوق الإشعارات واستقلال حالة القراءة بين طالبين. |
| Push | تسجيل جهاز فعلي، تشغيل حملة فورية ومجدولة، ومراجعة ملخص المستهدف/المقبول/المرفوض وعدم تكرار الحملة. |
| المساعد | سؤال عن مادة وسعر ورابط، وسؤال طالب عن جامعته وطلبه، وسؤال بلهجة، من الويب والتطبيق. |
| الدفع | تنفيذ Tap Sandbox والتأكد من منح الوصول بعد تأكيد Webhook فقط وإصدار الفاتورة. |
| العرض | اختبار الثيم الفاتح والليلي وأحجام الخط على هاتف وجهاز لوحي ومتصفحات سطح المكتب. |
| الاستعادة | إنشاء نسخة احتياطية مشفرة لقاعدة PostgreSQL والـVolume ثم اختبار استعادتها فعليًا. |

لا تفتح التسجيل العام قبل إكمال Tap Sandbox وPush على جهاز فعلي واختبار حمل واختبار اختراق مستقل.
