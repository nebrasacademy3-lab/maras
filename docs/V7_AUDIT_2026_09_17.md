# مراجعة وإصلاحات v7 — 17 سبتمبر 2026

## النطاق
مراجعة فعلية لفرع `implement/plan-v7-20260916` انطلاقًا من `3b3d8f3dc5a84be4778006d4ef48e16f45f6cf08`، مع الرجوع إلى خطة الإصدار السابع (192 حالة قبول مطلوبة). هذا التقرير ليس شهادة اكتمال الخطة.

## الإصلاحات
- إصلاح فشل اختبار واجهة تغيير البريد: انتقال كامل إلى تسجيل الدخول لتفريغ سياق الحساب القديم.
- تحقق من الجلسة داخل معاملة تغيير البريد وإلغائه، وقفل الحساب والوجهة النهائية، وإبطال جميع الجلسات وتحديات إعادة كلمة المرور وMFA وتبادل OAuth بعد اكتمال التغيير، مع إبقاء اعتماد الأجهزة.
- منع إحياء طلب أُلغي أثناء إرسال الرموز، وعدم إعادة استهلاك طرف سبق تأكيده، وإرجاع تعارض آمن عند تصادم التفرد.
- إيقاف إعادة كتابة بريد الفاتورة وسجل الفاعلين التاريخي. عرض هوية العميل من لقطة الفاتورة في العرض والتنزيل، وجلب فواتير التطبيق عبر الطلب المملوك بدل البريد التاريخي.
- حماية مجموعة Gemini المجانية من الانكماش الكاذب بسبب مفتاح بارد أو معطوب، ومن تكرار مفتاح مدفوع تحت تصنيف مجاني أو إعادة تشغيل مفتاح أوقفته الإدارة.
- تدوير الموارد المجانية بحسب آخر استخدام مع كسر تعادل المللي ثانية، وحماية تحديثات الحالة من الردود المتأخرة بعد الاستبدال أو الإيقاف، وفحص الأهلية قبل حجز الميزانية وبعده.
- زيادة حد محاولات المفاتيح الافتراضي من 3 إلى 10 ضمن مهلة الطلب وحد أقصى قابل للضبط 64؛ لم تُلغ حدود الزمن ولم تُستخدم مفاتيح حقيقية.
- إضافة 10 اختبارات سلوكية لمجموعة Gemini و4 لاستخدام لقطة الفاتورة، و9 حالات تكامل فعلية لخدمة البريد على PostgreSQL مع ناقل رسائل اصطناعي.
- ربط تكامل البريد بمسار CI، وإزالة workflow تجهيز المراجعة المؤقت.

## التحقق المحلي على المصدر المعدل
- بناء Next.js الإنتاجي: ناجح.
- TypeScript للويب: ناجح.
- مجموعة الويب الكاملة: **541 ناجحًا، صفر فاشل**، وتشمل فحوص HTTP للنسخة المبنية.
- تكامل البريد المعزول: **9 حالات ناجحة** على PostgreSQL؛ استدعاءات المسار والمصادقة وقاعدة البيانات وتوليد الرسائل حقيقية، لكن ناقل Resend محاكى ولا يرسل بريدًا خارجيًا.
- ESLint لملفات الويب والاختبارات المعدلة: ناجح.
- نتائج GitHub Actions والمتصفحات والتطبيق تخص الالتزام الذي شُغلت عليه وتراجع من تبويب Actions؛ لا تُستبدل نتائج تشغيل جديد بنتائج التزامات تاريخية.

## بوابات لا تزال مفتوحة — لا تنشر بوصفها مكتملة
1. **الهوية:** لا يزال نقل الملكية إلى userId ثابتًا غير مكتمل في جميع السجلات والمهام القديمة. الجسر الحالي يحدّث المراجع التشغيلية القديمة فقط. `EMAIL_CHANGE_ENABLED=false` افتراضيًا؛ يبقى التصحيح الذاتي مغلقًا حتى قبول الترحيل واختبارات ID كاملة، بما فيها التسجيل الخاطئ والاسترداد والجوال وتأكيد الدفع المتأخر. لا تفعّله لمجرد نجاح الحالات التسع.
2. **نطاقات المشرفين:** لا تغطي المراجعة كل مسار قديم. توجد استثناءات تحتاج معالجة، منها السماح العام عند غياب تعيينات النطاق في بعض مسارات الطالب/المادة ومساحة المشرف، ومرشحات مرتبطة بصفحات بيانات أخرى في console. يجب توحيد التفويض على مستوى السجل قبل اعتماد فصل المشرفين.
3. **Gemini الكامل:** الإصلاح الحالي ليس سجل مشاريع وفوترة موثقًا ولا موزع حصص موزونًا ذريًا بين جميع الخوادم. ما زالت الحاجة إلى توثيق المشروع والفوترة، تقاسم حصص مفاتيح المشروع، أسعار/توكنز وحجوزات كل جزء، وشهادة نفاد مرتبطة بإصدار السياسة. الاحتياط المدفوع يبقى مغلقًا؛ لا تفعّل فوترة إنتاج اعتمادًا على اختبارات المحاكاة.
4. **الملفات والأدوات:** الرفع القابل للاستئناف والمعالجة المعزولة والتقسيم العلمي والتعافي من تعطل أجزاء مستند كبير وتطابق PDF والنطق والدروس عبر العميلين تحتاج قبول ST/LF كاملًا. اختبارات العقود الحالية ليست دليل اكتمال هذه الميزات.
5. **التطبيقات والتكاملات الحية:** لا اختبار جهاز Android أو iPhone فعلي، ولا APK/AAB/IPA موقعة أو إرسال للمتاجر. مشتريات المتاجر/RevenueCat وTap وPush وOTP وResend وGemini الحية ليست معتمدة بهذا الفحص.
6. **السعة والأمان:** لا يوجد في هذه الدفعة اختبار حمل يثبت ألف مستخدم متزامن، ولا تدقيق اختراق مستقل أو ضمان انعدام الثغرات.
7. **الإنتاج:** تمت مراجعة Railway قراءة فقط. عند المراجعة كان main المنشور `f2348e3`؛ فرع التطوير منفصل. لم تُغيّر بيانات الإنتاج أو المفاتيح، ولم يُرسل بريد أو طلب دفع أو توليد مدفوع. يلزم فحص صحة صريح وخطة نسخة احتياطية/ترحيل/رجوع قبل النشر.

## إعادة التشغيل
- `npm run build`
- `node --test --test-concurrency=1 tests/*.test.mjs`
- `QA_DATABASE_URL=<dedicated loopback maras_qa> node scripts/qa-study-ci.mjs`
- لا تضع متغيرات الإنتاج أو بيانات الطلاب الحقيقية في بيئة QA.

المطلوب النهائي 192 حالة قبول وأدلة تشغيل للويب والتطبيقات؛ عدد اختبارات الوحدة الأكبر لا يعني تحقق الحالات الـ192 تلقائيًا.
