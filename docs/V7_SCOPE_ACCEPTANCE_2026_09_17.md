# نطاقات المشرفين وحماية سجلات الطلاب — 17 سبتمبر 2026

## مصدر هذه الدفعة
استكمال من `c96c9b7f784e84fa8f292742e060d131c3c82267`، بعد إصلاحات الهوية وGemini في `5c79138`. لم يتغير main أو Railway أو بيانات الإنتاج. حالات الاختبار أدناه على بيانات اصطناعية وقاعدة PostgreSQL معزولة.

## السلوك المنفذ
- عدم وجود نطاق فعّال للمشرف لم يعد يعني السماح بجميع البيانات. الصلاحيات الوظيفية وحدها لا توسع نطاق البيانات.
- صلاحية `data.all` مستقلة وصريحة لمن يحتاج نطاق جميع البيانات. لا تُمنح تلقائيًا لمشرف قديم، ولا تمنح عرضًا أو تعديلًا أو نشرًا أو حذفًا بمفردها. المدير الأعلى يحتفظ بقدراته الحالية.
- ترشيح الطلاب والطلبات والدعم والاشتراكات والتقييمات والدروس والفيديوهات وملفات الطلبات يتم في SQL قبل الصفحات والأعداد؛ لا تعتمد شاشة الدعم على أن تكون بيانات الطلاب قد طُلبت في الشاشة نفسها.
- الطلب المالي المختلط لا يُكشف لمشرف يملك نطاق مادة واحدة فقط من مواده. ملف الطالب وقائمة مشتركي المادة يتبعان نطاق كل سجل تابع أيضًا، لا مجرد السماح بفتح صفحة الطالب.
- تعديل سجل موجود أو نقله إلى جامعة أو مادة أخرى يفحص المعرّف الحقيقي والوجهة المقترحة. لا يمكن تعديل درس من مادة أخرى بتغيير courseSlug في الطلب.
- توقيع الرفع وملفات الدروس ومرفقات الطلبات والدعم ومساراتها القديمة تستخدم نطاق الخادم نفسه. السجل الممنوع يرفض قبل قراءة التخزين أو توقيع رابط له.
- المادة المشتركة على مستوى الجامعة تحتاج نطاقًا يشمل الجامعة كلها، لا تخصصًا واحدًا فقط.
- إسناد طلب مادة لمشرف يُقفل بمعاملة؛ طلبان متزامنان لا يستبدلان اسم المسؤول بصمت. سحب النطاق يرفض الطلب التالي، وتعديل المالك للنطاق يتطلب MFA ويبطل جلسات المشرف المتأثر مع سجل تدقيق داخل المعاملة.
- شاشات وإجراءات المنصة العامة التي لا تدعم ترشيح السجلات بأمان تتطلب `data.all` إلى جانب صلاحية الوظيفة؛ تشمل المالية المركزية والإحالات وأدوات الذكاء والتشغيل والإشعارات وإدارة الجامعات العامة. يبقى عرض الطلبات والاشتراكات المحددة بالنطاق متاحًا بصلاحياته. إتاحة جميع إجراءات المالية داخل نطاق جزئي تحتاج تطويرًا إضافيًا ولا تُعد منجزة هنا.
- واجهتا الويب والتطبيق تخفيان المداخل والأزرار التي تتطلب النطاق العام عند عدم منحه. العقود المشتركة للمنح والتنقل متطابقة بين العميلين.
- إصلاح تسرب ملاحظة الدعم الداخلية من مسار console القديم إلى نص إشعار الطالب. عند تغيير الحالة مع ملاحظة داخلية، يحتوي الإشعار على تحديث الحالة فقط.

## أدلة التحقق
- مجموعة اختبارات الويب المحلية: 548 اختبارًا ناجحًا دون فشل في نقطة التحقق السابقة للتنقيحات النهائية؛ تعتمد النتيجة النهائية لنفس الالتزام من GitHub Actions.
- TypeScript للويب: ناجح بعد إضافة الاختبارات النهائية.
- اختبارات النطاقات المباشرة في `scripts/qa-supervisor-data-scope.ts`: 14 حالة تكامل ناجحة. تستخدم مسارات الخادم والمصادقة وسياسة الصلاحيات وSQL الحقيقي، لا نسخة بديلة من منطق الرفض.
- تشمل الاختبارات 60 طالبًا أحدث خارج النطاق لإثبات صحة الترقيم والأعداد؛ طلبًا ماليًا مختلطًا؛ 14 محاولة تعديل خارج النطاق؛ طلبَي إسناد متزامنين؛ سحب النطاق؛ منح النطاق العام دون منح الأفعال؛ منع تسرب ملاحظة داخلية؛ والتحقق من MFA وإبطال الجلسات عند تعديل النطاق.
- أُعيد تشغيل تكامل الأجهزة السابق (11 حالة) وتكامل إدارة الصلاحيات والمحتوى (8 حالات) بنجاح. بيانات الاختبار حصلت على نطاقات اصطناعية صريحة بدل إعادة السماح الافتراضي في الكود.
- أضيفت 7 اختبارات سياسة وعقود، ودُمج تكامل النطاقات في CI مع حفظ تقرير JSON غير سري.
- لا مزود بريد/ذكاء/دفع حي، ولا اختبار أجهزة فعلية أو ادعاء سعة ألف مستخدم متزامن في هذه الأدلة.

## ضوابط النشر
قبل اعتماد الإنتاج: يراجع المالك منح المشرفين ويحدد نطاقاتهم الفعلية أو يمنح `data.all` لمن يحتاجها عمدًا. لا تنسخ معرفات أو بيانات QA إلى الإنتاج. التعديل لا يتطلب ترحيل جدول جديد، لكنه يضيق الوصول الذي كان يُمنح ضمنيًا؛ لذا يحتاج مراجعة تشغيلية للمنح قبل النشر.

تبقى الخطة الأوسع مفتوحة: ترحيل ملكية السجلات إلى userId وحالات استرداد وتصحيح الهوية، سجل مشاريع وفوترة وحصص Gemini لكل جزء، الملفات الكبيرة القابلة للاستئناف والتكافؤ العلمي وPDF والنطق، التكاملات الحية وتوقيع التطبيقات والأجهزة واختبار الحمل. نجاح هذه الدفعة لا يغلق حالات القبول الـ192 تلقائيًا.
