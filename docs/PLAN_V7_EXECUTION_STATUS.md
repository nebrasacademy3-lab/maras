# تحديث مرحلة ملكية الطلبات والإشعارات — 17 سبتمبر 2026

أضيف ترحيل `0036_order_owner_and_notification_target` مع تفويض الطلبات والفواتير بمعرّف الحساب، وحماية تأكيد الدفع المتأخر والاسترداد والكوبونات والإحالات، وتوحيد خصوصية الإشعارات والإرسال المجدول. أضيفت 9 اختبارات سلوكية و9 مجموعات تكامل و5 مجموعات قبول للترحيل، وفحص واجهة المراجعة المالية.

التفاصيل وحدود القبول والنشر: [تقرير ملكية الطلبات](V7_ORDER_OWNERSHIP_2026_09_17.md). لم تُنفذ هجرة الإنتاج، ولا يزال تغيير البريد مغلقًا افتراضيًا حتى ترحيل بقية الملكيات واستكمال مسارات الهوية. نتائج CI النهائية توثق في طلب الدمج؛ لا تعني إضافة الاختبارات نجاحها مسبقًا.

---

# حالة تنفيذ خطة v7 — 17 سبتمبر 2026

هذه دفعة إصلاحات وليست إعلان اكتمال الخطة ذات 192 حالة قبول. راجع [تقرير المراجعة والنواقص](V7_AUDIT_2026_09_17.md) للتفاصيل وحدود الأدلة.

## المنجز في المراجعة الحالية
- إصلاح فشل اختبار تغيير البريد وحماية حدود جلسة الحساب والكاش.
- حماية إلغاء طلب تغيير البريد أثناء إرسال الرموز، والتحقق داخل المعاملة، وإبطال الجلسات والتحديات القديمة دون إسقاط اعتماد الأجهزة.
- حفظ هوية العميل التاريخية في الفاتورة وسجل الفاعلين، وتصحيح العرض والتنزيل وقائمة فواتير التطبيق.
- منع فتح الاحتياط المدفوع بسبب اختفاء مورد مجاني بارد أو معطوب من قائمة الجاهزين؛ منع إعادة تفعيل مفتاح أوقفته الإدارة بسبب رد متأخر؛ تحسين تدوير المجاني والتحقق قبل إرسال المدفوع.
- إضافة 14 اختبار وحدة/عقد و9 حالات تكامل PostgreSQL، وربط تكامل البريد بمسار CI.
- إزالة ملف workflow تجهيز بيئة المراجعة المؤقت.

## التحقق المؤكد محليًا
البناء الإنتاجي وTypeScript ناجحان. مجموعة الويب: **541 ناجحًا، صفر فاشل**. تكامل البريد: **9 حالات ناجحة** على قاعدة معزولة، مع ناقل رسائل اصطناعي لا يتصل بـResend. فحص ESLint للويب والاختبارات المعدلة ناجح.

نتائج التطبيق والمتصفحات على GitHub Actions مرتبطة بالتزام المصدر نفسه؛ لا تعتمد نتائج التزامات سابقة بوصفها دليلًا لنسخة جديدة.

## ما يمنع اعتبار الخطة مكتملة
- إكمال ترحيل ملكية جميع السجلات والمهام إلى userId، واسترداد الحساب وتصحيح وسائل التواصل وقبول ID بالكامل.
- مراجعة نطاقات المشرفين لجميع endpoints، خصوصًا الافتراض العام عند غياب النطاقات ومسارات console القديمة.
- سجل مشاريع Gemini وفوترتها المثبتة، وموزع الحصص والميزانية الذري لكل جزء عبر الخوادم.
- قبول ST/LF الكامل للملفات الكبيرة والاستئناف والدقة العلمية وPDF والتكافؤ بين الويب والتطبيقات.
- أجهزة Android/iOS فعلية ونسخ متجر موقعة ومشتريات/Push/OTP وتكاملات حية.
- اختبار حمل موثق، وفحص أمان مستقل، ونسخ احتياطي وترحيل ورجوع ونشر مضبوط.

`EMAIL_CHANGE_ENABLED=false` افتراضيًا حتى قبول ترحيل الملكية. الاحتياط المدفوع لا يُفعّل بهذه الدفعة. لم تُغيّر قاعدة الإنتاج أو مفاتيحه ولم تُرسل مدفوعات أو رسائل حقيقية.

السجل السابق محفوظ في تاريخ Git للملف؛ أحدث تقرير أعلاه هو مرجع هذه الدفعة. لا يعد عدد اختبارات الوحدة شهادة اجتياز الحالات الـ192.

## استكمال نطاقات البيانات — 17 سبتمبر 2026
نُفذ رفض افتراضي للمشرف دون نطاق، وترشيح SQL قبل الصفحات والأعداد، وحماية المعرّفات والوجهات في التعديل والملفات والأجهزة، وقفل إسناد الطلبات، وإبطال جلسات الفريق عند تعديل النطاق بـMFA. عولج تسرب ملاحظات الدعم الداخلية عبر إشعارات المسار القديم. راجع [أدلة النطاقات وحدودها](V7_SCOPE_ACCEPTANCE_2026_09_17.md). لا يزال النشر وحالات الخطة الأوسع منفصلين عن نجاح اختبارات هذه الدفعة.
