# معالجة المستندات على أجزاء محفوظة — 20 سبتمبر 2026

## نطاق التغيير

نقطة البداية المطابقة للمصدر المستعاد: `4da17d52f0e101b33bdef268df55610bf0492869`، على فرع `integration/v7-on-main-20260919`. هذه الدفعة لا تدمج `main` ولا تغيّر متغيرات Railway أو بيانات الإنتاج، ولا تفتح احتياطًا مدفوعًا أو تغيير بريد أو تكامل دفع.

المرجع الوظيفي هو خطة V7 الأصلية `MARAS_ADMIN_REORGANIZATION_PLAN_AR_v7_GEMINI_FREE_FIRST_PAID_FALLBACK.md`، المستعادة من مكتبة المشروع. لا تُحتسب أعداد اختبارات البرمجيات أدناه باعتبارها اجتيازًا لحالات القبول الـ192.

## ما أصبح منفذًا

- المهام الجديدة تستخدم الإصدار الثاني للمعالجة. المهام الموجودة قبل الترحيل تبقى على الإصدار الأول دون اختلاق سجل أجزاء لها. المسار المتزامن للتطبيقات السابقة باقٍ.
- المصدر وإعدادات النموذج والتعليمات مثبتة ببصمات. يوجد مخطط محفوظ، ومدخل ومخرج لكل جزء ببصمة مستقلة. لا تعتمد النتيجة النهائية إلا بعد التحقق من التغطية والترتيب، وداخل معاملة نشر تعيد فحص الصلاحية وملكية حجز العامل.
- كل دور للعامل يعالج جزءًا ثم يحرر الدور. العامل المنتهي حجزه لا يستطيع كتابة نتيجة فوق خليفته. المحاولات غير المؤكدة تحتفظ بحالتها ولا تتحول إلى استهلاك صفري مزعوم.
- طلب المنتج يحتفظ بمعرّف واحد وحجز حصة واحد عبر الاستئناف. تعد محاولات المزوّد في سجل منفصل. حجز المهمة النشطة لا يختفي لمجرد مرور 15 دقيقة.
- عند `MAX_TOKENS` يُستبدل الجزء بأجزاء أصغر على حدود الوحدات المتاحة؛ لا تُقبل نتيجة مبتورة، ولا يُستعمل تبديل المفاتيح لتجاوز سبب التوقف. إذا كانت الوحدة العلمية غير قابلة للتقسيم بأمان تتوقف للمراجعة.
- واجهتا الويب والتطبيق تعرضان عدد الأجزاء والوحدات المعتمدة. لا تظهر 100% قبل اعتماد النتيجة. تجهيز PDF مرحلة مستقلة ولا تعني نسبة التوليد جاهزيته.
- أوامر إيقاف مؤقت، واستئناف، وإلغاء تحت فحص المالك والمصدر الحالي وCSRF/حد الطلبات. الإيقاف يحفظ الجزء الجاري قبل وقف الجدولة؛ الإلغاء يمنع النشر اللاحق ولا يعد برد حصة استهلكها نداء جارٍ للمزوّد.
- استعادة الطلب في المتصفح مرتبطة بمعرّف المستخدم، وليس اسم الأداة وحده. تبديل الحساب يعيد إنشاء المكون ويوقف المتابعة القديمة.

## حدود المصدر والأمان

النصوص وWord حتى مليوني محرف مستخرج، وبحد أقصى 4096 وحدة؛ العرض حتى 400 شريحة؛ PDF حتى 1000 صفحة وضمن حد الملف الذي تضبطه الإدارة، وبسقف تشغيلي 50 ميبيبايت. الوحدة النصية المنفردة فوق 48 ألف محرف تتطلب مراجعة بدل قصها. الأجزاء النصية تستهدف 6000 محرف دون قطع وحدة ذرية، وأجزاء PDF أربعة صفحات أصلية مع صفحة سياق مجاورة لكل طرف عند توفرها.

عمليات `pdfinfo` و`pdfseparate` و`pdfunite` محدودة بالوقت والذاكرة وحجم الملفات والمخرجات، داخل مجلد مؤقت خاص وبيئة لا تحمل مفاتيح أو بيانات اتصال قاعدة البيانات. هذا **عزل عمليات وموارد**، وليس ادعاء حاوية مستقلة أو sandbox نظام تشغيل كامل. أضيفت `poppler-utils` و`util-linux` إلى صورة التشغيل. الملفات المشفرة أو التفاعلية أو التي لا يمكن إثبات عدد صفحاتها تُرفض. لا تستعمل هذه الدفعة OCR أو جلب روابط خارجية.

المخططات والمعادلات والصور وخصائص الجداول التي لا يحفظها استخراج Office النصي تتطلب نسخة PDF، بدل إسقاطها بصمت. فحص الأرقام والوحدات في الترجمة إنذار محافظ، لا برهان تكافؤ علمي أو مراجعة اختصاصي. أرقام Word هي مواقع بنيوية وليست أرقام صفحات مخترعة.

النتائج النصية المجمعة محدودة بأربعة ملايين محرف. نافذة استكمال المهمة سبعة أيام؛ الطلب المتوقف مؤقتًا ينتهي بعد هذه النافذة حتى لا يحجز سعة الانتظار إلى الأبد. تحذف سجلات المهام النهائية وأجزاؤها في دفعات صغيرة بعد سبعة أيام من آخر تحديث. تبقى سياسة النتائج النهائية منفصلة.

## الأدلة المحلية المنفذة

`tests/study-processing-plan.test.mjs` و`tests/study-processing-client.test.mjs`: 23 اختبارًا ناجحًا، تشمل مدخلًا أطول من 60 ألف محرف، ترتيب Word والجداول وPowerPoint، حدود وحدات الشيفرة والمعادلات، إفساد البصمة، نقص التغطية، اختلاف الأرقام، ضبط التقدم والهوية وأوامر HTTP. ملف PDF اصطناعي فعلي من 431 صفحة عولج بأدوات Poppler؛ تحقق الاختبار من الصفحات 428–431 وترتيبها، مع فصل الصفحات المطلوبة عن السياق، دون إسقاط النهاية.

`scripts/qa-study-processing.ts`: 10 فحوص ناجحة على PostgreSQL فعلي منفصل وتخزين محلي، بوسيلة نقل Gemini اصطناعية محصورة لا تستطيع الوصول إلى الشبكة. يتعمد عامل فرعي الخروج برمز 73 بعد اعتماد 17 من 40 جزءًا، ثم يتحقق الأب من استكمال 18–40 فقط، وعدم تكرار الأجزاء أو حجز الحصة، وعدم نشر ملف نهائي مبكرًا. تشمل بقية الفحوص `MAX_TOKENS`، والنتائج المبتورة، والاستجابة المتأخرة، وملكية الطالب، وتغيّر المصدر والتعليمات، والإيقاف والإلغاء والانتهاء. يكتب `.data/qa-study-processing-report.json` ويعمل ضمن `qa-study-ci.mjs`.

فحوص `qa-study-tools.ts` القائمة نجحت أيضًا: عشرة فحوص تشمل التكرار المتزامن، مشاركة ناتج المصدر الرسمي بين طالبين دون مشاركة ملكية الناتج، وسحب الاشتراك، وعدم كشف إجابات الاختبار قبل إرساله. البناء الإنتاجي وlint نجحا محليًا. نتائج CI المرتبطة بالالتزام النهائي هي المرجع للنشر، لا نتيجة دورة سابقة.

## بوابات باقية صراحةً

هذه الدفعة لا تغلق قبول الملفات الكبيرة كله: تخطيط توزيع الأسئلة للاختبارات الكبيرة ما زال يستخدم عقد الاختبار الكامل القديم، مع حد استخراج النص السابق 60 ألف محرف، وليس اختيار جزء أول من الملف. إخراج PDF/DOCX للنتائج النصية فوق حد المصدر الخاص بالتصدير، وهو 100 ألف محرف حاليًا، يحتاج مرحلة تصدير كبيرة مستقلة؛ لا يقطع النص ولا يعيد توليد المحتوى. لا يجوز تسويق توليد نص كبير بوصفه PDF كبيرًا ناجزًا.

كما لا توجد هنا مراجعة علمية بشرية لكل صفحة، أو اختبار Gemini حي، أو توقيع APK/AAB/IPA، أو اختبار هاتف فعلي، أو برهان سعة ألف مستخدم، أو مراجعة ملكيات بيانات الإنتاج. لا يتحول نجاح هذه الفحوص الاصطناعية إلى موافقة تسليم المنصة الكاملة.

## تشغيل ومراجعة

تُطبق `0045_study_processing_checkpoints.sql` على قاعدة الاختبار أولًا. لا تضبط الاختبارات على قاعدة إنتاج. يتطلب فحص الاستئناف `.data/qa-database.json` لقاعدة `127.0.0.1/maras_qa` وبيانات `qa-seed.mjs`، ويرفض مفاتيح المزوّد أو الدفع أو علامات Railway الحية. اختبار CI يشغله بعد أدوات الدراسة الحالية. يلزم التأكد من صورة العامل التي تحتوي Poppler ومن نجاح فحوص المصدر وعدم إعادة كتابته خلال الاختبار.

قبل نشر أي ترحيل إنتاجي: نسخة احتياطية قابلة للاستعادة، ومراجعة مجمل ترحيلات فرع التكامل، واختبارات مرحلية، وخطة تراجع تحافظ على المخططات والأجزاء. الرجوع إلى عامل قديم لا يجعله قادرًا على استكمال مهام الإصدار الثاني؛ لا تُحذف هذه السجلات ولا تُنسب لها نتيجة نجاح عند التراجع.
