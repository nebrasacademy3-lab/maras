# تقرير تسليم مشروع مراس العلم

**التاريخ:** 29 أغسطس 2026  
**إعداد:** Manus AI

## الملخص التنفيذي

تم تطوير مشروع **مراس العلم** عبر الويب وتطبيق Expo والخادم وقاعدة البيانات ولوحة الإدارة. شمل العمل توحيد الهوية الرسمية، وإعادة تصميم تجربة التطبيق والفلاتر، وتوسيع التحكم الإداري، وربط البرامج بالمصدر الحكومي، وإصلاح الرفع الكبير، وتطوير الإعلانات وPush، وتحويل المساعد إلى خدمة ديناميكية تقرأ الكتالوج وحساب الطالب، ثم اختبار المشروع في بيئة PostgreSQL محلية فعلية.

أغلقت الاختبارات النهائية عطلين إنتاجيين مهمين لم يكونا ظاهرين من الفحص الثابت وحده. الأول كان حد Proxy الافتراضي الذي يقطع بعض طلبات multipart الكبيرة قبل وصولها إلى التحقق، والثاني كان اختلاف تحقق CORS عن تحقق CSRF الداخلي لنطاق Expo Web، وهو ما كان يجعل المساعد داخل التطبيق يعيد «تعذر التحقق من مصدر الطلب». أصبح كلا المسارين مختبرًا باختبار رجعية.

## نطاق التطوير المنجز

| المحور | النتيجة النهائية |
|---|---|
| الهوية | توحيد علامة M والنجمة الرسمية في الويب وExpo وPWA وAndroid وiOS، مع شفافية وهوامش آمنة ومنع القص. |
| تجربة التطبيق | شاشة ترحيب وتهيئة أولى احترافية، تخزين متوافق مع الويب والأجهزة، اتجاه عربي، وتنقل سفلي أوضح. |
| الفلاتر | استبدال القوائم الطويلة باختيارات جامعات وتخصصات قابلة للبحث في الويب والتطبيق. |
| الفوتر والمتاجر | إضافة App Store وGoogle Play إلى فوتر الويب والتطبيق عبر إعدادات الإدارة، مع إمكانية إخفاء أي زر بترك رابطه فارغًا. |
| المواد | السماح بالاشتراك بعد جاهزية أول درس منشور، وإظهار البقية «قريبًا»، وحساب التقدم من المحتوى المتاح فقط. |
| الإدارة | تعديل ونشر وإخفاء الجامعات والتخصصات والمواد والوحدات والدروس، ومزامنة البرامج الرسمية، وتحكم أوسع بالإعدادات والإعلانات. |
| الرفع | تقدم وسرعة ووقت متبقٍ وإلغاء ومهلة للويب وExpo؛ حد 100MB لطلب المادة و200MB لفيديو الإدارة. |
| الإعلانات | جمهور عام/زوار/طلاب/مستخدم محدد، عرض كشريط أو نافذة أو صندوق، جدولة، انتهاء، رابط آمن وحفظ الإخفاء. |
| الإشعارات | حالة قراءة منفصلة لكل طالب، ملخص تسليم Push، تعطيل الرموز غير الصالحة، وتسليم مجدول مرة واحدة. |
| المساعد | سياق حي للكتالوج والإعدادات والحساب والطلبات والدعم، لهجات وأخطاء إملائية، JSON صارم، روابط مصفاة، وFallback محلي. |
| الشبكة والأمان | CORS وCSRF متطابقان مع `EXPO_WEB_ORIGIN`، ورفض أصول وروابط وجماهير غير صالحة، وتحديث رؤوس الأمان. |

## مصدرية الجامعات والبرامج

يعتمد دليل الجهات على وزارة التعليم ومنصة «ادرس في السعودية» الحكومية.[1] [2] أُعيد بناء جالب البرامج لدعم UUID الرسمي، والبحث عن المؤسسة، والترقيم الصفحي، وإزالة التكرار، وحالات التحقق. لا تعيد المزامنة نشر تخصص أخفاه المدير يدويًا، ولا تستبدل تعديلات الإدارة بلا سبب.

جرى اختبار جامعة الملك سعود من واجهة الإدارة حتى قاعدة البيانات وسجل التدقيق. اكتشفت الخدمة 16 برنامجًا رسميًا فريدًا، وأنشأت 9 تخصصات، وحدّثت 7، وأنشأت 10 روابط جديدة. سجلت قاعدة الاختبار 14 تخصصًا و14 رابطًا مرتبطًا مباشرة بمصدر الملك سعود؛ يعود الفرق إلى توحيد أسماء البرامج المتداخلة وإزالة التكرار على مستوى التخصص العالمي.

## الرفع والملفات

أضيف محرك رفع موحد يعرض النسبة والسرعة والوقت المتبقي ويتيح الإلغاء. يستخدم الخادم بث الملفات إلى التخزين مع التحقق من النوع والبصمة والحجم والعدد. ضُبط `proxyClientMaxBodySize` على 220MB وفق إعداد Next.js المخصص لطلبات Proxy الكبيرة.[3]

| الاختبار | النتيجة |
|---|---|
| دعم الويب | نجح رفع PDF صالح بحجم 10MB، وحُفظت التذكرة والمرفق بالحجم الصحيح. |
| طلب مادة | نجح رفع PDF صالح بحجم 10MB، وحُفظ الطلب والمرفق والإشعار المرتبط. |
| فيديو الإدارة | نجح رفع MP4 صالح بحجم 12MB وربطه بالدرس بعد تجاوز حد القطع السابق البالغ 10MB. |
| بصمة الملف | رُفض ملف يحمل امتداد PDF دون توقيع PDF حقيقي، ثم نجح الملف الصحيح. |
| حالة النجاح | أُصلحت قراءة `event.currentTarget` بعد `await` حتى لا يظهر خطأ كاذب بعد حفظ الطلب أو التذكرة. |

## الإعلانات وPush

تستخدم قراءة الإشعارات جدولًا مستقلًا لكل مستخدم، فلا يؤدي فتح إعلان جماعي لدى طالب إلى اعتباره مقروءًا لدى بقية الطلاب. تختبر خدمة Push استجابة Expo، وتلغي تنشيط رمز `DeviceNotRegistered`، وتعيد للمدير أعداد الأجهزة المستهدفة والمقبولة والمرفوضة. تتطلب النسخ المنشورة معرّف EAS حقيقيًا وفق إعداد Expo Push.[4]

أثبت اختبار الجدولة أن الحملة المستقبلية لا تُرسل عند الإنشاء، ثم تُسلّم بعد وقت البداية، وتسجل `pushDeliveredAt`، ولا تُرسل مرة ثانية عند تشغيل الموزع مجددًا. أضيف `scripts/dispatch-notifications.sh` لتشغيل المسار المحمي من خدمة Cron في Railway، كما بقي زر تشغيل يدوي في لوحة الإدارة.

## المساعد الديناميكي

كان المسار قبل الإصلاح ينتهي عند مهلة 12 ثانية ويعود إلى دليل عام. أصبح يستخدم تفكيرًا منخفضًا ومخطط JSON صارمًا ومهلة خادم 22 ثانية، بينما أصبحت مهلة تطبيق Expo 30 ثانية. تظهر واجهتا الويب والتطبيق رسالة انتظار واضحة بدل التجمد.

قرأ المساعد في الاختبارات مادة Java وسعرها ورابطها، وفهم صياغة لهجية ناقصة، وقرأ جامعة الطالب وتخصصه وآخر طلب مادة ومرفقه وحالته من PostgreSQL. فُصل مسار طلبات المواد `/dashboard?view=requests` عن مسار المشتريات، وأضيفت قواعد تمنعه من اختراع تعديل أو إلغاء طلب بعد الإرسال أو ربطه بالدفع.

كان ملف البيئة القديم يضع `OPENAI_API_URL` كمسار `chat/completions` كامل، بينما كانت الشيفرة تضيف المسار مرة أخرى. أصبح الكود يقبل عنوان `/v1` أو المسار الكامل. اختُبرت الصيغة القديمة على خادم معزول فعادت إجابة صحيحة بحالة 200 خلال 8.58 ثوانٍ.

اختبار Expo Web كشف أن CORS كان يسمح بالنطاق، لكن `sameOriginRequest` لم يقرأ `EXPO_WEB_ORIGIN`. أضيف المتغير إلى التحقق الداخلي واختبار الرجعية. بعد الإصلاح وصل رد Java كامل داخل تطبيق Expo خلال المهلة، وظهرت أزرار المادة والفهرس وموادي وطلب مادة.

## نتائج الاختبارات النهائية

| الفحص | النتيجة |
|---|---|
| اختبارات الخادم | **39/39 ناجحة**. |
| ESLint وTypeScript للخادم | ناجحان بلا أخطاء. |
| Next.js production build | ناجح، مع 155 صفحة ثابتة/ديناميكية قيد التجميع. |
| Expo Doctor | **21/21 ناجح** بعد توحيد `expo-font` إلى `~57.0.2`. |
| TypeScript وESLint للتطبيق | ناجحان بلا أخطاء. |
| تدقيق اعتماديات الإنتاج | `0 vulnerabilities` للخادم والتطبيق وقت الفحص. |
| تصدير Expo Web | ناجح، 8.1MB. |
| تصدير Android | ناجح، 11MB. |
| تصدير iOS | ناجح، 9.4MB. |
| حالة قاعدة البيانات | `/api/health` أعاد 200 و`database=ready` خلال 0.152 ثانية. |
| الصفحة الرئيسية | 200 خلال 0.327 ثانية في البيئة المحلية. |
| كتالوج التطبيق | 200 خلال 0.136 ثانية في البيئة المحلية. |
| الإعدادات العامة | 200 خلال 0.029 ثانية في البيئة المحلية. |

رُفضت لوحة الإدارة دون صلاحية بحالة 403، ورُفض رمز Push الخاطئ بحالة 403، ورُفض طلب المساعد من أصل خبيث بحالة 403. رفض الخادم رابط إعلان يبدأ بـ`//` وجمهور مستخدم محدد بلا بريد بحالة 400. منح CORS الأصل المهيأ فقط، وظهرت رؤوس CSP و`X-Content-Type-Options` و`X-Frame-Options` و`Referrer-Policy` و`Permissions-Policy`.

## ما يلزم قبل الإطلاق العام

لا يحتوي المشروع على أسرار إنتاجية أو معرف EAS حقيقي أو مفاتيح Tap/Resend. يجب على مالك المنصة إدخال القيم في Railway وEAS، ثم تنفيذ إعادة نشر ومراقبة سجل الهجرات. ينبغي ربط Volume دائم بالمسار `/data`، وتعيين `APP_URL` و`NEXT_PUBLIC_SITE_URL` و`EXPO_WEB_ORIGIN` بدقة، وتوليد أسرار مستقلة للجلسات والإدارة والرفع والفيديو.

بعد ضبط Tap، يجب تنفيذ شراء محدود في Tap Sandbox من إنشاء الطلب حتى Webhook وحالة `CAPTURED` والفاتورة. وبعد ضبط EAS، يجب تثبيت نسخة فعلية على Android وiOS، ومنح إذن الإشعارات، والتحقق من تسجيل الرمز ووصول حملة حقيقية وفتح الرابط العميق. كما يُوصى باختبار حمل واختبار اختراق مستقل ونسخ احتياطي مشفر مع تجربة استعادة قبل فتح التسجيل العام.

## تشغيل Push المجدول في Railway

أنشئ خدمة Cron من الشيفرة نفسها واضبط `APP_URL` و`ADMIN_API_TOKEN` ثم استخدم:

```bash
./scripts/dispatch-notifications.sh
```

يشغل الأمر `/api/admin/notifications/dispatch` بطريقة محمية. يمكن تشغيله كل دقيقة أو خمس دقائق بحسب دقة الجدولة المطلوبة، مع الانتباه إلى أن توقيت التنفيذ الفعلي يتأثر بجدولة مزود الخدمة.

## محتويات التسليم

| العنصر | الوصف |
|---|---|
| `backend/` | خادم الويب وقاعدة البيانات والهجرات والاختبارات ولوحات التحكم. |
| `mobile/` | تطبيق Expo النهائي وأصول الهوية وإعدادات البناء. |
| `README.md` | تشغيل ونشر ومتغيرات وفحوص محدثة. |
| `DELIVERY_REPORT_AR.md` | هذا التقرير. |
| `SECURITY.md` | ضوابط الأمان وقائمة الإطلاق. |
| `CATALOG_SOURCES.md` | مصدرية بيانات الجامعات والبرامج. |
| `research_sources.md` | ملاحظات المصادر الخارجية المستخدمة أثناء التطوير. |

## المراجع

[1]: https://www.moe.gov.sa/ar/education/highereducation/Pages/UniversitiesList.aspx "وزارة التعليم السعودية — قائمة الجامعات"
[2]: https://studyinsaudi.sa/ar/Institutions "ادرس في السعودية — المؤسسات التعليمية"
[3]: https://nextjs.org/docs/app/api-reference/config/next-config-js/proxyClientMaxBodySize "Next.js — proxyClientMaxBodySize"
[4]: https://docs.expo.dev/push-notifications/push-notifications-setup/ "Expo — Push notifications setup"
