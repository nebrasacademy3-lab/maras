# تشغيل الدعم وقنوات التواصل

## بعد نشر نسخة Railway

يُطبّق `scripts/start-railway.sh` هجرات Drizzle تلقائيًا عند بدء الخادم. يجب مراقبة السجل حتى يظهر تطبيق `0006_square_living_mummy.sql`، ثم فتح `/api/health` والتأكد من حالة PostgreSQL. لا تُنسخ ملفات `.env` أو مفاتيح التخزين إلى الحزمة؛ أضف متغيرات البيئة من Railway Variables فقط.

## قنوات التواصل

من لوحة الإدارة، افتح قسم **التواصل والإعدادات** في الويب أو قسم **Communication** في Expo. احفظ بريد الدعم، رقم واتساب، رسالة واتساب الافتراضية، ساعات العمل، وروابط X وInstagram وTikTok وYouTube وTelegram وLinkedIn وFacebook وSnapchat وThreads. تُعرض القنوات تلقائيًا في صفحة التواصل والتذييل وصفحة الدعم والتطبيق. يقبل الخادم روابط HTTPS فقط.

## محادثة الدعم

الدعم لا يعمل للزائر. يجب أن يسجل الطالب الدخول لفتح التذكرة أو قراءة المحادثات. عند الإنشاء يختار الطالب قناة المتابعة المفضلة: داخل مراس أو البريد أو واتساب. تبقى المحادثة داخل مراس مصدر الحقيقة المحمي، ويظهر الاختيار للمشرف حتى يعرف القناة المفضلة. يمكن إرسال نص أو حتى خمسة مرفقات في الرسالة، بحد أقصى 15 ميجابايت للملف، ومن الأنواع المدعومة PDF والصور وOffice وTXT.

يرد المشرف من لوحة الإدارة أو من إدارة Expo، ويستطيع تغيير الحالة إلى مفتوحة أو بانتظار الطالب أو محلولة أو مغلقة. لا يستطيع الطالب إغلاق التذكرة، لكنه يستطيع إعادة فتح المحادثة بعد إغلاقها وإرسال رد جديد. الردود الداخلية لا تصل إلى الطالب. تُخزّن بيانات الملف في PostgreSQL وتُخزّن البايتات على Railway Volume، ولا يُفتح الملف إلا بعد جلسة صالحة ونجاح فحص ملكية التذكرة أو الدور الإداري.

## البريد وواتساب الخارجيان

تسجيل البريد وواتساب في نموذج التذكرة هو **تفضيل للقناة** مع روابط مباشرة من صفحة التواصل. الإرسال الآلي خارج المنصة يحتاج مزود بريد أو WhatsApp Business API وإعدادًا مستقلًا؛ لا تعتمد المنصة على مفاتيح غير موجودة ولا تدّعي تسليمًا خارجيًا تلقائيًا. عند تفعيل مزود لاحقًا يجب أن يبقى نص الرسائل خاليًا من كلمات المرور وبيانات البطاقات، وأن يُرسل فقط إلى البريد أو الهاتف المرتبط بالحساب.

## اختبار قبول سريع

بعد تسجيل الدخول، افتح `/support` وأنشئ تذكرة بلا ملف ثم تذكرة بمرفق PDF وصورة. تحقق من ظهور المحادثة في الويب وExpo، أرسل ردًا من الإدارة، افتح المرفق من الطرفين، غيّر الحالة إلى مغلقة، ثم أعد فتحها من حساب الطالب. بعد ذلك غيّر رابط شبكة اجتماعية من الويب وتأكد من ظهوره في Expo والتذييل. اختبر أيضًا أن الزائر يحصل على توجيه لتسجيل الدخول ولا يرى أي تذكرة أو مرفق.
