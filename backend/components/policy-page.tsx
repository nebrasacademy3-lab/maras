import Link from"next/link";import{ChevronLeft,ShieldCheck}from"lucide-react";import{SiteHeader}from"./site-header";import{SiteFooter}from"./site-footer";
export function PolicyPage({title,intro,sections}:{title:string;intro:string;sections:{title:string;body:string}[]}){return <main><SiteHeader/><section className="page-hero"><div className="container"><div className="breadcrumbs"><Link href="/">الرئيسية</Link><ChevronLeft size={13}/><span>{title}</span></div><h1>{title}</h1><p>{intro}</p></div></section><section className="content-page"><div className="container legal-layout"><article className="legal-content"><div className="legal-updated"><ShieldCheck size={16}/> آخر تحديث: 22 أغسطس 2026</div>{sections.map((section,index)=><section id={`section-${index+1}`} key={section.title}><span>{String(index+1).padStart(2,"0")}</span><div><h2>{section.title}</h2><p>{section.body}</p></div></section>)}</article><aside><strong>في هذه الصفحة</strong>{sections.map((section,index)=><a key={section.title} href={`#section-${index+1}`}>{section.title}</a>)}<Link href="/support">لديك سؤال؟ تواصل معنا</Link></aside></div></section><SiteFooter/></main>}
