# مراس العلم — تسليم تطوير الإدارة والربط — 11 سبتمبر 2026

## حالة هذا التسليم

هذه **نسخة المصدر الكاملة المعدّلة** من الملف المرفوع `maras-main.zip`: الويب، واجهات الخادم، تطبيق Expo، الترحيلات، الاختبارات، وملفات النشر الأصلية. ليست ملفات تعديل منفصلة، وليست APK أو IPA.

أُنجزت التعديلات المبينة أدناه واختُبر الجزء الذي أمكن تشغيله في بيئة العمل. **لم ينجح البناء الإنتاجي هنا بسبب غياب حزم المشروع، ولم تُشغّل الترحيلات على PostgreSQL حقيقي. لذلك لا تُعامل هذه النسخة على أنها اعتماد نهائي للإنتاج أو ضمان خلو من الثغرات.** يجب اجتياز قائمة التحقق أدناه في بيئة تجريبية قبل استبدال الخدمة الحالية.

هذا التقرير هو المرجع الحالي. تقارير 9 سبتمبر وما قبلها وملفات checksums القديمة محفوظة للتاريخ ولا تثبت حالة هذا الإصدار. ملف سلامة المصدر الحالي: `SOURCE_CHECKSUMS_20260911.sha256`.

## 1. ملف الطالب 360

أضيفت 19 عملية إدارية في 8 أقسام، باستخدام واجهات خادم مشتركة للويب والتطبيق:

| القسم | العمليات المضافة |
|---|---|
| الحساب | تعديل الاسم والجوال والبيانات الدراسية؛ تفعيل الحساب أو إيقافه |
| المواد | منح مادة إداريًا؛ إيقاف الوصول مؤقتًا، استئنافه، تمديده أو إلغاؤه |
| أدوات مراس | منح استحقاق مستقل؛ تفعيل الاستحقاق أو إلغاؤه |
| الاهتمامات | إلغاء انتظار مادة أو اهتمام بمسار؛ إزالة عنصر من المفضلة أو السلة بطلب الطالب |
| الدعم | الرد العام أو الملاحظة الداخلية؛ تعديل حالة التذكرة |
| طلبات المواد | متابعة الحالة وربط المادة المنشورة عند إعلان إتاحتها |
| الإشعارات | إرسال إشعار؛ إلغاء إشعار مؤهل لم يُرسل؛ إعادة محاولة Push فاشل |
| الجلسات والأجهزة | إنهاء جلسة أو جميع الجلسات؛ تعطيل إرسال Push لجهاز |

توجد نماذج داخل أقسام الملف في الويب، وبطاقة إجراءات في إدارة الطالب بالتطبيق. أضيف بحث بالسجلات الأقدم بدل الاعتماد على نافذة التفاصيل الحديثة فقط. تعرض واجهة البحث أول 100 نتيجة مع العدد وإمكانية تضييق البحث؛ لم تتحول كل شاشة تاريخية قديمة إلى عرض غير محدود.

كل إجراء جديد يتحقق من صلاحية المشرف وملكية السجل للطالب، ويطلب سببًا، ويسجل نتيجة تدقيق. يدعم المسار الجديد منع تكرار العملية عند إعادة المحاولة بمعرّف العملية ومحتوى الطلب داخل معاملة قاعدة بيانات. تعديل بيانات الطالب يتحقق من عدم تغير نسختها منذ فتح النموذج.

تغيير الجوال يلغي توثيقه السابق. إيقاف الحساب ينهي الجلسات ويعطل Push دون حذف الأجهزة المعتمدة نهائيًا. المنح الإدارية ليست دفعات ولا تُزوّر فواتير. سجلات المدفوعات والهوية والتوثيق لا تتحول إلى حقول قابلة للتغيير الحر.

حُسّنت مؤشرات أساسية بحسابات مباشرة من قاعدة البيانات، منها الوصول النشط والمدفوعات والدعم والإشعارات. لا يَعني ذلك أن كل ملخص تاريخي، بما فيه المكافآت والإحالات القديمة، صار غير محدود.

## 2. إدارة مشتركي المادة وقائمة الانتظار

أضيفت صفحة `/admin/courses/[slug]` ورابط لها من إدارة المواد، مع واجهة مماثلة في التطبيق. تعرض أعداد مشتركي المادة وحالات وصولهم، وأسماء الطلاب ووسائل التواصل، ومن سجّلوا تنبيه الإتاحة، مع البحث والتصفية والتنقل بين الصفحات وفتح ملف الطالب.

أضيف وضع اشتراك مستقل لكل مادة:

- **تلقائي:** يحافظ على السلوك السابق، ويتطلب درسًا جاهزًا.
- **مفتوح:** يسمح بالاشتراك في المادة المنشورة حتى قبل تجهيز الدروس.
- **مغلق:** يمنع الاشتراك حتى لو كانت الدروس موجودة.

المادة غير المنشورة لا تصبح قابلة للشراء بمجرد اختيار «مفتوح». عند إعلان الإتاحة، يستطيع المشرف تشغيل معالجة التنبيهات من صفحة المادة، ويستمر العامل الدوري الموجود في معالجة الباقي وفق إعداد تشغيله.

استُبدلت معالجة أحدث 2000 تسجيل بانتظار مؤهل على دفعات، حتى لا تُهمل التسجيلات القديمة. يُحفظ إشعار الإتاحة وتحديث التسجيل معًا في قاعدة البيانات، مع أقفال وتفادي التكرار لكل دورة تفعيل. عند الإلغاء، يلغى إشعار ذلك التسجيل المؤهل الذي لم تبدأ معالجته؛ إعادة التفعيل تحصل على إصدار جديد.

هذه ضمانات على مستوى منطق التخزين والمعالجة المقصود، وليست إثباتًا لاختبارات تنافس على PostgreSQL في هذا التسليم. قبول مزود Push للطلب لا يثبت وصوله للجهاز أو قراءة الطالب له. لا يمكن سحب إشعار قُبل أو بدأ إرساله، ولا يوجد ضمان إرسال خارجي مرة واحدة في كل أعطال الشبكة.

## 3. إصلاح مفاتيح أدوات مراس وتشخيص التشغيل

صُحّح إرسال مخطط JSON الخاص بالاختبارات إلى `responseJsonSchema` بدل تمريره كنوع المخطط الآخر. وُسّع التعامل مع رفض المفتاح أو الحصة أو اختلاف إتاحة النموذج بين المشاريع للانتقال إلى مفتاح بديل مناسب، بدل توقف المجموعة عند المفتاح الأول.

أضيف في الويب والتطبيق قسم **فحص التشغيل الفعلي واستبدال المفاتيح**. يمكن اختبار المحادثة، التلخيص، الترجمة والاختبارات باستخدام إعداد الخدمة المحفوظ نفسه ومسار التوليد الذي يستخدمه الطلاب. اختبار الاختبارات يتحقق أيضًا من بنية JSON. يعرض نتيجة آمنة ورمز خطأ عند الفشل، دون إرجاع المفتاح الخام.

أضيف استبدال قيمة مفتاح موجود مع تشفيرها والتحقق من نسخة السجل. لا تعيد نتيجة فحص قديمة تفعيل مفتاح عطّله المشرف أو تكتب حالة مفتاح استُبدل أثناء تنفيذ الطلب. يظهر تعذر فك التشفير بدل اعتباره نجاح حفظ.

**لم يُختبر مفتاح إنتاج فعلي هنا.** التشخيص المضاف يجب تشغيله بعد النشر، وقد يستهلك حصة لدى المزود. وهو ليس اختبارًا لرفع الملفات أو الدفع أو جميع الحدود التشغيلية.

مرجع الحقل الرسمي: https://ai.google.dev/api/generate-content

## 4. الحماية والاتساق والمزامنة

حُدّدت المدخلات وأحجام الطلبات، وطبقت حماية الأصل وطلب التحقق الإضافي للمشرف في الإجراءات الحساسة الجديدة. يمنع مسار الطالب تعديل سجل يخص طالبًا آخر، أو رابط إشعار خارجي أو إداري، أو إعادة استعمال معرّف عملية بمحتوى مختلف.

أضيفت حماية متزامنة لعدم إزالة آخر مدير نشط، وإبطال الجلسات عند تعديلات حساسة في إدارة الحساب. وصُحّح مسار الدعم القديم حتى لا يظهر نص الملاحظة الداخلية في معاينة إشعار الطالب.

حُسّنت أقفال العامل الدوري بحيث يُكتسب القفل ويُحرر على اتصال قاعدة البيانات نفسه، مع التخلص من الاتصال عندما يتعذر ضمان تحريره. أضيفت إشارات مزامنة لتغييرات استحقاقات الأدوات وقوائم انتظار المواد وأجهزة Push، وتحديث البيانات الإدارية ذات الصلة في التطبيق. حُميت ذاكرة الكتالوج المؤقتة من استبدال تحديث جديد بنتيجة طلب قديم، وأُخفيت المؤسسات غير العامة من الكتالوج العام.

هذه تقوية للمسارات المذكورة؛ ليست مراجعة اختراق شاملة لكل الملف الأصلي، ولا إثباتًا بأن جميع الوظائف القديمة خالية من الأخطاء. لم يُنفّذ تدقيق حي للمدفوعات أو صلاحيات مزودي الخدمات أو البنية السحابية.

## 5. SEO

أضيف مركز `/admin/seo` يعرض جاهزية إعدادات الفهرسة وأعداد الصفحات العامة ونواقص العناوين والوصف والغلاف وتكرار بعض العناوين. أضيف API إداري للتقرير، وأعيد تنظيم توليد sitemap لتقليل المرور المتكرر على كامل المواد.

أضيف فحص HTTP مستقل لصفحات الموقع العامة:

```bash
npm run seo:audit -- --base https://marasalelm.com --limit 100 --output seo-audit-20260911.json
```

يشترط تحديد النطاق صراحة، ولا يسجل دخولًا أو يرسل طلبات إدارية. يفحص حدود العينة، الاستجابات، العناوين والوصف، canonical، H1، لغة الصفحة، الصور بلا alt، JSON-LD، وإشارات منع الفهرسة. يمنع الانتقال إلى نطاق آخر ويحد حجم الاستجابة والوقت وعدد الصفحات. يرفض استبدال ملف نتيجة موجود؛ اختر اسمًا جديدًا عند الإعادة.

التقرير يعرض نتائج ومحدوديات، **ولا يولد درجة SEO وهمية**. ليس بديلًا عن Lighthouse أو Search Console، ولا يحلل تنفيذ JavaScript والأداء أو كل قواعد robots.txt. لا يثبت أن Google فهرس الصفحات. صفحات الإدارة والطلاب الخاصة تبقى خارج الفهرسة عمدًا.

توضح Google أن sitemap لا يضمن الفهرسة أو تحسن الترتيب: https://developers.google.com/search/help/crawling-index-faq

## 6. الترحيل وملفات المصدر

الترحيل الجديد هو `drizzle/0029_admin_control_completion.sql`، ومعه snapshot وإدخال journal مطابق. يضيف وضع الاشتراك، وسجل العمليات الإدارية، وإصدار تفعيل الانتظار، وفهارس مساعدة، ومحفزات مزامنة الحساب. يجب تنفيذ سلسلة الترحيلات بالترتيب، بما فيها `0028` وما سبق حسب حالة قاعدتك؛ لا تنفذ الملف منفصلًا متجاوزًا سجل Drizzle.

حُفظت إصدارات الحزم وملفات القفل الأصلية. لم تُرقّ Expo أو Next أو React بصورة تخمينية. أضيف فقط أمر فحص SEO إلى scripts في ملف حزم الجذر.

أهم الملفات الجديدة: `lib/student-control-contract.ts`، مسارا actions وrecords داخل API الطالب، `lib/course-launch-notifications.ts`، صفحة وAPI جمهور المادة، `lib/gemini-request-policy.ts`، مكونات الإدارة الجديدة في الويب والموبايل، و`lib/seo-readiness.ts` و`scripts/audit-public-seo.mjs`.

## 7. نتائج التحقق الفعلية

| الفحص | النتيجة | حدود النتيجة |
|---|---|---|
| اختبارات الويب قبل التعديل | 326 ناجحًا من 336؛ 10 فاشلة | خط أساس في بيئة الحزم الناقصة |
| اختبارات الويب بعد التعديل | 384 ناجحًا من 394؛ 10 فاشلة | الفشل المتبقي من الحزم والبناء المفقودين؛ الأمر ينتهي بخروج غير صفري |
| الزيادة في اختبارات الويب | 58 حالة ناجحة | منها اختبارات سلوكية ومنها فحوص عقود المصدر؛ ليست كلها اختبارات نهاية إلى نهاية |
| إجراءات ملف الطالب | 24 حالة ناجحة | تشغيل المسار مع قاعدة محاكاة ومعاملات محاكاة، وليس PostgreSQL حقيقيًا |
| اختبارات التطبيق | 48 ناجحًا من 62؛ 14 فاشلة | الحالات الفاشلة تعذر تحميل React؛ لا يوجد اعتماد تشغيل للتطبيق |
| تحليل TS/TSX المعدّل والمضاف | 42 ملفًا بلا أخطاء صياغة | transpileModule فقط؛ ليس typecheck كاملًا |
| TypeScript الكامل | لم ينجح | حزم وتعريفات ناقصة، ومنها types/node |
| بناء الويب | لم ينجح | `next: not found`؛ لا توجد حزمة إنتاج مبنية |
| ترحيل PostgreSQL وبناء APK/IPA | لم يُنفّذا | لا توجد قاعدة تشغيل أو بيئة EAS معتمدة هنا |

حالات الويب العشر المتبقية: ست حالات تتطلب `tsx`، وملف OAuth يحتاج `jose`، وملف رؤوس الاستجابة يحتاج `next`، واختبارا تشغيل يحتاجان بناء خادم Next. لم تُخفَ هذه النتائج أو تُحوّل إلى skipped.

سجلات الأوامر وملخص JSON موجودة داخل `verification/20260911/`. أُجريت اختبارات الصياغة بالنسخة العامة المتاحة TypeScript 5.8.3، لا بديلًا عن النسخة المقفلة في المشروع. بيئة التنفيذ Node 22.16.0.

## 8. الترقية والتشغيل — قبل الإنتاج

1. **خذ نسخة احتياطية** من PostgreSQL وملفات الفيديو والرفع ومفاتيح البيئة. استخدم نسخة تجريبية من القاعدة أولًا. لا تستبدل الأسرار بقيم `.env.example`.
2. ثبت Node وفق `engines` في المشروع (22.13 أو أحدث ضمن البيئة المدعومة)، ثم نفذ `npm ci` من الجذر و`npm --prefix mobile ci`. عدم إضافة node_modules إلى الأرشيف مقصود؛ تُثبت من ملفات القفل.
3. عيّن `DATABASE_URL` الصحيح في بيئة العملية. **drizzle.config.ts لا يحمّل `.env` تلقائيًا**؛ نسخ الملف وحده لا يكفي لأمر الترحيل. في PowerShell، مثال: `$env:DATABASE_URL = "رابط قاعدة الاختبار"`، ثم `npm run db:migrate`. على الاستضافة اضبط المتغير في إعدادات الخدمة. لا تشغّل ترحيلًا دون التأكد من عنوان القاعدة المستهدفة.
4. بعد نجاح الترحيلات نفذ `npm run build` و`npm test` و`npm run lint`. وعلى الموبايل: `npm --prefix mobile run typecheck` و`npm --prefix mobile test` و`npm --prefix mobile run doctor`. لا تتجاوز فشل هذه الخطوات لمجرد أن اختبارات الصياغة نجحت هنا.
5. احتفظ بقيمة `AI_KEYS_ENCRYPTION_KEY` المستقرة. **لا تغيّر مفتاح التشفير دون خطة لإعادة تشفير القيم أو استبدال المفاتيح المخزنة.** إذا كانت القيم القديمة تستخدم مفتاح MFA كخيار احتياطي، فإن تعيين سر AI مختلف يقتضي معالجة القيم القديمة، لا مجرد إعادة تشغيل الخادم. احتفظ أيضًا بأسرار MFA والجلسات الحالية.
6. بعد النشر التجريبي شغّل فحص أدوات مراس لكل خدمة من الإدارة؛ راجع حالة التفعيل والنموذج والحصة عند أي رفض. اختبر عملية طالب فعلية بملف تجريبي بعد نجاح فحص النص؛ الفحص الجديد لا يغطي الملفات.
7. تحقق من إعداد مزود Push وأذونات الأجهزة على Android وiOS والويب. لا تعطّل `LIFECYCLE_SCHEDULER_ENABLED` إن كنت تعتمد على العامل الدوري. اختبر مادة مغلقة، اشترك بتنبيهها من حساب طالب، افتحها من المشرف، وتحقق من صندوق إشعارات الطالب وإشعار الجهاز وسلوك الإلغاء وإعادة التفعيل.
8. تحقق من رابط API في إعدادات التطبيق؛ ابنِ نسخة EAS تجريبية من `mobile/` ثم اختبر التسجيل والمصادقة الإضافية وإجراءات الإدارة والجلسات. هذه الحزمة لا تحتوي ملف APK/IPA جديدًا.
9. اضبط `APP_URL` و`NEXT_PUBLIC_SITE_URL` على النطاق العام HTTPS، واجعل `SEO_INDEXING_ENABLED` غير مفعل في التجربة ومفعلًا في الإنتاج المقصود فهرسته. نفذ فحص SEO على النطاق المنشور ثم راجع Search Console وLighthouse؛ نتيجة هذا التسليم ليست درجة أداء أو SEO للموقع الحي.
10. اختبر منح المادة وإيقافها واستئنافها، منع تعديل طالب آخر، تكرار نفس الطلب، سجل التدقيق، عدم ظهور الملاحظات الداخلية، وتنفيذ عمليتي مشرف متزامنتين على قاعدة تجريبية. اختبر الدفع واستقبال webhook بمسار المزود التجريبي قبل اعتماد تغيير الإنتاج.

مسار Railway الأصلي ما زال موجودًا، وبدء التشغيل الأصلي يدعم تنفيذ الترحيلات عند ضبط بيئته. لا تشغّل أمر النشر على الخدمة الحالية قبل التحقق من النسخة الاحتياطية والنتائج السابقة. الرجوع إلى المصدر السابق وحده لا يعكس بيانات غُيرت؛ استخدم خطة رجوع ونسخة قاعدة متوافقتين.

## 9. ما لا يتضمنه التسليم

لا يتضمن نشرًا فعليًا، نتيجة Lighthouse على الموقع الحي، اعتماد اختراق، ضمان انعدام الثغرات، تحديث متاجر التطبيقات، مفاتيح مزودي الخدمات، أو تأكيد أن كل وظيفة قديمة في المشروع اختُبرت. بعض تفاصيل التاريخ القديم والوظائف المالية تظل في شاشاتها المتخصصة ولا تُفتح كتعديل حر في ملف الطالب. التقرير يفرق بين التطوير المنفّذ وبين التحقق الإنتاجي الذي ما زال مطلوبًا.
