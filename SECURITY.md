# سياسة أمن منصة مراس العلم

## النطاق والضمانات

تستخدم المنصة **دفاعًا متعدد الطبقات** لحماية الطلاب والإدارة والملفات والمدفوعات. لا يمكن لأي مراجعة برمجية أو إعداد أن يضمن انعدام الثغرات تمامًا؛ لذلك يجب إجراء اختبار اختراق مستقل قبل الإطلاق العام، ومراقبة الاعتماديات والسجلات والنسخ الاحتياطية بصورة مستمرة.

هذه الوثيقة تصف الحماية المطبقة في الحزمة الحالية، ولا تستبدل سياسة الخصوصية أو شروط الاستخدام أو متطلبات الامتثال القانوني.

## الحماية المطبقة

| المجال | التطبيق |
| --- | --- |
| الجلسات | رموز opaque عشوائية، تخزين hash للجلسة في PostgreSQL، جلسات HttpOnly وSameSite=Lax، مدة انتهاء، وإبطال الجلسات عند إعادة تعيين كلمة المرور. تطبيق Expo يخزن الرمز في SecureStore ويستخدم Bearer فقط. |
| كلمات المرور | PBKDF2-HMAC-SHA256 بعدد 210,000 تكرار، salt عشوائي، حد طول 10–128، واشتراط رقم ورمز. |
| CSRF | فحص Origin وSec-Fetch-Site للطلبات المتغيرة، مع قبول MOBILE_APP_URL معرف صراحة. طلبات Expo تتطلب x-meras-client: mobile-v1. |
| تحديد المعدل | حدود ذرية في PostgreSQL لمحاولات الدخول، إعادة التعيين، الدعم، جلسات الفيديو، الإدارة، الرفع وطلبات المواد. يلزم أيضًا وضع حد على مستوى Railway أو WAF. |
| الإدارة | فصل ADMIN_API_TOKEN عن ADMIN_UPLOAD_TOKEN، مقارنة ثابتة الزمن، صلاحية admin فقط للجلسة، وسجل تدقيق للعمليات الحساسة. رمز الرفع لا يمنح صلاحيات لوحة الإدارة. |
| الملفات | تخزين خاص على Volume، منع traversal، صلاحيات ملفات 0600، أسماء مفاتيح عشوائية، حدود حجم وعدد، ورفض SVG/HTML والأنواع غير المسموحة. الفيديو والشعارات والمرفقات تتحقق من magic bytes إلى جانب MIME المعلن. |
| الرفع | بث مباشر إلى ملف مرحلي بدل تجميع الملفات الكبيرة في RAM، إجمالي وحدود لكل ملف وعدد الأجزاء، إلغاء stream عند التجاوز، حذف المرحلي عند الفشل، وربط سجلات المرفقات داخل transaction. |
| الفيديو | روابط موقعة قصيرة العمر، تحقق من الحساب والوصول والصلاحية، بث خاص مع no-store وnosniff وRange مضبوط. منع التقاط الشاشة في التطبيق يرفع الحماية لكنه لا يمنع التصوير بجهاز خارجي. |
| Tap | جسم Webhook محدود، رفض التوقيع المفقود، تحقق HMAC SHA-256 hashstring بمقارنة ثابتة الزمن، إعادة جلب العملية من Tap، وفحص idempotency قبل منح الوصول. |
| الاستجابات | no-store للمسارات الحساسة، nosniff، SAMEORIGIN، HSTS، Referrer-Policy، Permissions-Policy، COOP وCORP. |
| المتصفح | Content-Security-Policy تمنع objects وتقيّد scripts والاتصالات والإطارات، والصور الخارجية لا تمر عبر image optimizer مفتوح. |
| التزامن | قنوات SSE محمية بالجلسة والدور والنطاق مع حد اتصالات لكل هوية/IP، ولا ترسل بيانات الصفوف نفسها؛ ترسل أرقام مراجعات فقط ثم تعيد الواجهة جلب API المصرح به. |
| البيع | الخادم يرفض السلة والدفع لمادة غير مكتملة الفيديوهات أو بلا درس تجريبي جاهز، حتى لو استُدعي API مباشرة. |
| قاعدة البيانات | Pool محدود بمهلات اتصال واستعلام، فهارس للمسارات عالية الاستخدام، وهجرات PostgreSQL قابلة للتشغيل على Railway. |

## متغيرات Railway المطلوبة

اضبط المتغيرات في خدمة `backend` ولا تضع أي مفتاح سري في Expo أو في ملفات الواجهة:

```text
DATABASE_URL=...
NEXT_PUBLIC_SITE_URL=https://your-service.up.railway.app
APP_URL=https://your-service.up.railway.app
MOBILE_APP_URL=https://your-mobile-web-origin.example
DATABASE_SSL=false
DATABASE_SSL_REJECT_UNAUTHORIZED=true
RUN_DB_MIGRATIONS=true
AUTO_SEED_CATALOG=true
CATALOG_SEED_MODE=core
UPLOAD_DIR=/data/uploads
VIDEO_SIGNING_SECRET=<32+ random characters>
ADMIN_API_TOKEN=<long random token>
ADMIN_UPLOAD_TOKEN=<different long random token>
TAP_SECRET_KEY=<Tap server secret>
TAP_WEBHOOK_SECRET=<dedicated Tap HMAC secret when provided>
# Optional general-answer provider; all keys stay server-side.
OPENAI_API_KEY=<server-only key>
OPENAI_API_URL=https://api.openai.com/v1
ASSISTANT_MODEL=gpt-4o-mini
```

استخدم سرين مختلفين على الأقل للإدارة والرفع. يمكن توليد أسرار مناسبة مثل:

```bash
openssl rand -base64 48
```

لا تضبط `NODE_TLS_REJECT_UNAUTHORIZED=0`، ولا تسجل الأسرار في Git أو سجلات Railway. دوّر الأسرار عند تغيير الموظفين، أو الاشتباه بتسريب، أو وفق جدول دوري. إذا كان `TAP_WEBHOOK_SECRET` فارغًا تستخدم الحزمة `TAP_SECRET_KEY` كبديل متوافق، لكن يفضّل ضبط سر Webhook مستقل إذا كان حساب Tap يوفره.

## Tap Webhook

يجب ضبط عنوان Webhook في Tap إلى:

```text
https://your-service.up.railway.app/api/webhooks/tap
```

تعتمد الحزمة على توثيق Tap الرسمي لبناء `hashstring` من حقول العملية ومقارنة قيمة `hashstring` أو `x-hashstring`. لا تقبل Webhook بلا توقيع، ولا تمنح وصولًا اعتمادًا على جسم الطلب وحده؛ إذ يعاد التحقق من العملية عبر Tap API ثم تطبق idempotency في PostgreSQL. راجع [توثيق Tap Webhooks](https://developers.tap.company/docs/webhook) عند تغيير نسخة API أو أسماء الحقول.

## التخزين والنسخ الاحتياطي

اربط Railway Volume بمسار `/data`، واترك `UPLOAD_DIR=/data/uploads`. لا تجعل مجلد الرفع تحت `public` ولا تعرض مفاتيح التخزين مباشرة. التخزين على Volume ليس نسخة احتياطية؛ أنشئ نسخًا مشفرة دورية لقاعدة PostgreSQL والملفات، واختبر الاستعادة فعليًا، وحدد مدة الاحتفاظ والوصول إلى النسخ.

## تشغيل الإدارة بأمان

أنشئ أول حساب إداري من قناة موثوقة، واستخدم كلمات مرور فريدة ومدير كلمات مرور، وقلل عدد الحسابات الإدارية، وأوقف الحسابات المغادرة فورًا. لا تشارك `ADMIN_API_TOKEN` في المتصفح أو التطبيق. إذا احتجت تكاملًا آليًا، استخدم الرمز عبر خادم موثوق فقط، وقيّد الوصول إليه على مستوى الشبكة أو WAF وسجّل استخدامه.

## المراقبة والاستجابة

راقب أخطاء 401 و403 و429، وفشل Webhook، وفشل الهجرات، وامتلاء Volume، وزيادة أخطاء قاعدة البيانات. احتفظ بسجلات التدقيق دون كلمات مرور أو رموز جلسات أو مفاتيح Tap، وحدد صلاحيات ومدة الاحتفاظ بها. عند الاشتباه بحادث: أوقف الرمز المتأثر، دوّر الأسرار، ألغِ الجلسات، احفظ السجلات، افحص منح الوصول والمدفوعات، ثم استعد من نسخة موثوقة بعد تحديد السبب.

## الفحوص المطلوبة قبل الإطلاق

نجحت الحزمة في ESLint وTypeScript وNext production build و39/39 اختبار عقود وتكامل، وExpo Doctor ‏21/21، وتصدير Expo للويب وAndroid وiOS كلًا على حدة. نجحت اختبارات شبكة فعلية على PostgreSQL محلية، بما فيها التسجيل والدخول والصلاحيات وCORS/CSRF والمساعد والإعلانات وحالة قراءة الإشعارات وتسليم Push المجدول مرة واحدة. نجح رفع PDF صالح بحجم 10MB في الدعم وطلب المادة، وMP4 صالح بحجم 12MB في الإدارة، ورُفض ملف ذو توقيع مزيف. أعاد `npm audit --omit=dev` صفر ثغرات معروفة وقت الفحص للخادم والتطبيق. لا تحتوي نسخة التسليم على أسرار Tap/Resend أو معرف EAS حقيقي، لذلك يجب بعد النشر تنفيذ Tap Sandbox وإشعار Push على جهاز فعلي، ثم SAST/DAST واختبار حمل واختبار اختراق مستقل واختبار استعادة النسخ الاحتياطية.

## الإبلاغ عن الثغرات

لا تنشر تفاصيل ثغرة غير مُعالجة أو أسرارًا في البلاغ. أرسل وصفًا قابلًا لإعادة الإنتاج، الأثر، النطاق، والنسخة المتأثرة إلى قناة الدعم الأمنية التي تعتمدها إدارة مراس، وامنح الفريق وقتًا معقولًا للإصلاح قبل النشر العام.
