# ابدأ هنا — تحديث مراس العلم، 6 سبتمبر 2026

هذه نسخة مبنية من أرشيفك الأصلي `maraseeeeeeeeeeeee.zip`، وباسم المجلد الداخلي نفسه `maraseeeeeeeeeeeee/`. جميع مسارات ملفات الأصل الـ807 محفوظة دون حذف، مع تطبيق التعديلات وإضافة 40 ملفًا؛ الإجمالي 847 ملفًا. حُفظت أيضًا المجلدات الفارغة والملفات المخفية. ليست ملفات ترقيع أو مشروعًا بديلًا.

أُعيد الملفان `.env.railway.generated` و`tsconfig.tsbuildinfo` من الأصل دون تغيير لتكون النسخة كاملة كما طلبت. **الأرشيف خاص: ملف البيئة يحتوي قيمًا حساسة كما وردت منك؛ لا تنشره أو الأرشيف في مستودع عام.** بقي استبعاده من Git وسياق Docker كما في التعديلات. لا تحتوي النسخة قاعدة الإنتاج أو الفيديوهات الخارجية أو APK/AAB/IPA مبنية، لأنها لم تكن ضمن الأرشيف الأصلي.

**قبل تبديل النسخة الحية:** احتفظ بنسخة احتياطية من PostgreSQL ومن التخزين، وانشر على بيئة اختبار أولًا. بناء Next/Expo وفحص الأنواع الكامل لم يكتمل هنا بسبب تعذر الوصول إلى سجل npm وغياب الاعتماديات. نتائج الاختبارات المنفذة وحدودها في `docs/validation-2026-09-06.md`؛ ليست شهادة خلو من جميع الأخطاء أو اختبار جميع الأجهزة.

## 1. النشر الآمن دون فقد البيانات

انسخ متغيراتك الحالية من الاستضافة إلى بيئة الاختبار بطريقة آمنة، ولا تستبدل قاعدة البيانات بقيم `.env.example`. حُفظ `.env.railway.generated` في هذه النسخة الخاصة لأنه كان موجودًا في الأصل، ولم يُحمَّل إلى الاستضافة أو يُفعَّل تلقائيًا. قبل نشر ملفات المشروع أو تشغيل فحص `npm run preflight`، احتفظ بنسخة آمنة منه خارج مجلد النشر ثم أبعده عن نسخة النشر؛ الفحص يرفض وجوده عمدًا لحماية الأسرار. ملف `tsconfig.tsbuildinfo` محفوظ من الأصل أيضًا، وهو كاش قديم يمكن حذفه من نسخة البناء ليُعاد توليده. إن استُخدمت هذه الأسرار بالفعل، دوّر أسرار الجلسات والتوقيع والمهام بصورة منظمة. **لا تغيّر مفاتيح تشفير MFA وAI عشوائيًا قبل فك/إعادة تشفير البيانات التابعة لها أو إعداد خطة استعادة.**

من جذر المشروع الذي يحتوي `package.json` و`Dockerfile`:

```bash
npm ci
# عرّف DATABASE_URL ومتغيرات البيئة في الاستضافة قبل تنفيذ الهجرة.
npm run db:migrate
npm run lint
npm run build
npm run typecheck
node --test --test-concurrency=1 tests/*.test.mjs
npm run preflight
npm start
```

لبيئة التطوير: انسخ `.env.example` إلى `.env.local`، واضبط بيانات قاعدة الاختبار ثم شغّل `npm run dev`. بعض أدوات الطرفية لا تقرأ ملفات Next تلقائيًا؛ مثال التحقق المحلي: `node --env-file=.env.local scripts/preflight.mjs`. لا تنسخ ملف البيئة إلى المستودع أو رابط تنزيل عام.

**الهجرة الجديدة `0028_catalog_deletion_ledger.sql` لازمة قبل تشغيل الكود الجديد على قاعدة موجودة.** تضيف سجلًا يمنع إعادة المواد والجامعات والتخصصات المحذوفة من البيانات المدمجة. بعد أخذ النسخة الاحتياطية، طبّق الهجرات بالترتيب مع الهجرات القديمة، لا باستخدام `db:push` على الإنتاج. لا تحذف جداول المستخدمين أو الاشتراكات. سكربت Railway يطبّق الهجرات عند `RUN_DB_MIGRATIONS=true`؛ احتفظ بهذا الإعداد في النشر المعتاد.

الحذف السابق الذي أعادت نسخة قديمة إنشاء سجله بالفعل يحتاج مراجعة إدارية؛ لا تحذف هذه الهجرة سجلًا قائمًا تلقائيًا لأنه قد يكون أُعيد إنشاؤه عمدًا. أما الحذف الجديد من هذه النسخة فيسجل العلامة في المعاملة نفسها.

## 2. البريد والهوية

اضبط `RESEND_API_KEY` و`EMAIL_FROM` على نطاق مرسل موثّق، و`APP_URL` و`NEXT_PUBLIC_SITE_URL` على `https://marasalelm.com`. افتراضيًا تُرسل القوالب الجديدة مباشرة من الملفات داخل `emails/resend/`، حتى لو كانت معرّفات القوالب القديمة موجودة في Variables. اترك `RESEND_USE_HOSTED_TEMPLATES=false` لتجنب رجوع التصميم القديم.

لاستخدام قوالب Resend المستضافة: حدّث القوالب الثلاثة بمحتوى هذه النسخة ثم فعّل `RESEND_USE_HOSTED_TEMPLATES=true` مع المعرّفات الصحيحة. الشعار داخل الرسالة هو `/brand/mark-light.png` نفسه المستخدم في الهيدر. انظر `docs/email-brand-and-bimi.md` لأيقونة المرسل؛ هذه تحتاج إعدادات وشهادة خارج الكود، وليست حقلًا داخل HTML البريد.

## 3. تيليجرام والمساعد والحركة

من إعدادات التواصل في الإدارة، أدخل رابط تيليجرام الحقيقي في `social_telegram` مثل `https://t.me/<اسم_حسابك>`. يدعم التطبيع أيضًا اسم الحساب بصيغة `@...`. تظهر القناة في الويب والجوال والبريد عند ضبط رابط صالح. لم توضع قناة تخمينية أو رابط لشخص آخر.

اضغط مطولًا على المساعد ثم اسحبه. الويب يدعم الأسهم لتحريكه وHome لإعادته، ويحفظ الإحداثيات النسبية محليًا؛ التطبيق يستخدم التخزين الآمن لموضع المساعد. في المتصفح الذي يمنع التخزين يظل السحب يعمل خلال الجلسة. لا يسمح بخروج الزر عن حدود العرض. توجد مراعاة لتقليل الحركة في النظام، وزر لتفضيل الحركة في فوتر الويب؛ لا يُخفى المحتوى عندما تتعطل الحركة.

## 4. فحص المرفقات

لا تُلغِ حماية المرفقات لتمرير مشكلة ماسح الملفات. في الإنتاج تبقى المرفقات معلقة إلى أن يصدر حكم سليم صريح. اضبط `MALWARE_SCAN_URL` و`MALWARE_SCAN_TOKEN`. أُرفق جسر اختياري إلى ClamAV في `services/file-scanner/` مع تعليماته، وحدود الحجم والوقت والاتصالات. يحتاج خادمًا ومحركًا مهيأين وتحديث تواقيع حقيقيًا؛ لم يُختبر محرك ClamAV الفعلي في بيئة التسليم.

أعِد فحص الملفات المعلقة من الإدارة بعد إعداد الماسح، وتابع `hasMore` عند التشغيل الدوري. استخدام التطوير من دون ماسح ليس فحصًا حقيقيًا للبرمجيات الضارة.

## 5. بناء التطبيق وروابط الفتح

```bash
cd mobile
npm ci
npm run lint
npm run typecheck
npm test
npx expo-doctor
npx expo export --platform all
# بعد ضبط حساب Expo والشهادات:
npm run build:android
npm run build:ios
```

نسخة `production` تحفظ وضع `reader` الموجود في المشروع؛ لم أغيّر سياسة الشراء داخل نسخ المتاجر تلقائيًا. بيانات المستخدم والاشتراكات تأتي من الخادم نفسه.

لفتح روابط الموقع في التطبيق، عرّف `ANDROID_SHA256_FINGERPRINTS` ببصمة Play App Signing الفعلية و`APPLE_APP_ID_PREFIX` أو `APPLE_TEAM_ID` الصحيح. المساران `.well-known/assetlinks.json` و`.well-known/apple-app-site-association` يعيدان 404 عند غياب هوية توقيع صالحة بدل نشر بيانات زائفة. لا يكفي وضع اسم النطاق في Expo وحده؛ أعد بناء التطبيق وتحقق من الملفين على النطاق العام.

## 6. اختبار القبول قبل استبدال النسخة الحية

جرّب التسجيل والتحقق وإعادة الإرسال وانتهاء الرمز، ونسيان/تغيير كلمة المرور وتسجيل الخروج من الجلسات، والتنقل بين كل تبويبات الطالب والعودة في المتصفح. اختبر مادة مشتركة قبل تمييزها وبعده، والحذف ثم إعادة تشغيل الخادم، وشراء Tap في بيئة الاختبار ومنح الاشتراك وتشغيل فيديو ومرفق سليم ورفض ملف غير سليم وإشعار للجهاز.

اختبر iPhone وAndroid فعليين ومتصفحات Safari/Chrome/Firefox، مع شاشة صغيرة، تكبير النص، اتصال ضعيف، الوضع الداكن وتقليل الحركة. لا يُغني نجاح فحوص الوحدات والمحاكاة عن هذه التجربة. لا تنشر مفاتيح الاختبار أو كلمات المرور في لقطات الدعم.
