# تقرير التحقق الحالي — 29 أغسطس 2026

هذا الملف يلخص ما تم التحقق منه **على نسخة العمل الحالية فقط** بعد تطوير الهوية، RTL، الإدارة، الإشعارات، التسجيل والدفع. أي نتائج Build/Lint أقدم في تقارير سابقة لا تُعد تحققًا من هذه النسخة إلا بعد إعادة تشغيلها ببيئة Dependencies كاملة.

## النتائج التي تم التحقق منها داخل بيئة العمل

| الفحص | النتيجة |
|---|---|
| اختبارات العقود Node | **41/41 ناجحة** |
| فحص تركيب TypeScript/TSX | **207 ملفًا بدون أخطاء Syntax** |
| Drizzle migration 0011 snapshot JSON | صالح |
| Drizzle migration journal JSON | صالح |
| حالة قراءة الإشعارات | لكل مستخدم عبر `notification_reads` |
| المواد المنشورة أثناء إضافة الفيديوهات | تبقى قابلة للسلة والاشتراك |
| Feature switches | التسجيل، الدفع، طلب المواد، المساعد، والتصفح كضيف مرتبطة بالـAPI والويب والتطبيق |
| روابط الخدمات الموقوفة | تُخفى أو تستبدل بمسار مناسب في أهم واجهات الطالب العامة |
| اتجاه تطبيق Expo | RTL عربي افتراضي للنصوص وحقول الإدخال |
| هوية التطبيق | M والنجمة كاملة، وأصول Splash/Adaptive/PWA موحدة |

الأمر المستخدم لاختبارات العقود:

```bash
cd backend
files=$(find tests -maxdepth 1 -name '*.test.mjs' ! -name 'smoke.test.mjs' -printf '%p ')
node --test $files
```

النتيجة الأخيرة: 41 اختبارًا، 41 ناجحًا، 0 فشل.

## ما لم يتم اعتباره متحققًا في هذه البيئة

لم يتم اعتماد `npm run build` أو `npm run lint` أو Expo typecheck كاختبار نهائي لهذه النسخة داخل بيئة العمل الحالية، لأن تثبيت `node_modules` الموجود أثناء المراجعة كان جزئيًا ولم يحتوِ `@types/node` وبقية Dependencies بصورة كاملة. تمت إزالة التثبيت الجزئي من الحزمة النهائية حتى لا ينتقل إلى بيئة النشر.

قبل نشر الإنتاج يجب تشغيل تثبيت نظيف:

```bash
cd backend
npm ci
npm run lint
npm run build

cd ../mobile
npm ci
npm run typecheck
npm run lint
npx expo-doctor
```

ويجب إجراء اختبار قبول باستخدام PostgreSQL وTap وPush ورفع الملفات بمفاتيح Sandbox/Production الخاصة بمالك المنصة. لا يمكن اعتبار الاختبارات النصية أو التركيبية بديلًا عن تشغيل الخدمات الخارجية فعليًا.

## أهم العقود التي تغطيها الاختبارات الحالية

- صلاحيات الحذف الإداري والحماية من حذف السجلات المالية والتدقيقية.
- حماية عمليات Checkout ورفع الفيديو وطلبات المواد.
- الكوبونات والسلة والمفضلة والجلسات.
- إعدادات المظهر في الويب وExpo.
- Feature switches الإدارية المشتركة.
- فصل حالة قراءة الإشعار لكل مستخدم.
- مزامنة Realtime في الويب والتطبيق.
- عقود الدعم ومرفقاته.
- سلوك مساعد مراس وسياق المستخدم الخاص.
- السماح بشراء المادة المنشورة بدون اشتراط اكتمال جميع فيديوهاتها.

## ملاحظات النشر

- migration `0011_notification_read_state.sql` يجب أن تطبق قبل الاعتماد على حالة قراءة الإشعارات الجديدة.
- خدمة الدفع تحتاج `TAP_SECRET_KEY` صحيحة، ولا تنشئ عملية جديدة إذا أوقفت الإدارة `payments_enabled`.
- التسجيل العام يرفض من الخادم عند إيقاف `student_registration_enabled`، وليس مجرد إخفاء زر الواجهة.
- طلبات المواد ترفض من الخادم عند إيقاف `course_requests_enabled`.
- مساعد مراس يرفض الطلبات عند إيقاف `assistant_enabled` ويمنع توجيه المستخدم إلى الخدمات الموقوفة.
- التطبيق يمنع التصفح كضيف عند إيقاف `guest_browsing_enabled`.

للتفاصيل الوظيفية الكاملة للتغييرات راجع `CHANGES_20260829.md`.
