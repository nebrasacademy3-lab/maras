# تقرير التحقق

تمت مراجعة الخادم والويب وتطبيق Expo بعد آخر تحديث، مع التركيز على العقود المشتركة والجلسات والكتالوج والأداء.

| المجال | نتيجة التحقق |
|---|---|
| خادم Node/Next | `npm run lint` — ناجح |
| بناء الإنتاج | `npm test` و`next build` — ناجح |
| Smoke للويب والتطبيق | الصفحات العامة ومسارات الكتالوج وواجهة التطبيق العامة — ناجحة |
| قاعدة البيانات | PostgreSQL Pool بمهلات وحدود اتصالات ومراقبة أخطاء idle clients |
| الكتالوج | كاش ذاكرة قصير مع منع تكرار الاستعلامات المتزامنة، وISR لمدة دقيقة للصفحات العامة |
| لوحة الهاتف | الردود تُجلب لتذاكر المستخدم فقط بدل تحميل 300 رد دعم عام لكل مستخدم |
| الفهارس | أضيفت فهارس الدعم والتقييمات في هجرة PostgreSQL رقم 0001 |
| اختبار الضغط المحلي | 60 طلب كتالوج متوازٍ و30 طلب صفحة مواد متوازٍ — جميعها أعادت HTTP 200 |
| تطبيق Expo | `npm run typecheck` — ناجح، و`npm run lint` — ناجح |
| Expo Web | `npx expo export --platform web` — ناجح |
| عميل الشبكة | مهلة 15 ثانية ورسائل أخطاء عربية موحّدة وإلغاء الطلبات مدعوم |

> اختبار الضغط المحلي ليس بديلًا عن اختبار حمل حقيقي على Railway بموارد الإنتاج وقاعدة بيانات فعلية. يجب ضبط `DATABASE_POOL_MAX` بما يناسب حد اتصالات PostgreSQL وعدد نسخ الخدمة.

لم يتم تنفيذ عمليات Tap أو Resend أو مزود المساعد الخارجي الفعلية دون مفاتيح الخدمة الخاصة بالمالك، ولم يتم تشغيل Docker daemon في بيئة الفحص الحالية. ملفات Docker وRailway والهجرة جاهزة للنشر.

## تدقيق الأمان الأخير

| المجال | النتيجة |
|---|---|
| حماية الإدارة | `ADMIN_API_TOKEN` منفصل عن `ADMIN_UPLOAD_TOKEN`، مقارنة ثابتة الزمن، rate limit للقراءة والكتابة والموظفين، وسجل تدقيق للعمليات الحساسة |
| CSRF وعزل التطبيق | فحص Origin/Sec-Fetch-Site، وتعريف `MOBILE_APP_URL` اختياريًا، وطلبات Expo تتطلب `x-meras-client: mobile-v1` |
| رفع الفيديو | allow-list للأنواع، magic bytes، حد 200MB، rate limit، تخزين خاص، وتنظيف الملف إذا فشل إدخال PostgreSQL |
| رفع الشعارات | PNG/JPEG/WebP فقط مع magic bytes، رفض SVG، حد 4MB، rate limit، وتنظيف عند الفشل |
| طلبات المواد | حتى 5 ملفات/15MB، فحص PDF/Office/images بالمحتوى، rate limit، تنظيف الملفات والصف عند الفشل، والمشرف يرى الطلبات المسندة إليه فقط |
| الفيديو | Range والتحقق من صلاحية الحساب والوصول ورابط HMAC، و`private, no-store, nosniff` |
| Tap Webhook | جسم 256KB، توقيع HMAC إلزامي، مقارنة ثابتة الزمن، إعادة تحقق عبر Tap API، وidempotency PostgreSQL |
| رؤوس HTTP | nosniff وSAMEORIGIN وHSTS وReferrer-Policy وPermissions-Policy وCOOP وCORP، وno-store للاستجابات الحساسة |
| اعتماديات الخادم | `npm audit --omit=dev --audit-level=high` — لا ثغرات معروفة |
| اعتماديات التطبيق | `npm audit --omit=dev --audit-level=high` — لا ثغرات بعد override uuid المتوافق |
| فحوص الخادم بعد التعديل | lint ناجح بلا تحذيرات، `next build` ناجح، وsmoke test ناجح (1/1) |
| فحوص التطبيق بعد التعديل | typecheck ناجح، lint ناجح، Expo Doctor: 21/21، وتصدير Expo Web سابق ناجح |

> لا تزال اختبارات PostgreSQL/Tap/رفع الملفات الفعلية واختبار الحمل الإنتاجي بحاجة إلى تشغيل ببيئة اختبار حقيقية وأسرار sandbox. لا تعتبر فحوص المختبر المحلي أو `npm audit` ضمانًا لانعدام الثغرات.

## تطوير لوحة الإدارة

أضيف قسم إدارة الموظفين مع إنشاء الحسابات عبر `/api/admin/staff`، وقائمة حالة الموظفين، وربط نطاقات المشرفين بالجامعة والتخصص عبر `saveSupervisorAssignment`. أصبحت العمليات الإدارية تعيد نتيجة نجاح/فشل موحدة، ولا تُفرغ النماذج عند فشل الشبكة أو الخادم، كما تعيد اللوحة مزامنة بياناتها بعد أي عملية.

تمت إضافة تجاوب خاص للوحة على الهاتف: قائمة جانبية قابلة للفتح والإغلاق، جداول أفقية قابلة للتمرير، نماذج متجاوبة، ونوافذ حفظ قابلة للتمرير. جميع أقسام الإدارة الحالية موصولة بعقود الخادم: الكتالوج، المحتوى، الطلاب، الوصول، الدفع، الطلبات، الدعم، التقييمات، الإشعارات، الكوبونات، الإعدادات، سجل النشاط، الموظفون ونطاقات الإشراف.

## تدقيق تكامل الويب وتطبيق Expo

تمت مطابقة مسارات الخادم مع الاستدعاءات الفعلية في `mobile/app` و`mobile/src`، وتشمل المصادقة والتسجيل والخروج والملف الشخصي والإرشادات والكتالوج والجامعات والمواد والمفضلة والملاحظات والتقدم والفيديو والدعم وطلبات المواد والإشعارات والدفع والمساعد ومساحة المشرف ولوحة الإدارة.

أُصلحت فجوتان وظيفيتان مؤكدتان. أصبحت روابط الإشعارات الداخلية تفتح شاشة التطبيق المناسبة للمواد والطلبات والدعم والحساب والإدارة والمشرف بدل الاكتفاء بتحديد الإشعار كمقروء. وأصبح المشرف يستطيع فتح مرفقات طلبات الطلاب من الجوال عبر تنزيل موثق بجلسة Bearer إلى التخزين المؤقت ثم فتح الملف، مع حالة تحميل ورسالة فشل واضحة. كما أضيفت معالجة أخطاء تحميل مساحة المشرف واختيار الوحدة الأولى تلقائيًا بعد تحديث البيانات، وأضيف قسم إنشاء الموظفين إلى لوحة الإدارة داخل تطبيق Expo.

نتيجة الاختبار بعد التعديلات: بناء Next.js وlint واختبار الخادم ناجحة، وExpo typecheck وlint ناجحان، وExpo Doctor يمر عبر 21/21، وتصدير Expo Web ناجح باستخدام عنوان HTTPS تجريبي للفحص فقط. يجب استخدام عنوان API الحقيقي عند بناء نسخة الإنتاج.

## تحديث المساعد المتجدد

أصبح مسار `/api/assistant` مستقلًا عن مزود واحد بعينه. يقرأ الكتالوج الحي من `getInstitutionsCatalog()` و`getCoursesCatalog()`، ويقرأ إعدادات التواصل العامة من PostgreSQL، مع إبطال فوري للكاش بعد حفظ الجامعة أو التخصص أو المادة أو الإعدادات من لوحة الإدارة. عند ضبط `OPENAI_API_KEY` اختياريًا، يستخدم المساعد واجهة OpenAI-compatible للإجابة عن الأسئلة العامة مع سياق الحساب؛ وعند غياب المفتاح يعود تلقائيًا إلى محرك المعرفة العربي المحلي دون توقف الخدمة.

تم توسيع تطبيع العربية ليشمل الهمزات والحروف البديلة والتشكيل، وإضافة مرادفات للهجات وصيغ مثل «ما لقيت مادتي»، «وين مادتي»، «بكم»، «كيف أشتري»، و«ما يفتح». الإجابة الموحدة تعيد `answer` و`actions` و`suggestions`، وتُرشّح الروابط داخليًا حسب صلاحية المستخدم قبل عرضها في الويب أو تطبيق Expo.

## مراجعة Lighthouse وتحسينات المستوى العالمي

تمت مراجعة تقرير Lighthouse المرفق للنسخة المنشورة: Performance 36، Accessibility 91، Best Practices 100، SEO 100، مع FCP 4.7s وLCP 7.3s وTBT 2050ms وCLS 0. كما سجل التقرير طلبات render-blocking، JavaScript غير مستخدم، عمل main thread مرتفعًا، وتحذير أبعاد الصور.

في نسخة العمل تم تأجيل تحميل المساعد الذكي ومنسق الأنيميشن إلى وقت خمول المتصفح بعد رسم المحتوى الأساسي، وإضافة أبعاد صريحة وصور async للشعارات والصورة التجريبية وشعار مشغل الفيديو، وإضافة `content-visibility` لأقسام ما بعد الجزء المرئي، وإضافة Schema.org لـOrganization وWebSite مع SearchAction. هذه التحسينات تستهدف LCP وTBT وCLS وظهور الدومين، ولا تُعد رقمًا جديدًا لـLighthouse حتى يُعاد القياس على الدومين النهائي بعد النشر.

تظل نتيجة Google تعتمد أيضًا على جودة المحتوى الفريد، سرعة Railway وقاعدة البيانات، الدومين والعمر والروابط الخارجية، Search Console، وبيانات المستخدم الحقيقية. لا يمكن ضمان ظهور الموقع أول نتيجة فور شراء الدومين، لكن الحزمة تحتوي على أساس SEO تقني صحيح وقابل للتحسين المستمر.
