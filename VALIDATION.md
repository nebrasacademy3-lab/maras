# تقرير التحقق

تمت مراجعة الخادم والويب وتطبيق Expo بعد آخر تحديث، مع التركيز على العقود المشتركة والجلسات والكتالوج والأداء.

| المجال | نتيجة التحقق |
|---|---|
| خادم Node/Next | `npm run lint` — ناجح |
| بناء الإنتاج | `npm test` و`next build` — ناجح |
| Smoke للويب والتطبيق | الصفحات العامة ومسارات الكتالوج وواجهة التطبيق العامة — ناجحة |
| قاعدة البيانات | PostgreSQL Pool بمهلات وحدود اتصالات ومراقبة أخطاء idle clients |
| الكتالوج | كاش ذاكرة قصير مع منع تكرار الاستعلامات المتزامنة، وISR لمدة دقيقة للصفحات العامة |
| لوحة الهاتف | الردود تُجلب لتذاكر المستخدم فقط بدل تحميل 300 رد دعم عام لكل مستخدم |
| الفهارس | أضيفت فهارس الدعم والتقييمات في هجرة PostgreSQL رقم 0001 |
| اختبار الضغط المحلي | 60 طلب كتالوج متوازٍ و30 طلب صفحة مواد متوازٍ — جميعها أعادت HTTP 200 |
| تطبيق Expo | `npm run typecheck` — ناجح، و`npm run lint` — ناجح |
| Expo Web | `npx expo export --platform web` — ناجح |
| عميل الشبكة | مهلة 15 ثانية ورسائل أخطاء عربية موحّدة وإلغاء الطلبات مدعوم |

> اختبار الضغط المحلي ليس بديلًا عن اختبار حمل حقيقي على Railway بموارد الإنتاج وقاعدة بيانات فعلية. يجب ضبط `DATABASE_POOL_MAX` بما يناسب حد اتصالات PostgreSQL وعدد نسخ الخدمة.

لم يتم تنفيذ عمليات Tap أو Resend أو مزود المساعد الخارجي الفعلية دون مفاتيح الخدمة الخاصة بالمالك، ولم يتم تشغيل Docker daemon في بيئة الفحص الحالية. ملفات Docker وRailway والهجرة جاهزة للنشر.

## تدقيق الأمان الأخير

| المجال | النتيجة |
|---|---|
| حماية الإدارة | `ADMIN_API_TOKEN` منفصل عن `ADMIN_UPLOAD_TOKEN`، مقارنة ثابتة الزمن، rate limit للقراءة والكتابة والموظفين، وسجل تدقيق للعمليات الحساسة |
| CSRF وعزل التطبيق | فحص Origin/Sec-Fetch-Site، وتعريف `MOBILE_APP_URL` اختياريًا، وطلبات Expo تتطلب `x-meras-client: mobile-v1` |
| رفع الفيديو | allow-list للأنواع، magic bytes، حد 200MB، rate limit، تخزين خاص، وتنظيف الملف إذا فشل إدخال PostgreSQL |
| رفع الشعارات | PNG/JPEG/WebP فقط مع magic bytes، رفض SVG، حد 4MB، rate limit، وتنظيف عند الفشل |
| طلبات المواد | حتى 5 ملفات/15MB، فحص PDF/Office/images بالمحتوى، rate limit، تنظيف الملفات والصف عند الفشل، والمشرف يرى الطلبات المسندة إليه فقط |
| الفيديو | Range والتحقق من صلاحية الحساب والوصول ورابط HMAC، و`private, no-store, nosniff` |
| Tap Webhook | جسم 256KB، توقيع HMAC إلزامي، مقارنة ثابتة الزمن، إعادة تحقق عبر Tap API، وidempotency PostgreSQL |
| رؤوس HTTP | nosniff وSAMEORIGIN وHSTS وReferrer-Policy وPermissions-Policy وCOOP وCORP، وno-store للاستجابات الحساسة |
| اعتماديات الخادم | `npm audit --omit=dev --audit-level=high` — لا ثغرات معروفة |
| اعتماديات التطبيق | `npm audit --omit=dev --audit-level=high` — لا ثغرات بعد override uuid المتوافق |
| فحوص الخادم بعد التعديل | lint ناجح بلا تحذيرات، `next build` ناجح، وsmoke test ناجح (1/1) |
| فحوص التطبيق بعد التعديل | typecheck ناجح، lint ناجح، Expo Doctor: 21/21، وتصدير Expo Web سابق ناجح |

> لا تزال اختبارات PostgreSQL/Tap/رفع الملفات الفعلية واختبار الحمل الإنتاجي بحاجة إلى تشغيل ببيئة اختبار حقيقية وأسرار sandbox. لا تعتبر فحوص المختبر المحلي أو `npm audit` ضمانًا لانعدام الثغرات.

## تطوير لوحة الإدارة

أضيف قسم إدارة الموظفين مع إنشاء الحسابات عبر `/api/admin/staff`، وقائمة حالة الموظفين، وربط نطاقات المشرفين بالجامعة والتخصص عبر `saveSupervisorAssignment`. أصبحت العمليات الإدارية تعيد نتيجة نجاح/فشل موحدة، ولا تُفرغ النماذج عند فشل الشبكة أو الخادم، كما تعيد اللوحة مزامنة بياناتها بعد أي عملية.

تمت إضافة تجاوب خاص للوحة على الهاتف: قائمة جانبية قابلة للفتح والإغلاق، جداول أفقية قابلة للتمرير، نماذج متجاوبة، ونوافذ حفظ قابلة للتمرير. جميع أقسام الإدارة الحالية موصولة بعقود الخادم: الكتالوج، المحتوى، الطلاب، الوصول، الدفع، الطلبات، الدعم، التقييمات، الإشعارات، الكوبونات، الإعدادات، سجل النشاط، الموظفون ونطاقات الإشراف.

## تدقيق تكامل الويب وتطبيق Expo

تمت مطابقة مسارات الخادم مع الاستدعاءات الفعلية في `mobile/app` و`mobile/src`، وتشمل المصادقة والتسجيل والخروج والملف الشخصي والإرشادات والكتالوج والجامعات والمواد والمفضلة والملاحظات والتقدم والفيديو والدعم وطلبات المواد والإشعارات والدفع والمساعد ومساحة المشرف ولوحة الإدارة.

أُصلحت فجوتان وظيفيتان مؤكدتان. أصبحت روابط الإشعارات الداخلية تفتح شاشة التطبيق المناسبة للمواد والطلبات والدعم والحساب والإدارة والمشرف بدل الاكتفاء بتحديد الإشعار كمقروء. وأصبح المشرف يستطيع فتح مرفقات طلبات الطلاب من الجوال عبر تنزيل موثق بجلسة Bearer إلى التخزين المؤقت ثم فتح الملف، مع حالة تحميل ورسالة فشل واضحة. كما أضيفت معالجة أخطاء تحميل مساحة المشرف واختيار الوحدة الأولى تلقائيًا بعد تحديث البيانات، وأضيف قسم إنشاء الموظفين إلى لوحة الإدارة داخل تطبيق Expo.

نتيجة الاختبار بعد التعديلات: بناء Next.js وlint واختبار الخادم ناجحة، وExpo typecheck وlint ناجحان، وExpo Doctor يمر عبر 21/21، وتصدير Expo Web ناجح باستخدام عنوان HTTPS تجريبي للفحص فقط. يجب استخدام عنوان API الحقيقي عند بناء نسخة الإنتاج.

## تحديث المساعد المتجدد

أصبح مسار `/api/assistant` مستقلًا عن مزود واحد بعينه. يقرأ الكتالوج الحي من `getInstitutionsCatalog()` و`getCoursesCatalog()`، ويقرأ إعدادات التواصل العامة من PostgreSQL، مع إبطال فوري للكاش بعد حفظ الجامعة أو التخصص أو المادة أو الإعدادات من لوحة الإدارة. عند ضبط `OPENAI_API_KEY` اختياريًا، يستخدم المساعد واجهة OpenAI-compatible للإجابة عن الأسئلة العامة مع سياق الحساب؛ وعند غياب المفتاح يعود تلقائيًا إلى محرك المعرفة العربي المحلي دون توقف الخدمة.

تم توسيع تطبيع العربية ليشمل الهمزات والحروف البديلة والتشكيل، وإضافة مرادفات للهجات وصيغ مثل «ما لقيت مادتي»، «وين مادتي»، «بكم»، «كيف أشتري»، و«ما يفتح». الإجابة الموحدة تعيد `answer` و`actions` و`suggestions`، وتُرشّح الروابط داخليًا حسب صلاحية المستخدم قبل عرضها في الويب أو تطبيق Expo.

## مراجعة Lighthouse وتحسينات المستوى العالمي

تمت مراجعة تقرير Lighthouse المرفق للنسخة المنشورة: Performance 36، Accessibility 91، Best Practices 100، SEO 100، مع FCP 4.7s وLCP 7.3s وTBT 2050ms وCLS 0. كما سجل التقرير طلبات render-blocking، JavaScript غير مستخدم، عمل main thread مرتفعًا، وتحذير أبعاد الصور.

في نسخة العمل تم تأجيل تحميل المساعد الذكي ومنسق الأنيميشن إلى وقت خمول المتصفح بعد رسم المحتوى الأساسي، وإضافة أبعاد صريحة وصور async للشعارات والصورة التجريبية وشعار مشغل الفيديو، وإضافة `content-visibility` لأقسام ما بعد الجزء المرئي، وإضافة Schema.org لـOrganization وWebSite مع SearchAction. هذه التحسينات تستهدف LCP وTBT وCLS وظهور الدومين، ولا تُعد رقمًا جديدًا لـLighthouse حتى يُعاد القياس على الدومين النهائي بعد النشر.

تظل نتيجة Google تعتمد أيضًا على جودة المحتوى الفريد، سرعة Railway وقاعدة البيانات، الدومين والعمر والروابط الخارجية، Search Console، وبيانات المستخدم الحقيقية. لا يمكن ضمان ظهور الموقع أول نتيجة فور شراء الدومين، لكن الحزمة تحتوي على أساس SEO تقني صحيح وقابل للتحسين المستمر.

## إصلاح تعذر التحقق من طلب المساعد خلف Railway

أظهر فحص النسخة المنشورة أن `POST /api/assistant` كان يعيد HTTP 403 حتى مع `Origin: https://marase.up.railway.app` بسبب أن Railway قد يمرر عنوان الطلب الداخلي عبر proxy بصيغة مختلفة عن Origin المتصفح. تم تعديل `sameOriginRequest` ليقبل عنوان HTTPS للدومين الموجود في Host عندما يكون صالحًا، إضافة إلى العناوين المعرفة صراحة في `APP_URL` و`NEXT_PUBLIC_SITE_URL` و`MOBILE_APP_URL`، مع رفض HTTP في الإنتاج ورفض المصادر الخارجية.

تم اختبار الحالة بوحدة مباشرة: قبول HTTPS للدومين العام خلف proxy، رفض `https://evil.example`، رفض HTTP في الإنتاج، وقبول طلبات التطبيق التي لا ترسل Origin. يجب رفع الإصدار الجديد إلى Railway حتى يظهر الإصلاح في النطاق المنشور؛ فحص النطاق الحالي قبل إعادة النشر قد يستمر في إرجاع 403 لأنه يعمل بالإصدار السابق.

## إصلاح تسجيل الخروج والتدقيق الأمني

تم إصلاح تسجيل الخروج في الويب ليبطل جلسة PostgreSQL ويمسح `meras_session`، مع ضمان مسح الكوكي محليًا حتى عند تعطل قاعدة البيانات مؤقتًا، وإضافة `no-store` و`Clear-Site-Data` للكاش. في تطبيق Expo أصبح إبطال الجلسة على الخادم يتبعه دائمًا مسح الرمز من الذاكرة وSecureStore وحالة المستخدم، حتى إذا انتهت الجلسة أو فشل الاتصال أو تعذر SecureStore.

نتائج الفحص: `npm audit --omit=dev --audit-level=high` أعاد صفر ثغرات معروفة للخادم والتطبيق، وفحص الكود لم يجد إعدادات TLS غير آمنة أو مراجع قديمة أو أسرارًا فعلية. نجحت اختبارات Next lint وbuild وsmoke، وExpo typecheck وlint وDoctor 21/21. يبقى اختبار الإبطال مع PostgreSQL إنتاجية فعلية واختبار اختراق مستقل مطلوبين قبل الإطلاق، ولا يمكن لأي فحص ضمان انعدام الثغرات مطلقًا.

## متابعة Lighthouse — 27 أغسطس 2026

التشغيل الأحدث على `https://marase.up.railway.app/` أعطى Performance 49 وAccessibility 95 وBest Practices 96 وSEO 100، مع FCP ‏4.6s وLCP ‏7.0s وTBT ‏620ms وCLS ‏0. تم تسجيل ملاحظات render-blocking، cache lifetimes، image delivery، DOM size، JavaScript/CSS غير المستخدم، ومهمات animation غير composited.

تم بعد ذلك إصلاح ARIA للحاويات التي تحمل `aria-label` بإضافة الأدوار المناسبة، وتأجيل المكونات العالمية الثقيلة، وتثبيت أبعاد الصور، وتوفير 85 شعار مؤسسة بصيغة WebP مع PNG كاحتياط، وإضافة Schema.org. لم يتم ادعاء نتيجة Lighthouse جديدة قبل نشر هذه النسخة؛ يجب إعادة القياس على الدومين النهائي بعد الـRedeploy.

## تدقيق الكتالوج — 27 أغسطس 2026

| المؤشر | النتيجة |
|---|---:|
| مؤسسات مصدر البيانات | 85 |
| قائمة CUA الحكومية المطابقة | 29/29 |
| سجلات CUA الأهلية والكليات التي لها مقابل | 46/46 عبر أسماء أو aliases أو علاقة تبعية |
| البرامج العامة في خريطة الاكتشاف | 3008 علاقة برنامج/جهة تقريبًا |
| البرامج الرسمية المضافة صراحة | 25 برنامجًا للجامعة السعودية الإلكترونية، من صفحة البرامج الرسمية |
| المواد/الدورات المنشورة ذات الدروس الفعلية | 8 |

تمت إضافة `CATALOG_SOURCES.md`، وسجل source metadata في المؤسسة والتخصص والمادة، وهجرة `backend/drizzle/0002_catalog_provenance.sql`. تعرض الواجهة حالة `official-program` أو `discovery` أو `pending-review`، وتقدم طلب توفير مادة عندما لا يوجد محتوى دراسي فعلي. لا تُحسب أسماء المواد المقترحة كدورات مدفوعة.

نجحت اختبارات runtime المحلية: `/api/mobile/catalog` أعاد 85 مؤسسة و8 مواد منشورة، و`/api/catalog/programs?institution=seu` أعاد 25 برنامجًا بحالة `official-program`. كما ظهرت تسمية «جامعة الأمير فهد بن سلطان» والعدادات الفعلية للمواد في العقد المحمول.

آخر نتائج الفحص بعد تغييرات الكتالوج: `npm run lint` و`npm run build` و`node --test tests/*.test.mjs` في backend ناجحة، و`npm run typecheck` و`npm run lint` و`npx expo-doctor` في mobile ناجحة (21/21). لم تُطبق الهجرة على قاعدة Railway لعدم توفر بيانات الاتصال الإنتاجية في بيئة العمل؛ سيطبقها `start-railway.sh` عند Redeploy، ويجب مراقبة سجل الهجرة وفحص endpoint بعد النشر.

## تدقيق الربط والصلاحيات — 27 أغسطس 2026

أعاد تدقيق البيانات الثابتة: 85 مؤسسة بلا slugs مكررة، و8 مواد بلا أي جامعة مفقودة، و8 مواد بلا أي تخصص غير معروف بعد احتساب aliases الصحيحة مثل «الطب» ← «الطب والجراحة» و«إدارة الأعمال» ← برنامج إدارة الأعمال في الجامعة السعودية الإلكترونية و«الصيدلة» ← دكتور صيدلة في جامعة الملك خالد. لا توجد مؤسسة بلا خريطة برامج في طبقة العرض الحالية، بإجمالي 3009 علاقة برنامج/جهة اكتشافية أو رسمية.

أضيفت هجرة `0003_catalog_relationship_integrity.sql` التي تفرض FK بين `institution_specialties` والجهات والتخصصات، وبين `catalog_courses` والجهة والتخصص ورابط الجهة/التخصص المركب. استُخدمت `NOT VALID` لتفادي فشل الإقلاع إذا احتوت قاعدة قديمة على صفوف يتيمة؛ القيود تتحقق من كل إدخال جديد، بينما تُخفي طبقة `catalog-store` السجلات المدارة التي لا تملك سلسلة ربط صحيحة حتى تُراجعها الإدارة.

مسار `saveCourse` لا يحفظ مادة إلا إذا كانت الجهة موجودة والتخصص موجودًا والرابط المنشور بينهما موجودًا. ومسار `saveSupervisorAssignment` لا ينشئ نطاقًا إلا لمشرف فعلي وتخصص إداري مربوط بالجامعة نفسها. وتستمر مساحة المشرف في إعادة التحقق من نطاق المادة والوحدة والدرس على الخادم، لا في الواجهة فقط.

## الدعم والتواصل والهوية البصرية — 27 أغسطس 2026

أصبح `POST /api/support` محميًا بجلسة مستخدم فعالة؛ أي طلب بلا جلسة يعاد له `401` ولا تُنشأ تذكرة ولا يمكن تزوير بريد صاحبها. صفحة `/support` نفسها تتحقق من كوكي الجلسة على الخادم وتحوّل الزائر إلى `/login?return_to=%2Fsupport`، ثم يعاد إلى الدعم بعد الدخول. شاشة `mobile/app/support.tsx` تستخدم تحويلًا مبكرًا مماثلًا إلى مجموعة المصادقة، ولا تعرض نموذج التذكرة للزائر.

فُصلت صفحة `/contact` العامة عن الدعم، وأصبحت تعرض واتساب والبريد وروابط الشبكات الاجتماعية المتاحة من الإعدادات العامة، مع بطاقة واضحة للدعم الفني ومتطلب تسجيل الدخول. أضيفت شاشة `mobile/app/contact.tsx`، وصُحح توجيه المساعد في Expo حتى يفتح `/contact` بدل تحويله خطأً إلى الدعم.

تم رفع أصول `logo-light.png` و`logo-dark.png` في الويب وExpo إلى 1984×1156 مع الحفاظ على الشفافية والألوان والكلمة العربية، وإزالة الشريط الأبيض غير المرغوب من النسخة الليلية. تم فحص الأصول بصريًا وفنيًا، وتبقى صيغة الوضع الليلي بيضاء للكتابة فوق الخلفيات الداكنة.

نتائج الفحص بعد هذه التعديلات: `npm run lint` و`npm run build` و`node --test tests/*.test.mjs` في backend ناجحة، و`npm run typecheck` و`npm run lint` و`npx expo-doctor` في mobile ناجحة بنتيجة 21/21. يلزم Redeploy للحزمة الجديدة حتى يظهر حارس الدعم وتحديثات التواصل والشعارات على النطاق العام.

## الكتالوج والسلة والمفضلة — 27 أغسطس 2026

أصبح فلتر المواد على الويب يعرض المؤسسات الـ85 من قائمة المؤسسات الكاملة، لا المؤسسات التي لديها مادة منشورة فقط. عند اختيار مؤسسة، يُجلب دليل تخصصاتها من `/api/catalog/programs?institution=...`، وتُصفّى المواد المتاحة على الجامعة والتخصص المختارين. يمكن ترك الفلاتر كلها واستخدام البحث الحر باسم المادة أو الرمز أو الجامعة أو التخصص. شاشة Expo تستخدم `/api/mobile/catalog` للمؤسسات وتستدعي دليل البرامج عند اختيار الجامعة نفسها.

أضيفت هجرة `0004_cart_order_items.sql` وجدولا `cart_items` و`order_items`. السلة مرتبطة ببريد الحساب، تمنع التكرار، وتستبعد المادة التي لديها وصول فعّال. `/api/cart` موحد للويب وExpo، وصفحتا `/cart` في الويب و`mobile/app/cart.tsx` تعرضان العناصر والإجمالي والحذف والخصم. يدعم `/api/checkout` الآن `courseSlugs` متعددة مع بقاء `courseSlug` الأول للتوافق مع الطلبات القديمة.

يُحتسب كود الخصم على السلة في الخادم عبر `quoteCouponForCart`، ويُرسل Tap مبلغًا واحدًا ووصفًا متعدد المواد. يتحقق Webhook من مبلغ الطلب وعملته وكل slugs الموجودة في `order_items`، ثم يفعّل `courseAccess` لكل المواد عند `CAPTURED` مع بقاء idempotency والفاتورة واحتساب استخدام الكود مرة واحدة لكل طلب.

أضيفت `/api/favorites` الموحدة للويب وExpo مع بقاء `/api/mobile/favorites` متوافقًا، وصفحة `/favorites` الويب وشاشة Expo تعرضان المحفوظات والإزالة. أضيفت مداخل السلة والمفضلة إلى هيدر الويب وحساب Expo، وأصبحت أزرار الإضافة ظاهرة في بطاقات المواد وتفاصيل المادة.

نتيجة الفحوص بعد هذه الإضافات: backend lint/build/tests وDrizzle check ناجحة، وExpo typecheck/lint و`expo-doctor` ناجحة بنتيجة 21/21. لم تُختبر عملية CAPTURED حقيقية لعدم توفر مفاتيح Tap الإنتاجية في بيئة التطوير؛ يجب تنفيذ اختبار دفع محدود على Railway بعد ضبط المفاتيح وWebhook.

## الإشعارات التفاعلية والإعلانات — 27 أغسطس 2026

أصبح `/api/mobile/notifications` يعيد قائمة الإشعارات مع `unreadCount`، ويقبل تعليم إشعار واحد أو جميع الإشعارات كمقروءة مع فحص جلسة المستخدم وsame-origin. لوحة الطالب الويب تعرض العداد الحقيقي في الشريط الجانبي وهيدر الموقع، وتحدّث الإشعارات عند فتح القسم. شاشة Expo تعرض العداد وتعلّمها كمقروءة عند فتح الشاشة، مع زر تحديد الكل وروابط عميقة للمادة والطلبات والدعم والتواصل والسلة والمفضلة.

تمت إضافة `actionLabel` و`presentation` و`pushEnabled` و`startsAt` و`expiresAt` و`dismissible` إلى جدول الإشعارات في `0005_notification_campaign_fields.sql`. يمكن للإدارة إنشاء إعلان لجمهور الطلاب أو الجمهور العام أو المشرفين أو الإدارة، واختيار مركز الإشعارات أو البانر أو النافذة أو كل القنوات، وإضافة زر برابط داخلي فقط، وجدولة فترة ظهوره، وتفعيل Push أو إيقافه. يتحقق الخادم من نوع العرض وصحة المدة ويمنع الروابط الخارجية أو بروتوكولات javascript.

أضيف `/api/public/announcements` لعرض الإعلانات العامة النشطة فقط. مكوّن الويب `AnnouncementCampaign` يظهر البانر أو النافذة مرة لكل جلسة متصفح مع الإخفاء الاختياري، ومكوّن Expo المقابل يظهرها داخل التطبيق مع زر يفتح المسار الداخلي الصحيح. الجمهور العام يمكنه أيضًا الوصول إلى الإعلان من مركز الإشعارات بعد تسجيل الدخول.

تم ربط تحديث حالة طلب المادة بإشعار فوري وPush، وإذا أصبحت المادة متاحة وكان هناك مقرر منشور مطابق للجامعة والتخصص يفتح الإشعار المادة مباشرة؛ وإلا يفتح صفحة متابعة الطلب. كما يستمر إشعار تفعيل المادة بعد منح الوصول وإشعار رد الدعم، مع روابط وأزرار واضحة.

تسجيل الأجهزة قائم عبر Expo Push Token مع قناة Android عالية الأهمية، وبيانات Push لا تحتوي أسرارًا. الإرسال الخارجي اختياري وفشل مزود Push لا يمنع حفظ إشعار قاعدة البيانات. نتائج الفحص: backend lint/build/tests وDrizzle check ناجحة، وExpo typecheck/lint و`expo-doctor` ناجحة بنتيجة 21/21. لم يُختبر تسليم Push على جهاز فعلي أو خدمة Expo الإنتاجية داخل sandbox؛ يلزم اختبار جهاز Android وiOS بعد ضبط EAS project ID وPush credentials.

## التواصل وonboarding والدعم المحادثي — 27 أغسطس 2026

أصبحت صفحة التواصل على الويب وExpo تعرض البريد وواتساب وجميع القنوات الاجتماعية المدارة من لوحة الإدارة: X وInstagram وTikTok وYouTube وTelegram وLinkedIn وFacebook وSnapchat وThreads. تحفظ الإدارة الروابط عبر `platform_settings` وتظهر التغييرات تلقائيًا في صفحة التواصل والتذييل والتطبيق، ولا تُقبل الروابط الخارجية إلا عبر HTTPS.

تمت إعادة تصميم onboarding للويب وExpo إلى رحلة متعددة المراحل تشرح تخصيص الكتالوج، الفلترة بحسب المسار، المشغل وحفظ التقدم، طلب المواد، الدعم والمساعد. أضيفت مؤشرات تقدم وبطاقات فوائد وحركة دخول هادئة ومتجاوبة مع `prefers-reduced-motion` على الويب، وحركة انتقال قصيرة على Expo مع خيار التخطي، مع استمرار حفظ إكمال الرحلة في حساب المستخدم.

أصبح الدعم مركز محادثة مرتبطًا بحساب الطالب. عند فتح التذكرة يختار الطالب قناة المتابعة المفضلة: داخل مراس أو البريد أو واتساب، وتبقى المحادثة داخل الحساب مصدر الحقيقة الآمن. يمكن للطالب إرسال رسائل ورفع حتى 5 ملفات بحد 15MB للملف، ويستطيع المشرف أو الإدارة الرد والاطلاع على thread والمرفقات وتغيير الحالة. لا يستطيع الطالب إغلاق التذكرة؛ الإغلاق من المشرف أو الإدارة فقط، ويمكنه إعادة فتح التذكرة أو إرسال رسالة جديدة بعد الإغلاق. الردود الداخلية لا تُعرض للطالب.

تُحفظ بيانات المرفقات في `support_reply_files` بينما تبقى الملفات في Railway Volume، وتُخدم عبر `/api/support/files/[id]` بعد تحقق الجلسة وملكية التذكرة أو الدور الإداري. يتحقق الخادم من الحجم والنوع وبصمة PDF والصور وOffice، وينظف الملفات عند فشل الرفع. أضيفت الهجرة `0006_square_living_mummy.sql` التي تضيف `support_tickets.contact_channel` و`support_replies.author_role` و`support_reply_files`.

الفحوص الناجحة: backend lint وbuild واختبارات Node وDrizzle check، وmobile typecheck وlint و`expo-doctor` بنتيجة 21/21، إضافة إلى `git diff --check`. لم تُستخدم مفاتيح أو أسرار في العميل. البريد وواتساب محفوظان كقناة مفضلة وروابط تواصل مباشرة؛ الإرسال الخارجي الآلي يتطلب مزود بريد أو WhatsApp Business API مستقلًا، ولا توجد مفاتيح لهذه الخدمات في بيئة الاختبار.

## تحديث مركز محادثات الدعم — 27 أغسطس 2026

تم تطوير الدعم إلى بطاقات مستقلة لكل تذكرة تحمل عنوان الطالب ورقمها وقناة المتابعة، وعند اختيار البطاقة يظهر thread كامل بدل نموذج منفصل. تعرض لوحة الإدارة اسم الطالب وبريده وهاتفه والجامعة والتخصص وحالة الحساب دون إعادة كلمات المرور أو الرموز السرية.

أصبح الطالب والمشرف قادرين على إرسال النص والصور والمستندات داخل نفس المحادثة. في Expo لا تُفتح روابط الملفات المحمية مباشرة؛ يُنزّل الملف مع Bearer إلى cache الجهاز ثم يُفتح من هناك، وهو ما يعالج ظهور `Access Denied` الناتج عن فتح endpoint محمي بلا ترويسة مصادقة. أضيف السلوك نفسه إلى إدارة Expo.

أضيف `DELETE /api/support` بصلاحية المشرف أو الإدارة فقط. الحذف يتم داخل معاملة PostgreSQL للتذكرة ورسائلها وسجلات المرفقات، ثم تُحذف كائنات الملفات من Railway Volume، وتُسجل العملية في audit log. لا يستطيع الطالب تنفيذ الحذف، ولا يُسمح بالحذف دون جلسة أو طلب إداري صالح.

نتائج التحقق بعد التحديث: backend lint وbuild وsmoke بنتيجة 1/1 وDrizzle check، وmobile typecheck وlint وExpo Doctor بنتيجة 21/21، إضافة إلى `git diff --check`. لم يُنفذ حذف أو رفع على قاعدة Railway الإنتاجية داخل بيئة الفحص؛ يجب اختبار ذلك بعد Redeploy بحساب مدير فعلي وتذكرة اختبار غير مهمة.

## تحديث الكتالوج الإداري — 27 أغسطس 2026

أضيفت خدمة `syncCatalogTemplates` إلى API الإدارة وزر تشغيلها في لوحة الويب وتطبيق Expo. المزامنة تقرأ قائمة الجهات الحالية وملفات البرامج الأكاديمية الموجودة في `academic-data.ts`، ثم تنشئ أو تربط التخصصات وتنشئ لكل تخصص مواده بقوالب وصف وسعر ابتدائي 49 ر.س وحالة `published`، ما يجعلها قابلة للعرض والاشتراك بعد اكتمال الربط.

لكل مادة جديدة تُنشأ أربع وحدات: «الأساسيات والمفاهيم»، «التطبيق والتحليل»، «التمارين والمراجعة»، و«المشروع والاستعداد»، وخمسة دروس منظمة. الدرس الأول مفعّل كمعاينة مجانية، وجميع الدروس تحمل حالة «بانتظار الفيديو» حتى يرفع المسؤول فيديوهات الشرح من قسم «الوحدات والدروس والفيديو». المزامنة idempotent؛ إعادة تشغيلها لا تكرر التخصصات أو المواد أو الوحدات الموجودة ولا تستبدل تعديلات الإدارة اليدوية.

هذه القوالب هي هيكل إداري جاهز وليست نصوصًا أو فيديوهات أكاديمية رسمية. يجب على صاحب المنصة مراجعة أسماء البرامج والأسعار وإضافة الفيديوهات أو المصادر المعتمدة قبل التسويق لها كمحتوى نهائي. بعد Redeploy افتح لوحة الإدارة، انتقل إلى «الوحدات والدروس والفيديو»، واضغط «تجهيز الكتالوج الآن» مرة واحدة، ثم راجع العدادات وابدأ رفع الفيديوهات.

## تحديث المستوى الأكاديمي — 27 أغسطس 2026

أضيف الحقل الاختياري `users.academic_level` عبر الهجرة `backend/drizzle/0007_organic_punisher.sql`. بقي الحقل nullable حتى لا تتعطل حسابات الطلاب القديمة، بينما يرفض مسارا التسجيل الويب وExpo إنشاء طالب جديد من دون اختيار قيمة معتمدة من قائمة المستويات أو «خريج».

يعتبر حساب الطالب القديم ناقصًا إذا لم يملك المستوى، حتى لو كانت بقية بيانات ملفه محفوظة. عند فتح `/complete-profile` في الويب أو شاشة إكمال الملف في Expo تظهر بياناته الحالية مملوءة، ويحتاج إلى اختيار المستوى فقط ثم الحفظ. لا تُفرض بوابة المستوى على المشرفين أو الإدارة.

يظهر المستوى في بطاقة حساب الطالب المحمولة، وقوائم مستخدمي الإدارة، وبطاقات طلبات المواد، وبيانات طالب تذكرة الدعم. كما تتحقق طلبات المواد من وجود المستوى قبل السماح بالإرسال، ويُصفّر الحقل عند حذف الحساب. تم اختبار التسجيل والملف على مستوى TypeScript/build، أما تجربة الحساب القديم فعليًا فتُستكمل بعد تطبيق الهجرة على قاعدة Railway.

## تحديث طلبات المواد — 27 أغسطس 2026

تقبل واجهة طلب المادة في الويب وExpo الآن عددًا غير محدود من الملفات ضمن إجمالي أقصى قدره 100MB للطلب الواحد. ما زال الخادم يتحقق من امتداد MIME والتوقيع الثنائي للملف، ولا يثق باسم الملف أو نوعه المعلن فقط. حد جسم الطلب 120MB لاستيعاب رؤوس multipart وأسماء الملفات، بينما حد الملفات الفعلي 100MB.

تضيف لوحة الإدارة مرفقات الطلب إلى حمولة الإدارة مع اسم الملف ونوعه وحجمه ورابط تنزيل محمي عبر `/api/supervisor/request-files/:id`. لا يستطيع التنزيل إلا مدير أو مشرف مصرح له بالطلب، وتستخدم Expo Bearer عند تنزيل الملف لتجنب Access Denied.

أضيف الحقل `course_requests.prepared_course_slug` عبر الهجرة `backend/drizzle/0008_public_mystique.sql`. يمكن للإدارة اختيار مادة منشورة مطابقة للجامعة والتخصص ثم الضغط على «تم تجهيز الطلب». يحفظ النظام المادة المختارة، يحول الطلب إلى `available`، ويسجل العملية في audit log، ويرسل إشعارًا داخل المنصة وPush اختياريًا بعنوان «تم تجهيز المادة المطلوبة» وزر «فتح المادة» إلى `/learn/{courseSlug}`. تغيير الحالات الأخرى يرسل إشعار حالة إلى `/dashboard?view=requests`.

تمت إضافة اختبارات آلية لحد الحجم، غياب حد عدد الملفات، إجراء التجهيز، الرابط المباشر، وتصريحات الهجرة. لا يمكن تأكيد وصول Push على جهاز فعلي أو نجاح تنزيل ملف من Railway Volume قبل اختبار قبول بعد النشر.


## تحديث أغلفة الكتالوج والمعرفات التلقائية — 27 أغسطس 2026

أضيفت الهجرة `backend/drizzle/0009_parallel_tarantula.sql`، وتضيف `catalog_courses.cover_image_url` وحقلي `description` غير الفارغين إلى `course_units` و`lessons` مع قيمة افتراضية آمنة للسجلات القديمة. يحفظ الخادم الغلاف الخارجي عبر HTTPS فقط، أو يخزن الملف المرفوع كمرجع `r2:` داخل Railway Volume، بينما يخدمه `/api/covers/{courseSlug}` بنوع MIME الصحيح مع `nosniff` وذاكرة مؤقتة قصيرة.

أصبحت معرفات المؤسسة والتخصص والمادة والدرس قابلة للتوليد تلقائيًا من الاسم والسياق مع hash ثابت، وتبقى المعرفات الحالية محفوظة عند التعديل. لا تسمح واجهات الإدارة في الويب وExpo بتغيير المعرف القديم، ويمكن تركه فارغًا عند الإنشاء ليولده الخادم. أضيف الوصف الاختياري للوحدات والدروس إلى API، مزامنة القوالب، لوحة إدارة الويب، تفاصيل المادة، غرفة التعلم وبطاقات المحتوى في Expo.

تظهر صورة الغلاف في بطاقات المواد والتفاصيل والسلة والدفع وغرفة التعلم على الويب وExpo، مع fallback متدرج عند غيابها. لا يعرض الكتالوج درسًا مجانيًا أو رابط تشغيل على أنه جاهز قبل وجود سجل فيديو بحالة `ready`، وتُظهر الوحدات والدروس وصفها وحالة انتظار الفيديو عند الحاجة. حدود رفع الغلاف 6MB، والأنواع PNG/JPEG/WebP مع فحص magic bytes؛ أما الفيديو فيبقى بحده السابق 200MB.

تم التحقق محليًا من توليد الهجرة ومرور `next build` و`drizzle-kit check` واختبارات Node الخاصة بالمعرفات والعقود ومسارات الغلاف، إضافة إلى lint وtypecheck وExpo Doctor بنتيجة 21/21. لم تُطبق الهجرة ولم يُختبر رفع غلاف أو شعار أو فيديو على Railway الإنتاجية، ولم تُختبر PostgreSQL/Tap/Push الفعلية؛ بعد Redeploy يجب مراقبة تنفيذ 0009، ثم اختبار رفع صورة صغيرة ورابط HTTPS من حساب مدير فعلي، وإعادة فتح مادة من الويب وExpo.

> تبقى الصور والقوالب التي يضيفها المسؤول مسؤولية مالك المنصة. القوالب التنظيمية لا تعني وجود فيديوهات أو شرح أكاديمي رسمي؛ يجب رفع المحتوى المعتمد ومراجعته قبل التسويق للمادة.


## تحسين الهوية والفوتر والكوبونات — 27 أغسطس 2026

أصبح فوتر الويب يستخدم شعار `M` فقط داخل بطاقة بيضاء واضحة، وتظهر تحته أيقونات الشبكات الاجتماعية والبريد وواتساب من إعدادات الإدارة العامة. أضيف فوتر موحد إلى حاوية شاشات Expo ويقرأ نفس `/api/public/settings`، بحيث تتزامن قنوات التواصل مع الويب والتطبيق بعد حفظها من الإدارة. أصبحت إعدادات التواصل تُقرأ ديناميكيًا من الخادم، مع إبطال كاش داخلي قصير واستجابة `no-store`، ولا تُقبل الروابط الاجتماعية إلا عبر HTTPS.

تم استبدال أصول الأيقونة والفافيكون وأيقونة التطبيق وadaptive icon وnotification icon بأصل M عالي الدقة بخلفية بيضاء، كما استُبدلت نسخ شعار الويب منخفضة الدقة بنسخ HQ. بقي الشعار الكامل في مواضع الهوية التي تحتاج اسم المنصة، بينما استُخدم الرمز المختصر في الفوتر والفافيكون وأيقونة التطبيق.

تم تقوية خدمة الكوبونات: تتحقق الإدارة من وجود المادة المخصصة، وصحة الحالة، وقيمة الخصم، وحد النسبة 95%، وبداية الصلاحية ونهايتها واتساقهما. يعاد التحقق من الكوبون خادميًا عند الدفع، ويُوزع الخصم على `order_items` للمادة الصحيحة عند وجود سلة متعددة المواد، بدل افتراض أن المادة الأولى هي المستحقة دائمًا.

نتائج الفحص المحلي بعد هذه التعديلات: `next build` ناجح، اختبارات Node عددها 8/8 ناجحة، `drizzle-kit check` ناجح، وExpo `lint` و`typecheck` و`expo-doctor` بنتيجة 21/21، و`git diff --check` ناجح. تظهر أربعة تحذيرات Next غير مانعة من lint بسبب استخدام `<img>` لأصول يحددها المسؤول؛ لا توجد أخطاء lint أو TypeScript. لم تُختبر Tap أو PostgreSQL أو Push أو حفظ إعدادات الإدارة على Railway الإنتاجية، ولذلك يجب إجراء اختبار قبول محدود بعد Redeploy.


## مركز الدعم الموحد — 27 أغسطس 2026

أصبح مركز الدعم يعرض تذاكر الطالب كبطاقات واضحة في لوحة الطالب، وتفتح البطاقة محادثة التذكرة نفسها عبر `ticketId` مع عرض الرسائل والمرفقات وإرسال ردود جديدة وإعادة فتح التذكرة بعد إغلاقها. كما يقرأ مركز الدعم في صفحة الويب رابط التذكرة من إشعارات المنصة أو بطاقات لوحة الطالب، ويفتح المحادثة المطلوبة مباشرة.

في لوحة الإدارة على الويب وExpo، تعرض التذاكر كبطاقات مستقلة تحتوي عنوان التذكرة ورقمها والطالب وقناة التواصل والمستوى الأكاديمي وعدد الردود. عند اختيار بطاقة تظهر المحادثة الكاملة، مع إرسال نص أو مرفق، تغيير الحالة إلى مفتوحة أو بانتظار الطالب أو محلولة أو مغلقة، والعودة إلى قائمة البطاقات. الحذف النهائي متاح للمدير/المشرف المصرح فقط، ويحذف سجلات التذكرة والردود وملفاتها من قاعدة البيانات والتخزين الدائم. جميع عمليات الملكية والصلاحيات يعاد تحققها في API ولا تعتمد على الواجهة.

نتائج الفحص بعد هذه التعديلات: `next build` ناجح، اختبارات Node عددها 10/10 ناجحة، `drizzle-kit check` ناجح، وExpo `lint` و`typecheck` و`expo-doctor` بنتيجة 21/21، و`git diff --check` ناجح. توجد أربعة تحذيرات Next غير مانعة بسبب استخدام `<img>` في صور الكتالوج/السلة، ولا توجد أخطاء lint أو TypeScript. يلزم بعد النشر اختبار تذكرة فعلية من حساب طالب، رد ومرفق من الإدارة على الويب، ثم الرد وتغيير الحالة والحذف من Expo، دون اعتبار ذلك اختبارًا إنتاجيًا تم داخل هذه البيئة.


## تجربة التجارة والمصادقة — 27 أغسطس 2026

أصبح المساعد الذكي مخفيًا من مسار إدارة الويب ومن شاشة إدارة Expo، مع بقائه متاحًا للطلاب والزوار في المسارات المسموح بها. أضيف مخزن حالة مشترك للمفضلة والسلة في الويب، بحيث يقرأ القوائم من API الخادم، ويحدّث القلب الأحمر والزر المضاف والعدادات في الرأس فور نجاح الإضافة أو الإزالة. صفحات المفضلة والسلة تزامن الحالة نفسها بعد التحميل والإزالة، وتعمل البطاقات بالتبديل الحقيقي بدل إرسال `active: true` دائمًا.

في Expo أضيفت عدادات السلة والمفضلة إلى الرأس، مع تحديث متفائل للقلب وحالة السلة في تفاصيل المادة ثم إعادة التحقق من الخادم. أصبح رأس الدخول والتسجيل مخصصًا ومتجاوبًا: شعار M في اليمين، زر الرئيسية وزر الوضع الليلي في الجهة المقابلة، دون تكرار الشعار داخل نموذج الدخول.

نتائج التحقق: `next build` ناجح، lint الويب ناجح دون أخطاء، اختبارات Node عددها 13/13 ناجحة، `drizzle-kit check` ناجح، وExpo lint وTypeScript و`expo-doctor` بنتيجة 21/21، و`git diff --check` ناجح. توجد أربعة تحذيرات Next غير مانعة بسبب استخدام `<img>` في صور الكتالوج/السلة. لم يتم تنفيذ اختبار قبول على جهاز Android/iOS أو جلسة Railway الإنتاجية؛ يجب بعد Redeploy اختبار الإضافة والإزالة من حساب طالب على الويب وExpo، ثم التأكد من ظهور العدادات بعد إعادة فتح التطبيق.


## قوائم التنقل بعد تسجيل الدخول — 27 أغسطس 2026

تم تحسين قائمة الطالب في رأس الويب لتعرض أيقونة واضحة بجانب كل رابط، مع روابط لوحتي وموادي واستكشاف المواد والسلة والمفضلة والإشعارات والدعم والحساب. أصبحت قائمة الجوال لوحة صغيرة منظمة تحتوي بطاقة الحساب، أيقونات لكل خيار، مؤشرات عدد السلة والمفضلة، وسهم انتقال، مع زر تسجيل خروج صغير يعرض أيقونة الخروج والنص كسطر متناسق بدل زر كبير.

في Expo تم تحسين تبويب حسابي ليضم اختصارات السلة والمفضلة والدعم، وبطاقات الخدمات ذات الأيقونات، وحالة ضغط خفيفة، ونقل تسجيل الخروج إلى صف مستقل صغير بأيقونة ونص ووصف «إنهاء الجلسة بأمان». كما حافظ رأس الدخول والتسجيل على شعار M في اليمين وأزرار الرئيسية والوضع الليلي في الجهة المقابلة.

نتائج التحقق: `next build` ناجح، lint الويب ناجح دون أخطاء مع أربعة تحذيرات صور غير مانعة، Expo lint وTypeScript ناجحان، `expo-doctor` بنتيجة 21/21، `drizzle-kit check` ناجح، اختبارات Node عددها 13/13 ناجحة، و`git diff --check` ناجح. يجب بعد Redeploy فحص القائمة على شاشة جوال فعلية في الوضعين الفاتح والداكن، والتحقق من تسجيل الخروج من الويب وExpo.


## إعدادات المظهر الموحدة — 27 أغسطس 2026

تمت إضافة لوحة مظهر موحدة للويب وExpo، وتظهر للطالب داخل إعدادات الحساب، وللمدير داخل إعدادات المنصة، وللمشرف داخل مساحة المظهر. تشمل اللوحة الوضع الفاتح والليلي، والثيم الرسمي والأزرق والبنفسجي والوردي والفيروزي، وأحجام خط صغير وقياسي وكبير وأكبر. التفضيلات شخصية ومحلية لكل جهاز، ولا تغيّر إعدادات المستخدمين الآخرين أو صلاحيات الإدارة.

يتم تطبيق اللوحة اللونية على متغيرات CSS في الويب وعلى عقد الألوان في Expo، وتتم تهيئة اختيار الويب قبل hydration لتقليل الوميض. يتغير لون الشعار حسب اللون الأساسي للثيم، مع الحفاظ على خلفية شعار M البيضاء. في Expo يحفظ `ThemeProvider` الوضع واللوحة وحجم الخط عبر `SecureStore`، وتطبّق `Screen` مقياس النص على الشاشات المشتركة.

نتائج التحقق: `next build` ناجح، lint الويب ناجح دون أخطاء، Expo lint وTypeScript ناجحان، `expo-doctor` بنتيجة 21/21، `drizzle-kit check` ناجح، اختبارات Node عددها 15/15 ناجحة، و`git diff --check` ناجح. توجد أربعة تحذيرات Next غير مانعة مرتبطة باستخدام `<img>` في صور الكتالوج والسلة. يلزم بعد Redeploy اختبار التفضيلات على متصفح وهاتف فعلي في الوضعين، ثم تجربة حساب طالب ومشرف ومدير للتأكد من ظهور اللوحة في موضعها.

## الحذف الإداري النهائي الآمن — 27 أغسطس 2026

أضيفت خدمة `backend/lib/admin-deletion.ts` ومسار `deleteEntity` داخل `/api/admin/console`. المسار لا يقبل اسم جدول من العميل؛ بل يستخدم قائمة بيضاء ثابتة للجهات والمؤسسات والتخصصات والمواد والوحدات والدروس والفيديوهات والحسابات والطلبات غير المالية وتذاكر الدعم والكوبونات والإشعارات والتقييمات وتكليفات المشرفين. يتطلب المدير المصرح، فحص Origin/جلسة الإدارة أو اعتماد التطبيق الإداري القائم، rate limit الموجود، وتأكيد كلمة «حذف» حرفيًا. تبقى سجلات `audit_logs` ولا توجد واجهة لحذفها.

يُنفذ حذف المادة ومحتواها بترتيب children-first داخل transaction، مع جمع مفاتيح أغلفة وفيديوهات ومرفقات الدعم والطلبات ثم حذف كائنات Railway Volume best-effort بعد نجاح المعاملة. إذا فشل تنظيف ملف، يسجل النظام تحذيرًا واضحًا في سجل التدقيق دون إعادة أسرار أو روابط خاصة. تشمل واجهتا الويب وExpo أزرار خطر منفصلة عن فتح الصفوف، ونافذة الويب تطلب كتابة «حذف»، بينما يعرض Expo `Alert` مؤكدًا قبل الإرسال، ثم يعيد تحميل بيانات لوحة الإدارة.

لا يحذف النظام الطلبات أو الفواتير أو أحداث الدفع أو سجل idempotency المالي. تُمنع إزالة مادة لها طلبات مدفوعة أو وصول فعال، ويُمنع حذف حساب مرتبط بتاريخ مالي، كما يُمنع حذف الحساب الإداري الحالي أو آخر مدير نشط. تستخدم المؤسسات والتخصصات المرتبطة بطلاب أو طلبات أو مواد مالية الإخفاء أو تحديث العلاقات بدل الإزالة القسرية. هذه القيود مقصودة لحماية السجل المالي والتشغيلي.

تمت إضافة اختبار عقدي للحذف، ونجحت فحوص backend: `npm run lint` مع أربعة تحذيرات صور غير مانعة، و`next build`، و`node --test tests/*.test.mjs` بعدد 19/19، و`npx drizzle-kit check`. ونجحت فحوص Expo: `npm run typecheck` و`npm run lint` مع تحذيرين قديمين في `AppearanceSettings.tsx`. لم تُنفذ عملية حذف فعلية على PostgreSQL أو Railway Volume أو Tap في الإنتاج، ولم يُدّع ذلك؛ يلزم اختبار قبول محدود بعد Redeploy بمدير فعلي وسجلات اختبار غير مهمة، مع التحقق من بقاء الطلبات والفواتير وسجل التدقيق.
