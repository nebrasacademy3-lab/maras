# ملفات الدروس وأدوات مراس — تشغيل وفحص

## نطاق هذا التحديث

أدوات التلخيص والترجمة والاختبار تعمل من ملف الطالب أو ملف معتمد ربطته الإدارة بالدرس. الويب والتطبيق يستخدمان نفس واجهات الخدمة والطابور ونتائج الاختبارات. ملفات النتائج بصيغة DOCX تحمل اسم مراس العلم وإسناد حقوق المصدر؛ هذا التحديث لا يضيف تصدير PDF أو ترجمة ملف PowerPoint بنفس تخطيط الشرائح.

في إدارة الويب: ملفات المواد ← المادة ← رفع الملف ← اختيار الدرس أو «مشترك». وفي تطبيق الإدارة: الكتالوج ← ملفات الدروس وأدواتها. لا يظهر الملف قبل اجتياز الفحص الأمني وتفعيل ظهوره. يستطيع المشترك استخدام الأدوات تحت الفيديو دون إعادة رفع الملف.

المعرّفات العامة للجهات والتخصصات والمواد والدروس والباقات والمسارات تُنشأ عند تركها فارغة، ويمكن إدخالها يدويًا عند الإنشاء. محاولة إنشاء معرّف موجود ترفض بدل الكتابة فوق سجل آخر. لا تُغيّر مفاتيح قاعدة البيانات الداخلية أو المعرّفات القديمة المرتبطة باشتراكات أو روابط منشورة.

## نشر آمن

1. احتفظ بنسخة احتياطية لقاعدة البيانات واختبر التحديث في بيئة منفصلة قبل الإنتاج. الهجرتان `0030_lesson_study_tools.sql` و`0031_study_source_indexes.sql` إضافيتان؛ لا تعيدان تسمية البيانات القديمة. شغّل migrations مرة واحدة وفق إجراء النشر الحالي، وليس `db:push` على الإنتاج.
2. شغّل `npm run ai:worker` كعملية خادم مستمرة مع نفس قاعدة البيانات والتخزين الخاص. سكربت Railway الحالي يبدأ عاملًا تلقائيًا ما لم يكن `AI_WORKER_ENABLED=false`. عند فصل العامل إلى خدمة مستقلة، عطّله في خدمة الويب وشغّله في الخدمة المخصصة. استخدم restart policy ومراقبة حقيقية للعملية؛ لا يكفي وجودها في خلفية shell لضمان استمرارها.
3. يجب أن يتشارك الويب والعامل مخزن S3 الخاص نفسه. التخزين المحلي مناسب لاختبار جهاز واحد فقط؛ ملفات محلية على عقدة لا تكون مرئية تلقائيًا للعقد الأخرى. لا تشارك الأسرار مع العميل أو GitHub.
4. لا ترفع عدد النسخ أو حدود Gemini عشوائيًا. مجموع اتصالات PostgreSQL = أحواض الويب + عوامل الفيديو + عوامل AI + الفاحص + هامش الإدارة. اجعل `DATABASE_POOL_MAX` لكل عملية ملائمًا لحد اتصال قاعدة بياناتك. تم توحيد أقفال AI ومعالجة الفيديو في PostgreSQL لتعمل بين العمليات.

### إعدادات ابتدائية محافظة، وليست ضمانًا للسعة

| المتغير | الافتراضي | الغرض |
| --- | --- | --- |
| AI_WORKER_ENABLED | true | بدء عامل الأدوات داخل سكربت Railway |
| AI_WORKER_MAX_ACTIVE | 2 | أقصى وظائف ملفات نشطة عبر العمليات |
| AI_PROVIDER_MAX_CONCURRENT | 2 | أقصى اتصالات توليد Gemini المتزامنة |
| AI_PROVIDER_MIN_INTERVAL_MS | 5000 | أقل فاصل بين بدايات طلبات Gemini |
| AI_MAX_QUEUED_FILE_JOBS | 500 | حد الطابور العام قبل الرفض المنظم |
| VIDEO_WORKER_MAX_ACTIVE | 1 | أقصى عمليات تحويل فيديو نشطة عبر النسخ |
| VIDEO_FFMPEG_THREADS | 2 | ميزانية خيوط التشفير/فك الترميز |

هناك حد طلبين معلقين لكل مستخدم، وطلب نشط واحد لكل مستخدم. المهام تنتظر حتى 30 دقيقة ثم تفشل برسالة واضحة دون خصم حصة للانتظار. السجل النهائي للوظائف يُحذف بعد 7 أيام والنتائج المخبأة بعد 14 يومًا، على دفعات صغيرة. سجلات المحادثات والملفات الناتجة الخاصة بالطالب تخضع لسياسة الاحتفاظ الأصلية؛ تنظيف الطابور لا يحذفها.

## Gemini المجاني واستهلاك التوكنز

الحدود تتعلق بالمشروع والنموذج ومستوى الخدمة، لا بعدد مفاتيح API داخل المشروع. المرجع الرسمي: https://ai.google.dev/gemini-api/docs/rate-limits

هذا التحديث لا يتجاوز الحصص: يحفظ نتائج المصدر نفسه والإعدادات نفسها، يجمع الطلبات المتكررة، يحد التزامن، ويتراجع عند 429. ملف الإدارة المعتمد يمكن أن يعيد استخدام نتيجة بين المشتركين بعد فحص الصلاحية؛ ملفات الطلاب الخاصة لا تشارك نتائجها بين مستخدمين. تغيير محتوى الملف أو النموذج أو التعليمات يبطل المفتاح المخبأ. إعادة الاختبار وتصحيح الإجابات لا تطلب توليدًا جديدًا. حصة الاستخدام الشهرية للطالب ليست حصة Google الفعلية.

التلخيص والاختبار يستفيدان أكثر من التخزين المؤقت. الترجمة الكاملة لمستند طويل تحتاج مخرجات طويلة بطبيعتها؛ لا يوجد توفير يلغي تكلفة تمثيل النص نفسه. يُرفض رد Gemini المبتور بدل عرضه على أنه ترجمة مكتملة. لم نستخدم مفتاح الإنتاج لإثبات جودة الإجابات العلمية، وهي تحتاج مراجعة تعليمية بشرية.

## صيغ الملفات والحدود

DOCX وPPTX: استخراج النص والجداول وترتيب الفقرات/الشرائح، وليس OCR للصور داخل المستند. للمخططات والمعادلات المصوّرة استخدم PDF. صيغا DOC/PPT القديمتان، الملفات المشفرة، الماكرو، الملفات التالفة والأرشيفات المفرطة في الضغط ليست مدعومة. المستندات النصية التي تتجاوز 60 ألف محرف تُرفض مع طلب تقسيمها بدل بترها بصمت. حد رفع ملفات AI الحالي 20 ميجابايت افتراضيًا؛ موارد المادة قد تسمح 25 ميجابايت، لكن تشغيل الأداة يظل مقيدًا بإعداداتها.

## الفيديو وآلاف المتزامنين

تم إصلاح ربط S3 المتكرر بحيث لا يحذف الفيديو المرفوع عند إعادة المحاولة، وإعادة المحاولة المحدودة لمرحلة التأكيد لا ترفع البايتات ثانية. لا يُحذف الكائن تلقائيًا عند فشل التأكيد لأن طلبًا سابقًا قد يكون ربطه بالفعل. راقب الكائنات اليتيمة وضع سياسة تنظيف تتحقق من مراجع قاعدة البيانات قبل الحذف. هذا ليس استئناف رفع multipart؛ حد الرفع المفرد الحالي لم يُرفع.

ما زالت مقاطع HLS تمر عبر مسارات الخادم المحمية في التصميم الحالي. لآلاف المشاهدين يجب قياس الشبكة والـegress وحمل فك التوقيع/قاعدة البيانات، واختيار توزيع فيديو CDN يدعم صلاحية المشاهدة والتوقيع وقواعد التخزين الخاصة. لا تجعل bucket عامًا ولا تحذف التحقق من الاشتراك لتحقيق السرعة. فصل تحويل الفيديو عن الويب أفضل تحت الحمل؛ زيادة نسخ الويب وحدها لا تعالج عرض النطاق أو حصص Gemini المجانية.

**لا يوجد في هذا التحديث ادعاء بأن البنية الحالية اختُبرت على آلاف المستخدمين أو أصبحت خالية من الأخطاء.**

## الفحوص

- `npm run lint` ثم `npm run build` ثم `node --test --test-concurrency=1 tests/*.test.mjs`.
- داخل `mobile`: `npm run typecheck`، `npm run lint`، `npm test`.
- `npm run qa:study-tools` يحتاج قاعدة محلية اصطناعية أنشأتها أدوات QA؛ يرفض عنوان إنتاج ويستخدم مزوّد Gemini وهميًا داخل العملية. يختبر تكرار الطلبات، التخزين المشترك المصرح به، فصل ملكية المخرجات، إلغاء الاشتراك، أسئلة الاختبار وأقفال العامل.
- `scripts/qa-study-ci.mjs` يجمع الهجرات والبيانات الاصطناعية واختبار المتصفح في GitHub Actions. صور الفاتح/الداكن والهاتف والنتيجة تحفظ كأدلة؛ لا يحفظ ملفات جلسات الاختبار. فيديو بيانات الاختبار metadata-only، فلا تعتبر الصور دليلًا على تشغيل فيديو حقيقي.
- `scripts/staging-load.mjs` فحص HTTP محدود للـstaging فقط. لا يولد Gemini، ولا يرفع فيديو، ولا ينفذ مدفوعات. لا يمثل بمفرده سعة البث أو تجربة كل مستخدم. زد الحمل تدريجيًا وراقب p95 والأخطاء والذاكرة والاتصالات والطابور وعرض النطاق قبل كل خطوة.

## تراجع الإصدار

يمكن إعادة نسخة الكود السابقة مع إبقاء الجداول والأعمدة الإضافية. أوقف عامل AI عند التراجع، ولا تحذف جداول المستخدمين أو نتائجهم. عطّل الأدوات مؤقتًا من إعداداتها عند تعطل العامل، وتحقق من الطابور قبل إعادة تفعيلها. التطبيق يحتاج بناءً/تحديثًا جديدًا بالطريقة المعتمدة؛ رفع كود GitHub لا ينشر نسخة المتجر تلقائيًا.
