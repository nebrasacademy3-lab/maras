# تحديث مراس العلم — Railway / Web / API

هذه النسخة تحتوي كل تغييرات الخادم والويب المطلوبة للتطبيق الجديد.

## أهم التطويرات

- تسجيل الخروج ينتقل للرئيسية مباشرة ويُلغي الجلسة.
- شعارات عالية الدقة للوضع الفاتح والليلي، مع أيقونة PWA آمنة غير مقصوصة.
- البحث العام يعرض زر طلب مادة وهدف التوفير خلال 24 ساعة عند عدم وجود نتائج.
- طلب المادة: رابط HTTPS مستقل + ملفات السلايدات/التوصيف + عرض الرابط للإدارة + تنزيل كل المرفقات ZIP.
- حد أجهزة الطالب من الإدارة، الافتراضي جهازان عبر الويب والتطبيق معًا.
- كل جهاز له هوية مستقرة؛ إعادة الدخول من الجهاز نفسه لا تحسب جهازًا إضافيًا.
- قفل transaction advisory يمنع سباق تسجيل الدخول من تجاوز حد الأجهزة.
- الإدارة ترى الجلسات/الأجهزة وتستطيع تسجيل خروج جهاز محدد.
- منح مادة من الإدارة يتطلب/يقبل سعرًا ويسجل Order + Order Item + Payment Event مدفوع حتى يدخل الإيرادات والمدفوعات والإحصائيات.
- إعلانات مطورة: قوالب، بانر، Popup، صندوق إشعارات، رابط داخلي أو HTTPS خارجي، مادة من النظام، جمهور، مستخدم محدد مع البحث، Push.
- الإعلانات الموجهة تظهر فقط للمستخدم المناسب عند تسجيل الدخول، ولا تتسرب للزوار.
- Migration الجديدة `0014_device_limits_ads_requests.sql` تضيف الحقول والإعدادات المطلوبة.

## ترتيب النشر

1. ارفع هذه النسخة إلى Railway أولًا.
2. تأكد من PostgreSQL و`DATABASE_URL`.
3. Volume عند `/data`.
4. `RUN_DB_MIGRATIONS=true` حتى تطبق migrations تلقائيًا.
5. بعد نجاح `/api/health` ابنِ تطبيق EAS الجديد.

لا تضع مفاتيح حقيقية داخل ملفات المشروع. ضع أسرار Tap/Resend/OpenAI في Railway Variables فقط.
