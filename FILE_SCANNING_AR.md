# إصلاح فحص الملفات وتنزيلها — مراس العلم

## ماذا كان يحدث؟

كانت مرفقات الطلبات تبقى `pending` عندما لا يوجد `MALWARE_SCAN_URL` صالح. لم تكن صورة Docker تتضمن محرك فحص، ولم يكن بدء تشغيل الخادم يشغّل عامل إعادة فحص المرفقات تلقائيًا. كما أن المسار الإداري القديم لم يشمل ملفات أدوات مراس. هذه نتائج مراجعة المصدر المرفق؛ لم تُقرأ متغيرات استضافتك الحية أو سجلاتها.

لم يُحل العطل بإلغاء الفحص أو بتحويل الملفات يدويًا إلى `clean`. أصبحت ملفات الطلبات والدعم تُحفظ في طابور دائم بعد التحقق من نوعها وحجمها، ويعمل عامل الخادم دوريًا لفحصها. تشمل المعالجة الطلبات والدعم وموارد التعلم وملفات أدوات مراس. يبقى الملف محميًا أثناء تعطل المحرك، ويُتاح بعد حفظ نتيجة ناجحة فعلية.

## تشغيل النسخة على Railway باستخدام Docker المرفق

خذ نسخة احتياطية لقاعدة البيانات ومجلد الملفات/الحاوية التخزينية أولًا، وجرّب على بيئة مرحلية قبل استبدال الإنتاج. ارفع **المشروع كاملًا**؛ نسخ صفحات الإدارة وحدها لا يشغّل محرك الفحص.

الإعدادات التالية مضمنة في Dockerfile:

```dotenv
LOCAL_MALWARE_SCANNER_ENABLED=true
CLAMD_SOCKET=/tmp/meras-clamav/clamd.sock
FILE_SCAN_SCHEDULER_ENABLED=true
```

اترك `MALWARE_SCAN_URL` و`CLAMD_HOST` فارغين عند استخدام المحرك المحلي. وجود إعداد خارجي قديم أو غير صالح يمنع استخدام المحلي عمدًا؛ لا يوجد انتقال صامت قد يخفي خطأ التهيئة.

تتضمن الصورة حزم ClamAV وFreshClam ومشرف عمليات `tini`. يبدأ السكربت تنزيل تعريفات الفيروسات، ثم المحرك على مقبس Unix خاص، ويعيد تشغيله عند توقفه. لا تُضمَّن التعريفات في ZIP؛ يلزم اتصال خارجي صالح لتنزيلها وتحديثها. لن ينجح الفحص قبل جاهزيتها. لا تفتح منفذ ClamAV للعامة.

يُحفظ دليل التعريفات افتراضيًا في `/data/clamav` أو تحت `RAILWAY_VOLUME_MOUNT_PATH`. يمكن تحديد `CLAMAV_DATABASE_DIR`. يحتاج الدليل ومجلد الرفع إلى صلاحيات كتابة لمستخدم الحاوية غير الجذري `node`، ويفضل Volume دائم لتجنب إعادة تنزيل التعريفات مع كل نشر. لا يَنقل هذا الإصلاح الملفات القديمة بين Local وS3 تلقائيًا.

**الذاكرة مهمة:** توصي وثائق ClamAV بذاكرة في حدود 4 GiB لحاوية الفحص. يحتاج تطبيق Next.js والفيديو ومساحة العمل إلى موارد إضافية. لا تفترض أن خطة صغيرة تكفي لتشغيلها جميعًا في الحاوية نفسها. استخدم خدمة فحص منفصلة خاصة عند محدودية موارد خدمة الويب، وراقب الذاكرة فعليًا. لم يُختبر استهلاك هذه الصورة هنا.

سياسة التعريفات في السكربت: تحديث دوري كل ساعة بعد النجاح، إعادة محاولة عند التعذر، ومنع تشغيل المحرك بتعريفات لم يُسجَّل نجاح تحديثها خلال سبعة أيام. هذا حد تشغيلي لهذه النسخة وليس ضمانًا لاكتشاف جميع التهديدات.

## الترحيل

الترحيل الجديد هو `0030_reliable_file_scanning.sql`، ويأتي **بعد سلسلة الترحيلات الحالية حتى 0029**. يضيف سجلات المحاولات، موعد الإعادة، وأقفال الفحص ومصدر التخزين. لا تشغّله منفردًا على قاعدة فارغة. سكربت Railway الحالي يشغّل `drizzle-kit migrate` عند `RUN_DB_MIGRATIONS=true`.

في بيئة مرحلية مجهزة بالحزم:

```bash
npm ci
npm run db:migrate
```

يعيد الترحيل ملفاتًا وُسمت سابقًا بنمط `development-signature-check` إلى الانتظار لأن فحص التوقيع/النوع ليس فحصًا للبرمجيات الخبيثة. لذلك قد تنتظر هذه الملفات فحصًا حقيقيًا لأول مرة. موارد التعلم من هذه الفئة تُخفى إلى أن يراجعها المشرف؛ لا ينشر العامل موردًا مخفيًا تلقائيًا.

سجلات الملفات القديمة تحمل `storage_provider=legacy`: تستخدم إعداد التخزين السابق حفاظًا على التوافق، لا تخمينًا بأن كل الملفات موجودة محليًا. لا تغيّر `STORAGE_PROVIDER` أو الحاوية/المسار الذي توجد فيه ملفاتك القديمة قبل وضع خطة ترحيل ومطابقة فعلية للملفات. الرفعات الجديدة تحفظ المصدر المحدد مع كل ملف.

## متابعة المشكلة من الإدارة

افتح `/admin/files` بعد النشر. تعرض الصفحة إعداد المحرك، استجابة اتصال ClamAV، حالة العامل، أعداد الملفات وأسباب التعثر والمحاولة القادمة. فحص الاتصال وحده ليس اختبار كشف فيروسات. مع خدمة HTTP تُعرض جاهزية التهيئة دون ادعاء اختبار اتصال فعلي.

بعد إصلاح إعداد المحرك، اضغط «استئناف فحص المرفقات المعلقة». يبدأ دفعة محدودة، ويكمل العامل الباقي. لا يعني نجاح طلب تشغيل الدفعة أن كل الملفات اجتازت الفحص. في التطبيق يوجد قسم «أمان المرفقات» داخل المركز التشغيلي للإدارة.

يمكن تشغيل دفعة يدوية من بيئة الخادم المجهزة:

```bash
npm run files:scan
```

العامل المدمج مناسب للخادم الدائم مثل صورة Docker؛ بيئات Serverless التي تجمّد العملية تحتاج عاملًا خارجيًا/مهمة مجدولة حقيقية. يمكن تعطيل العامل الداخلي بـ `FILE_SCAN_SCHEDULER_ENABLED=false` عند تشغيل عامل بديل. يمنع قفل قاعدة البيانات تكرار فحص الملف نفسه بين النسخ، لكن حدود تزامن الذاكرة محلية لكل عملية؛ يلزم ضبط عدد النسخ وسعة المحرك الخارجي.

## محرك خارجي بدلًا من المحلي

محرك ClamAV على شبكة داخلية موثوقة:

```dotenv
LOCAL_MALWARE_SCANNER_ENABLED=false
CLAMD_HOST=private-scanner-host
CLAMD_PORT=3310
FILE_SCAN_SCHEDULER_ENABLED=true
```

اسم المضيف أعلاه مثال يجب استبداله بعنوان خدمتك الداخلية. يأخذ `CLAMD_HOST` أولوية على مقبس Docker الافتراضي. اضبط في المحرك الخارجي حدود الحجم والفك والتشفير والتحديثات؛ اختبار الربط لا يفرض إعداداته عليه. منفذ ClamAV TCP لا يقدم مصادقة أو تشفيرًا مدمجين، فلا يصلح كخدمة مكشوفة على الإنترنت.

بديل HTTP موثوق تملكه أو تثق بمشغله:

```dotenv
LOCAL_MALWARE_SCANNER_ENABLED=false
MALWARE_SCAN_URL=https://your-private-scanner.example/scan
MALWARE_SCAN_TOKEN=replace-with-a-secret-from-your-secret-manager
```

العنوان والرمز أمثلة وليسا خدمة جاهزة. الطلب `POST` يرسل **بايتات الملف الخام**، لا JSON ولا multipart، مع نوع المحتوى واسم الملف المرمّز ورمز Bearer. تُرفض إعادة التوجيه كي لا تنتقل الملفات أو الرموز إلى عنوان آخر. اجعل الخدمة HTTPS خارج شبكة محمية؛ صلاحية عنوان الإعداد لا تعني موثوقية مزوده.

أمثلة للرد:

```json
{"clean": true, "engine": "clamav"}
```

```json
{"status": "infected", "threat": "detected-threat", "engine": "clamav"}
```

يؤدي `clean:false` أو تصريح إصابة أو تهديد غير فارغ إلى الحجر حتى لو ظهر `clean:true` أيضًا. الرد الغامض أو خطأ المصادقة/الحصة/الخادم يبقي الملف معلقًا. لا تربط خدمة عامة قد تحفظ مستندات الطلاب أو تنشرها دون سند مناسب وإذن.

## اختبار المحرك الفعلي بعد تشغيله

```bash
npm run files:verify-live
```

هذا أمر مضاف للتحقق **بعد تجهيز الاستضافة**. يرسل نصًا سليمًا، ثم نمط EICAR الاختباري غير الضار، ثم ZIP يحتوي النمط. المطلوب قبول السليم وحظر الاختبارين الآخرين. لا يغيّر بيانات الطلبات أو الطلاب، ويخرج بفشل عند عدم توافر المحرك أو اختلاف النتيجة. لم يُشغَّل اختبار الكشف الفعلي في بيئة تجهيز هذا التسليم لغياب المحرك والتعريفات.

اختبر أيضًا طلبًا حقيقيًا في البيئة المرحلية: ارفع PDF صغيرًا بحساب طالب، راقب انتقاله من الانتظار إلى الاجتياز، ونزّله من حسابه ومن الإدارة. جرّب طالبًا آخر وتأكد من الرفض. أوقف المحرك مؤقتًا في البيئة المرحلية وتأكد من بقاء الملف الجديد محميًا، ثم أعده وتحقق من استئناف الطابور. اختبر تنزيل ZIP كاملًا والتطبيقين على أجهزة حقيقية قبل الإطلاق.

## دلالات الأخطاء

| الحالة | الإجراء |
|---|---|
| `scanner_not_configured` / `scanner_invalid_configuration` | أصلح إعداد المحرك، ثم استأنف الطابور. |
| `scanner_connection_failed` / `scanner_timeout` | راجع العملية والمقبس/الشبكة والذاكرة والتعريفات. |
| `scanner_http_401` / `403` | راجع رمز خدمة HTTP وصلاحياته. |
| `scanner_http_429` / `503` | راجع الحصة/جاهزية الخدمة؛ الإعادة تدريجية. |
| `stored_object_missing` | استعد الملف من التخزين الصحيح أو أعد رفعه؛ تغيير حالة الفحص لا يستعيد ملفًا مفقودًا. |
| `scanner_size_limit` | الملف أو المحتوى المفكوك يتجاوز سياسة المحرك؛ راجع الحجم ولا تتجاوز الحماية. |
| `quarantined` | لا يُعاد إتاحته بزر تنزيل أو استئناف عام. ارفع نسخة سليمة بعد مراجعة السبب. |

الحجر هنا **حظر وصول تطبيقي**، وليس نقلًا تلقائيًا إلى حاوية تخزين منفصلة. حافظ على خصوصية الحاوية الأصلية وعدم نشر روابط الملفات مباشرة؛ استخدام حاوية عامة يتجاوز حدود حماية التطبيق. لا يستبدل هذا الفحص حماية الأجهزة أو مراجعة الأمان الدورية، وقد تقع نتائج إيجابية كاذبة.

## المراجع

- [ClamAV: الفحص والتعريفات والتحذير من كشف TCP](https://docs.clamav.net/manual/Usage/Scanning.html)
- [ClamAV: تشغيل Docker والذاكرة واستدامة التعريفات](https://docs.clamav.net/manual/Installing/Docker.html)
- [OWASP: ضوابط رفع الملفات متعددة الطبقات](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html)

المراجع لتفسير الضوابط، وليست شهادة تدقيق أو نجاح اختبارات للمشروع.
