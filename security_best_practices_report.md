# مراجعة الأمان المصاحبة لتطوير مراس — 23 سبتمبر 2026

## النتيجة والنطاق
طُبّقت إصلاحات على عودة الدفع والاسترداد، منح الفيديو، تنزيل نتائج الذكاء، ومسارات الإدارة وتكليفات الشارحين. شملت المراجعة Next.js/React وواجهات Expo المشتركة وقاعدة PostgreSQL اصطناعية محلية. لم يُجر اختبار اختراق شامل للإنتاج، ولم تُستخدم أموال أو ملفات مستخدمين حقيقية. عدم ظهور تجاوز في الاختبارات لا يثبت خلو المنصة كلها من الثغرات.

## نتائج عولجت

### S01 — مرتفع: منع إعادة الاسترداد أثناء غموض رد المزود
قد ينشئ المزود الاسترداد حتى لو انقطع الاتصال. تصنيفه فشلًا نهائيًا يسمح بمحاولة مالية أخرى. يبقى الرد الناجح الناقص و408/409/425/429 و5xx في حالة تسوية معلقة. المرجع: lib/tap-payments.ts:3، app/api/admin/refunds/route.ts:77، tests/tap-refund-ambiguity.test.mjs.

### S02 — مرتفع: ربط تشغيل الفيديو بجلسة فعلية
تُرفض المنح القديمة دون ربط جلسة أو إثبات معاينة. يُتحقق من الحساب والجلسة وحق المادة لكل مقطع. أُزيل استعلام الجلسة المكرر داخل الطلب دون تخزين قرار الإذن بين الطلبات. المرجع: lib/video-access.ts:24، tests/encrypted-hls-session.test.mjs.

### S03 — متوسط: عودة Tap لا تثق بمدخلات المتصفح
التحديث يقرأ معرف العملية المحفوظ للحساب ويمر عبر المتحقق المالي نفسه، مع حد مشترك كل 15 ثانية وعدم تجاوز المراجعة المالية. لا يقبل معرف العملية من الرابط كدليل دفع. المرجع: lib/tap-return.ts:13، tests/tap-return-verification.test.mjs.

### S04 — متوسط: وجهة الإدارة مع إنفاذ الصلاحيات على الخادم
دخول المدير الأعلى والموظف المفوض يذهب للإدارة بعد بوابات التحقق، ولو حمل رابط الدخول وجهة طالب. تبقى صلاحيات التنفيذ على الخادم وMFA للعمليات الحساسة؛ إخفاء القسم ليس آلية التفويض. المرجع: lib/account-readiness.ts:4 و:31، lib/permissions.ts:8 و:23، tests/workspace-routing-behavior.test.mjs.

### S05 — متوسط: تكليف الشارح يحافظ على هوية المحتوى
تُربط المسودة بمعرفات الوحدات والدروس الأصلية بدل تكرارها. يُمنع حذف مصدر الخطة من مساحة الشارح واستبدال فيديو معتمد ضمنيًا، وتتوقف المراجعة عند تغير مصدر حساس. التحديث والنشر يستخدمان الأقفال والمعاملات. المرجع: lib/instructor-assignments.ts:45 و:163، drizzle/0048_instructor_course_structure.sql، scripts/qa-instructor-existing-course.ts.

### S06 — متوسط: مخرجات الذكاء محمية ومحدودة
التنزيل PDF فقط عبر ملكية الحساب والصلاحيات، مع إعادة التحقق بعد التصدير. تطبق qpdf تشفير AES-256 وكلمة مالك عشوائية عبر stdin فلا تظهر في قائمة العمليات. فشل الحماية يمنع إرجاع نسخة غير محمية. قوالب المعادلات لا تسمح بمحتوى موثوق عشوائي، وسياق المعلم المتكرر محدود بآخر 8 رسائل و12000 حرف. المرجع: scripts/study-pdf-protection.mjs:8، lib/ai-generation.ts:34، lib/study-rich-text.mjs:14.

## حدود قبل الإطلاق الواسع
- HLS وصلاحيات PDF ليست DRM، ولا تمنع تسجيل الشاشة أو قارئًا يتجاهل صلاحيات المالك. تشديد الفيديو أكثر يحتاج خدمة تراخيص DRM وتوزيع وسائط مناسب.
- لم يُثبت حمل مئات آلاف المتزامنين. مسار الوسائط الحالي عبر التطبيق وقاعدة البيانات عامل حاسم؛ يلزم فصل العمال وCDN وتخزين خاص مشترك وقياس حمل واقعي تدريجي.
- يلزم قبول Tap sandbox بحساب التاجر، خصوصًا 3DS والتكرار والاسترداد الجزئي وفقد الاتصال. الاختبارات المحلية لا تثبت تفعيل الوسائل أو التسوية البنكية.
- اختبارات عزل PDF وملكية POSIX والإشارات الخاصة بـLinux تبقى فعالة في Ubuntu CI. تخطيها على Windows لا يعني اجتيازها.
- شروط العقود قابلة للمراجعة؛ لا تمثل مراجعة قانونية ولا تنشئ تحويلًا ماليًا تلقائيًا عند انتهاء التجربة.

تفاصيل التشغيل: [Tap والفيديو](docs/tap-video-readiness-2026-09-23.md)، [أدوات الدراسة](docs/study-tools-readiness-2026-09-23.md)، [Windows وLinux](docs/windows-test-portability-2026-09-23.md).

