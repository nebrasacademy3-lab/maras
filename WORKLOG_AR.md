# سجل جاهزية منصة مراس العلم

بدأ العمل: 2026-09-20. هذا الملف يوثق التنفيذ والتحقق الفعلي؛ لا يعني اكتمال أي بند قبل وجود دليل اختبار.

## ضوابط العمل
- الحفاظ على الطلاب والطلبات الحقيقية وعدم تغييرها في الاختبارات.
- حصر التجارب التي تكتب بيانات في حسابات وسجلات اختبار معروفة.
- عدم حفظ كلمات المرور أو مفاتيح الخدمات أو مستندات الهوية في هذا السجل أو Git.
- فحص تغييرات قاعدة البيانات قبل نشرها؛ عدم تنفيذ اختبارات ضغط على الإنتاج.
- الأمان وقبول المتاجر والفهرسة والسعة تحتاج أدلة تحقق، ولا يمكن ضمانها بنسبة مطلقة.

## مسارات التنفيذ
| المسار | الحالة | الدليل / المتبقي |
| --- | --- | --- |
| فحص المستودع وRailway وأخطاء النشر | جارٍ | بدأت قراءة الإعدادات؛ شجرة Git نظيفة عند البداية |
| تحليل تقرير Search Console وإصلاح SEO | قيد العمل | التقرير المرفق سيُقرأ كبيانات فقط |
| إزالة RevenueCat وضبط سلوك اشتراكات التطبيقات | قيد العمل | يلزم مطابقة سياسات المتاجر الحالية |
| تدقيق Tap وسلامة المدفوعات والاسترداد | قيد العمل | يلزم توثيق المزود واختبارات دون تحصيل حقيقي |
| نظام الشارحين والتحقق والعقود والمحتوى والإدارة | تنفيذ واختبار | الجداول والواجهات والعقود والتوقيع قيد اختبارات القبول المحلية |
| أمن الحسابات والمشرفين والنطاقات وجهازين و2FA | قيد الفحص | اختبارات محلية ثم اختبارات حسابات مخصصة |
| اختبار الطالب والإدارة والدعم والإشعارات | لم يبدأ | عدم لمس السجلات الحقيقية |
| تحسين الصفحات العامة والحركة وإتاحة الاستخدام | أُنجزت تغييرات محلية | فحص عرض 1440 و390 بكسل وتقليل الحركة؛ لم تُنشر |
| اختبارات البناء والتكامل والأداء والاستعادة | لم يبدأ | توثيق الحدود والمتطلبات الخارجية |

## الأحداث
- إنشاء هذا السجل قبل تغييرات المنتج.
- بيئة الأوامر المعزولة تعذرت بسبب إعداد ACL؛ نجحت قراءة المشروع عبر مسار التنفيذ المصرح.
- المشروع يستخدم Next.js 16.3.4 وExpo 57؛ ستُقرأ الأدلة المحلية ذات الصلة قبل تعديل الكود.

## عوائق أو قرارات تحتاج متابعة
- لم تُختبر بعد مفاتيح Tap أو بناء المتاجر أو بيئة الإنتاج.
- لا تُزال noindex من الصفحات الخاصة؛ تصنيف الروابط يسبق أي تعديل للفهرسة.


## تقدم التنفيذ — المرحلة الأولى (2026-09-20)
- فرع العمل: `codex/platform-readiness-20260920`. لم يُنشر أي تعديل من هذا الفرع حتى الآن.
- Railway: بناء آخر نسخة نجح، لكن pre-deploy فشل عند قراءة GitHub CI بلا مصادقة لمستودع خاص. أُضيف دعم رمز مخصص Actions:read مع رفض آمن دون تسريب؛ 14 اختباراً ناجحاً. تهيئة النشر الفعلي ما زالت مطلوبة.
- SEO: التقرير المرفق إجمالي (1,793 مستبعدة و118 مفهرسة، آخر بيانات 2026-09-18)، ولا يحتوي قائمة الروابط. فحص 204 روابط sitemap مباشرة نجح. عُثر على روابط تخصصات فارغة واستعلامات بحث كثيرة تغذي noindex؛ أُصلحت آلية اكتشافها مع إبقاء الصفحات الخاصة غير مفهرسة. 45 اختباراً ناجحاً؛ التقرير المنفصل في docs/seo-audit-2026-09-20.md.
- Tap: أُصلح تكرار المحاولات ومفتاح idempotency وحالات النتيجة المجهولة والتحقق الرسمي من webhook. 80 اختباراً محلياً ناجحاً؛ لم يُجرَ دفع حقيقي. التفاصيل في docs/payments-audit-2026-09-20.md.
- التطبيقات: حذف RevenueCat من المسار النشط مع حفظ حقوق المشتريات السابقة؛ إعداد reader للمتاجر وحماية روابط الدفع. 96 اختباراً محلياً وفحص الأنواع والتحليل نجحت قبل إضافة واجهات الشارحين.
- الشارح: أُضيف دور مستقل وحد الجهازين؛ 9 اختبارات تسجيل أجهزة نجحت. أُضيف تشفير مرتبط بمالك البيانات وغرضها والتحقق من التوقيع والآيبان؛ 5 اختبارات أمان نجحت.
- بدأ إنشاء جداول الشارحين والوثائق والعقود والمسودات في ترحيل إضافي 0046؛ لم يُطبق على الإنتاج. العقود المعروضة/الموقعة محمية من تغيير نصها.
- أكد المستخدم أن العلاقة عقد عمل بأجر حسب الساعات أو المادة. سيضيف مرجع التوثيق من الإدارة.
- رفضت المراجعة الآلية تفعيل جمع نسخ الهوية بمرجع ومدة افتراضيين. البديل المنفذ: حقول إدارة فارغة للمرجع النظامي ومدة الاحتفاظ، مع بقاء جمع الهوية مقفلاً حتى حفظ القيم الفعلية. لا تغيّر هذا الرفض بقية النظام.
- قاعدة PostgreSQL اصطناعية محلية بدأت على loopback:55439؛ لا اتصال بقاعدة الإنتاج للاختبارات.
- ملاحظة تطبيقات اكتُشفت وتحتاج إصلاحاً: حذف الحسابات المرتبطة بـOAuth وبياناتها وإبطال Apple يحتاج تدقيقاً إضافياً.

## تقدم التنفيذ — الشارح والعقود والتحقق
- طبقت الجداول الستة للشارحين على قاعدة الاختبار المحلية فقط؛ نجح الترحيل 0046.
- أُضيفت واجهات الويب والتطبيق للتسجيل والتحقق من البريد والملف الشخصي والكاميرا والمستندات والبنك وحماية الحساب. التحقق النهائي للواجهات مستمر.
- أُضيفت إدارة الطلبات والمستندات والأجهزة والعقود والتكليفات، مع وصول المدير الأعلى وإعادة مصادقة للبيانات الحساسة.
- العقود: قالب عقد عمل عربي/إنجليزي قابل للتحرير، الأجر بالساعات/المادة، بيانات العمل والمزايا والتجربة. النسخة المعروضة مجمدة؛ التوقيع يتطلب كلمة المرور وقبول بصمة النسخة الحالية.
- نسخة PDF تحتوي البيانات المحفوظة وقت العرض والتوقيع الرقمي المرسوم والبصمة، وتعمل في معالج معزول بلا أسرار التطبيق أو اتصال خارجي. التنزيل يعيد فحص الجلسة والنسخة ومحدود التزامن. الفحص المرئي النهائي جارٍ.
- حُدد تعارض محتمل في ترتيب أقفال إدارة الأجهزة وعولج؛ لا يُعاد تفعيل حساب محذوف من قسم الشارحين.
- أُضيف حذف الوثائق عند انتهاء مدة الاحتفاظ المعتمدة عبر قائمة حذف دائمة قابلة لإعادة المحاولة.
- يجري إصلاح حذف حساب OAuth وبياناته وإلغاء Apple، مع ترحيل إضافي مستقل 0047.
- فحص الأنواع بعد العقود لم يجد خطأ فيها؛ أخطاء OAuth ظهرت أثناء تعديل ذلك المسار وسيُعاد الفحص الموحد.
- بيئة المكتبات المحلية كانت تفتقد katex وتحتوي إصدار playwright-core مختلفاً عن المثبت في lockfile؛ استعادة الإصدارين المعلنين جارية قبل تشغيل PDF. هذه مشكلة إعداد محلي، وليست نتيجة فشل اختبار الوظيفة.

## متابعة التنفيذ — 2026-09-21 (آخر تحقق)
- رأس فرع العمل الحالي: `338c7b58f63831abd1d69681235d2d54d4d10595`. أُعيد تشغيل فحوصات الرأس نفسه بعد إصلاح اختبار سياسة التخزين الخاصة بملف PDF.
- نجحت فحوصات GitHub Actions الخمسة لهذا الرأس: Quality gates، Production public checks، Dependency security، Recovery acceptance، وMobile release validation.
- رحلة متصفح Chromium المعزولة نجحت على عرضي سطح المكتب والهاتف: تبويبات مساحة الشارح بلا تجاوز أفقي، التوقيع يتطلب الموافقة وكلمة المرور والرسم، العقد المقدم يوقع بالنسخة نفسها، تنزيل PDF الخاص محمي، والوصول المجهول/الطالب/الشارح الآخر مرفوض.
- كان فشل الجولة الأولى في مقارنة `Cache-Control` حرفيًا؛ عُدّل الاختبار إلى تحقق دلالي من `private` و`no-store` مع رفض `public` ومدة تخزين موجبة، ثم نجح البناء والاختبار.
- محاولة دمج رأس أقدم رُفضت تلقائيًا لأن الفرع تحرك؛ لم يُستخدم الرأس القديم ولم تُغيّر بيانات الإنتاج. الدمج يجب أن يستعمل آخر رأس مفحوص فقط.
- Railway ما زال يشغّل `main` الناجح عند commit `739809f`; تغييرات PR #9 لم تُنشر بعد. لا يُسجل هذا الدليل نجاحًا إنتاجيًا لميزات الشارحين قبل الدمج والنشر والتحقق الحي.
- فحص متغيرات Railway أظهر وجود `DEPLOY_GITHUB_TOKEN`، بينما `INSTRUCTOR_DATA_ENCRYPTION_KEY` و`OAUTH_TOKEN_ENCRYPTION_KEY` غير موجودين. سيبقى تشفير بيانات الشارحين وApple OAuth غير متاحين في الإنتاج حتى تُضاف مفاتيح مستقلة محفوظة خارج Git. حقلا الأساس النظامي ومدة الاحتفاظ بالهوية يبقيان قرارًا إداريًا فعليًا ولا تُخترع لهما قيم.
- لم تُجرَ عملية دفع حقيقية أو اختبار ضغط على الإنتاج، ولم تُقرأ قيم الأسرار أو تُحفظ في السجل. ما تبقى قبل ادعاء الجاهزية: إعداد أسرار الإنتاج والسياسات، اختبار Tap sandbox، متابعة المشتريات السابقة بعد إزالة RevenueCat، توقيع المتاجر، ثم التحقق الحي من SEO وواجهات الويب والتطبيقات بعد النشر.

## متابعة النشر — 2026-09-21
- دُمج PR #9 في `main` بالـmerge commit `06ddf079e91e3832ff15c361cf44291e7ed5a2d6` بعد نجاح فحوصات الرأس الأخير، دون تعديل سجلات الطلاب أو أسرار الإنتاج.
- نشر Railway رقم `e3de3710-7791-4131-a71d-67929b26e9f7` نجح من ذلك الـmerge commit. اكتمل بناء Next.js (191 صفحة)، ونجحت migrations 0046 و0047، وبدأ الخادم المشرف وعامل الفيديو، ونجح healthcheck لمسار `/api/health`.
- سجل HTTP بعد النشر أظهر 200 للمسارات العامة الجديدة مثل `/join-instructors` و`/instructor/register`، و200 لمسار المزامنة؛ 401 لـ`/api/auth/me` بدون جلسة متوقع ومقبول. هذا تحقق حي للمسارات العامة، وليس اختبار قبول بحسابات حقيقية أو تحصيلًا ماليًا.
- فحوصات `main` الخمسة للـmerge commit اكتملت بنجاح: Quality gates، Production public checks، Dependency security، Recovery acceptance، وMobile release validation.
- المتبقي التشغيلي الموثق: إضافة `INSTRUCTOR_DATA_ENCRYPTION_KEY` و`OAUTH_TOKEN_ENCRYPTION_KEY` المستقلين في Railway عند تفعيل تلك الميزات، إدخال أساس جمع الهوية ومدة الاحتفاظ من الإدارة، اختبار Tap sandbox بمفاتيحه الفعلية، اختبار شراء/استرداد دون بيانات حقيقية، وتوقيع ملفات المتاجر والتحقق من SEO بعد النشر. لا تُعد هذه البنود مكتملة بمجرد نجاح البناء.

## تحقق لاحق للنشر — 2026-09-21
- نشر Railway الأخير من commit التوثيق `dc62fe32ef4781ec4da8978610f02b79e859c152` هو `df5f5973-2d4d-4294-a1b6-bace183b0d09` وحالته `SUCCESS`. نجح البناء، healthcheck، وتشغيل الخدمات الثلاث في الإنتاج.
- فحوصات GitHub الخمسة لنفس الـcommit اكتملت بنجاح في 2026-09-21: Production public checks، Dependency security، Recovery acceptance، Mobile release validation، وQuality gates.
- هذا النشر توثيقي لنفس كود الدمج ولا يثبت قبول المتاجر أو تحصيل Tap الحقيقي. تبقى البنود التشغيلية المذكورة أعلاه مطلوبة قبل إعلان الجاهزية النهائية.

## تصحيح حالة إعدادات الإنتاج — 2026-09-21
- بعد آخر تحقق من متغيرات Railway ظهرت أسماء `INSTRUCTOR_DATA_ENCRYPTION_KEY` و`OAUTH_TOKEN_ENCRYPTION_KEY` إلى جانب `DEPLOY_GITHUB_TOKEN` ومفاتيح Tap. القيم محجوبة عن هذا الاتصال؛ لذلك سُجل وجود المتغيرات فقط، وتبقى صلاحية الطول/الترميز واختبار مسارات التسجيل وApple OAuth وTap sandbox مطلوبة.
- سياسة جمع الهوية لا تُستنتج من وجود المتغيرات: النظام يقرأ مرجع السند ومدة الاحتفاظ من إعدادات الإدارة، ولا تُحفظ قيمة قانونية افتراضية.
- جهاز Remote Desktop Commander المحلي كان غير متصل وقت التحقق؛ لم تُنفذ أوامر أو تغييرات على نسخة اللابتوب.
