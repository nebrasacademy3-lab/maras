import { useQuery } from "@tanstack/react-query";
import { router, useLocalSearchParams } from "expo-router";
import React, { useState } from "react";
import { ScaledText as Text } from "@/src/components/ScaledText";
import { Pressable, StyleSheet, View } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { AppHeader } from "@/src/components/AppHeader";
import { SearchPicker } from "@/src/components/SearchPicker";
import { AppButton, Field, LoadingState, Screen } from "@/src/components/ui";
import { api, ApiError } from "@/src/lib/api";
import { useAuth } from "@/src/providers/AuthProvider";
import { useTheme } from "@/src/providers/ThemeProvider";
import { useLanguage } from "@/src/providers/LanguageProvider";
import { ACADEMIC_LEVELS as ACAMIC_LEVELS } from "@/src/constants/academic-levels";
import type { Catalog } from "@/src/types";

type ProgramsResponse = { programs: { name: string; degree: string; area: string }[] };
export default function Register() {
  const params = useLocalSearchParams<{ ref?: string }>();
  const { register } = useAuth(); const { colors } = useTheme(); const { direction, rowDirection } = useLanguage();
  const catalog = useQuery({ queryKey: ["catalog"], queryFn: () => api<Catalog>("/api/mobile/catalog") });
  const referralCode = String(Array.isArray(params.ref) ? params.ref[0] : params.ref || "").trim().toUpperCase().replace(/[^A-Z0-9-]/g, "").slice(0, 32);
  const [form, setForm] = useState({ fullName: "", email: "", phone: "", password: "", universitySlug: "", specialty: "", academicLevel: "", referralCode });
  const programQuery = useQuery({ queryKey: ["programs", form.universitySlug], queryFn: () => api<ProgramsResponse>(`/api/catalog/programs?institution=${encodeURIComponent(form.universitySlug)}`), enabled: Boolean(form.universitySlug) });
  const programs = programQuery.data?.programs || []; const programLoading = programQuery.isFetching; const [accepted, setAccepted] = useState(false);
  const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  if (catalog.isLoading) return <Screen><LoadingState label="نجهّز الجامعات والتخصصات..." /></Screen>;
  const institutions = catalog.data?.institutions || [];
  const submit = async () => { setLoading(true); setError(""); try { await register({ ...form, termsAccepted: true }); router.replace("/onboarding"); } catch (reason) { setError(reason instanceof ApiError ? reason.message : "تعذر إنشاء الحساب"); } finally { setLoading(false); } };
  const valid = form.fullName.trim().length >= 5 && form.email.includes("@") && form.phone.replace(/\D/g, "").length >= 9 && form.password.length >= 10 && form.universitySlug && form.specialty && form.academicLevel && accepted;
  return <Screen keyboard><AppHeader title="إنشاء حساب" subtitle="ملف جامعي مكتمل من البداية" auth /><Text style={[styles.title, { color: colors.text }]}>ابدأ رحلتك مع مراس</Text><Text style={[styles.copy, { color: colors.textSoft }]}>ستظهر لك مواد تخصصك أولًا، ويمكنك تصفح وشراء مواد بقية الجامعات أيضًا.</Text>{form.referralCode ? <View style={[styles.referral, { backgroundColor: colors.surfaceAlt, borderColor: colors.border }]}><View style={[styles.referralIcon, { backgroundColor: `${colors.primary}16` }]}><Ionicons name="gift-outline" size={20} color={colors.primary} /></View><View style={styles.referralCopy}><Text style={[styles.referralTitle, { color: colors.text }]}>دخلت من دعوة صديق</Text><Text style={[styles.referralCode, { color: colors.primary }]}>رمز الإحالة: {form.referralCode}</Text></View><Pressable onPress={() => setForm({ ...form, referralCode: "" })}><Ionicons name="close-circle" size={20} color={colors.textSoft} /></Pressable></View> : null}<Field label="الاسم الكامل" icon="person-outline" value={form.fullName} onChangeText={(fullName) => setForm({ ...form, fullName })} placeholder="الاسم الرباعي" /><Field label="البريد الإلكتروني" icon="mail-outline" autoCapitalize="none" keyboardType="email-address" value={form.email} onChangeText={(email) => setForm({ ...form, email })} placeholder="name@example.com" /><Field label="رقم الجوال السعودي" icon="call-outline" keyboardType="phone-pad" value={form.phone} onChangeText={(phone) => setForm({ ...form, phone })} placeholder="05xxxxxxxx" /><Field label="كلمة المرور" icon="lock-closed-outline" inputDirection="ltr" secureTextEntry={!showPassword} value={form.password} onChangeText={(password) => setForm({ ...form, password })} placeholder="10 أحرف، رقم ورمز خاص" trailing={<Pressable onPress={()=>setShowPassword((value)=>!value)} hitSlop={8}><Ionicons name={showPassword?"eye-off-outline":"eye-outline"} size={20} color={colors.textSoft}/></Pressable>} />{!referralCode ? <Field label="رمز إحالة — اختياري" icon="gift-outline" inputDirection="ltr" autoCapitalize="characters" value={form.referralCode} onChangeText={(value) => setForm({ ...form, referralCode: value.toUpperCase().replace(/[^A-Z0-9-]/g, "").slice(0, 32) })} placeholder="MERAS-XXXX" /> : null}<SearchPicker label="الجامعة أو الكلية" value={form.universitySlug} placeholder="اختر جهتك التعليمية" items={institutions.map((item) => ({ key: item.slug, label: item.name, detail: `${item.region} · ${item.type}` }))} onSelect={(item) => setForm({ ...form, universitySlug: item.key, specialty: "" })} /><SearchPicker label="التخصص" value={form.specialty} placeholder={programLoading ? "جارٍ تحميل التخصصات..." : "اختر تخصصك"} disabled={!form.universitySlug || programLoading} items={programs.map((item) => ({ key: item.name, label: item.name, detail: `${item.degree} · ${item.area}` }))} onSelect={(item) => setForm({ ...form, specialty: item.key })} /><SearchPicker label="المستوى الدراسي" value={form.academicLevel} placeholder="اختر مستواك الحالي" items={ACAMIC_LEVELS.map((level) => ({ key: level, label: level, detail: level === "خريج" ? "أنهيت الدراسة" : "مستوى دراسي" }))} onSelect={(item) => setForm({ ...form, academicLevel: item.key })} /><Pressable onPress={() => setAccepted((value) => !value)} style={[styles.terms, { direction, flexDirection: rowDirection }]}><Ionicons name={accepted ? "checkbox" : "square-outline"} size={23} color={accepted ? colors.primary : colors.textSoft} /><Text style={[styles.termsText, { color: colors.textSoft }]}>أوافق على شروط الاستخدام وسياسة الخصوصية ومعالجة بيانات الحساب.</Text></Pressable>{error ? <Text style={[styles.error, { color: colors.danger }]}>{error}</Text> : null}<AppButton title="إنشاء الحساب" icon="sparkles-outline" loading={loading} disabled={!valid} onPress={submit} /><View style={[styles.login, { direction, flexDirection: rowDirection }]}><Text style={{ color: colors.textSoft }}>لديك حساب؟</Text><Pressable onPress={() => router.replace("/(auth)/login")}><Text style={{ color: colors.primary, fontWeight: "900" }}>تسجيل الدخول</Text></Pressable></View></Screen>;
}

const styles = StyleSheet.create({ title: { fontSize: 25, fontWeight: "900", textAlign: "right", writingDirection: "rtl", marginTop: 8 }, copy: { fontSize: 12, lineHeight: 21, textAlign: "right", writingDirection: "rtl", marginBottom: 22 }, referral: { minHeight: 66, flexDirection: "row", alignItems: "center", gap: 9, borderWidth: 1, borderRadius: 17, padding: 10, marginBottom: 14 }, referralIcon: { width: 42, height: 42, borderRadius: 14, alignItems: "center", justifyContent: "center" }, referralCopy: { flex: 1, alignItems: "flex-start" }, referralTitle: { fontSize: 11, fontWeight: "900" }, referralCode: { fontSize: 9, fontWeight: "800", marginTop: 3 }, terms: { flexDirection: "row", alignItems: "center", gap: 9, marginVertical: 7 }, termsText: { flex: 1, fontSize: 10, lineHeight: 18, textAlign: "right", writingDirection: "rtl" }, error: { fontSize: 11, lineHeight: 19, textAlign: "center", marginVertical: 10 }, login: { flexDirection: "row", gap: 5, justifyContent: "center", marginTop: 18 },
});
