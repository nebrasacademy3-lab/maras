import { Ionicons } from "@expo/vector-icons";
import { useQuery } from "@tanstack/react-query";
import { router } from "expo-router";
import React from "react";
import { ScaledText as Text } from "@/src/components/ScaledText";
import { Pressable, StyleSheet, View } from "react-native";
import { AppHeader } from "@/src/components/AppHeader";
import { AppearanceSettings } from "@/src/components/AppearanceSettings";
import { AppButton, Card, EmptyState, Screen, SectionTitle } from "@/src/components/ui";
import { api } from "@/src/lib/api";
import { useAuth } from "@/src/providers/AuthProvider";
import { useTheme } from "@/src/providers/ThemeProvider";
import type { PublicSettings } from "@/src/types";

export default function Account() {
  const { user, logout } = useAuth(); const { colors } = useTheme();
  const settings = useQuery({ queryKey: ["settings"], queryFn: () => api<{ settings: PublicSettings }>("/api/public/settings"), staleTime: 5_000 });
  const requestsEnabled = settings.data?.settings.course_requests_enabled !== "false";
  const registrationEnabled = settings.data?.settings.student_registration_enabled !== "false";
  if (!user) return <Screen><AppHeader title="حسابي" subtitle="الدخول إلى مراس" /><EmptyState icon="person-circle-outline" title="أهلًا بك في مراس" text={registrationEnabled?"أنشئ حسابًا لإظهار مواد تخصصك وحفظ التقدم والطلبات والإشعارات.":"سجّل الدخول إلى حسابك الحالي لمتابعة موادك وتقدمك وإشعاراتك."} action={<View style={{ width: "100%", gap: 9 }}><AppButton title="تسجيل الدخول" onPress={() => router.push("/(auth)/login")} />{registrationEnabled?<AppButton title="إنشاء حساب" variant="soft" onPress={() => router.push("/(auth)/register")} />:null}</View>} /></Screen>;
  const items: { icon: React.ComponentProps<typeof Ionicons>["name"]; title: string; text: string; route: string }[] = [
    { icon: "person-outline", title: "بيانات الحساب", text: "الاسم والجوال والجامعة والتخصص والمستوى", route: "/profile" },
    { icon: "notifications-outline", title: "الإشعارات", text: "تحديثات المواد والطلبات", route: "/notifications" },
    { icon: "heart-outline", title: "المفضلة", text: "المواد التي حفظتها للرجوع", route: "/favorites" },
    { icon: "receipt-outline", title: "الطلبات والفواتير", text: "حالة الاشتراكات والفواتير", route: "/orders" },
    ...(requestsEnabled ? [{ icon: "cloud-upload-outline" as const, title: "طلبات المواد", text: "طلب جديد ورفع السلايدات", route: "/requests" }] : []),
    { icon: "headset-outline", title: "الدعم الفني", text: "التذاكر ووسائل التواصل", route: "/support" },
    { icon: "shield-checkmark-outline", title: "الأمان والخصوصية", text: "الجلسات وحذف الحساب", route: "/security" },
  ];
  if (user.role === "admin") items.unshift({ icon: "grid-outline", title: "لوحة الإدارة", text: "المستخدمون والطلبات والدعم", route: "/admin" });
  if (user.role === "supervisor" || user.role === "admin") items.unshift({ icon: "construct-outline", title: "مساحة المشرف", text: "الطلبات والمحتوى المسند", route: "/supervisor" });
  return <Screen><AppHeader title="حسابي" subtitle={user.role === "admin" ? "مدير المنصة" : user.role === "supervisor" ? "مشرف محتوى" : "طالب مراس"} /><Card style={styles.profile}><View style={[styles.avatar, { backgroundColor: colors.primary }]}><Text style={styles.avatarText}>{user.fullName[0]}</Text></View><View style={styles.profileCopy}><Text style={[styles.name, { color: colors.text }]}>{user.fullName}</Text><Text style={[styles.email, { color: colors.textSoft }]}>{user.email}</Text><Text style={[styles.study, { color: colors.primary }]}>{user.specialty || "أكمل تخصصك"} · {user.academicLevel || "أكمل مستواك"}</Text></View></Card><View style={styles.quickActions}><Pressable onPress={() => router.push("/cart")} style={[styles.quickAction, { backgroundColor: colors.surface, borderColor: colors.border }]}><Ionicons name="bag-handle-outline" size={19} color={colors.primary} /><Text style={{ color: colors.text }}>السلة</Text></Pressable><Pressable onPress={() => router.push("/favorites")} style={[styles.quickAction, { backgroundColor: colors.surface, borderColor: colors.border }]}><Ionicons name="heart-outline" size={19} color={colors.danger} /><Text style={{ color: colors.text }}>المفضلة</Text></Pressable><Pressable onPress={() => router.push("/support")} style={[styles.quickAction, { backgroundColor: colors.surface, borderColor: colors.border }]}><Ionicons name="headset-outline" size={19} color={colors.primary} /><Text style={{ color: colors.text }}>الدعم</Text></Pressable></View><SectionTitle title="الإعدادات والخدمات" subtitle="كل خدمة في بطاقة واضحة مع انتقال سريع" /><View style={styles.menu}>{items.map((item) => <Pressable key={item.title} onPress={() => router.push(item.route as never)} style={({ pressed }) => [styles.row, { backgroundColor: colors.surface, borderColor: colors.border, transform: [{ scale: pressed ? .985 : 1 }] }]}><View style={[styles.rowIcon, { backgroundColor: colors.surfaceAlt }]}><Ionicons name={item.icon} size={21} color={colors.primary} /></View><View style={styles.rowCopy}><Text style={[styles.rowTitle, { color: colors.text }]}>{item.title}</Text><Text style={[styles.rowText, { color: colors.textSoft }]}>{item.text}</Text></View><Ionicons name="chevron-back" size={18} color={colors.textSoft} /></Pressable>)}</View><AppearanceSettings /><Pressable accessibilityRole="button" accessibilityLabel="تسجيل الخروج" onPress={async () => { await logout(); router.replace("/(auth)/welcome"); }} style={({ pressed }) => [styles.logoutRow, { backgroundColor: colors.surface, borderColor: `${colors.danger}55`, transform: [{ scale: pressed ? .985 : 1 }] }]}><View style={[styles.logoutIcon, { backgroundColor: `${colors.danger}14` }]}><Ionicons name="log-out-outline" size={19} color={colors.danger} /></View><View style={styles.rowCopy}><Text style={[styles.logoutTitle, { color: colors.danger }]}>تسجيل الخروج</Text><Text style={[styles.rowText, { color: colors.textSoft }]}>إنهاء الجلسة بأمان</Text></View><Ionicons name="chevron-back" size={18} color={colors.textSoft} /></Pressable></Screen>;
}

const styles = StyleSheet.create({ profile: { flexDirection: "row-reverse", alignItems: "center", gap: 14 }, avatar: { width: 66, height: 66, borderRadius: 23, alignItems: "center", justifyContent: "center" }, avatarText: { color: "#FFF", fontSize: 23, fontWeight: "900" }, profileCopy: { flex: 1, alignItems: "flex-end" }, name: { fontSize: 18, fontWeight: "900", textAlign: "right" }, email: { fontSize: 10, marginTop: 3 }, study: { fontSize: 10, fontWeight: "800", marginTop: 7 }, quickActions: { flexDirection: "row-reverse", flexWrap: "wrap", gap: 9, marginTop: 13 }, quickAction: { flexBasis: "30%", flexGrow: 1, minHeight: 48, borderWidth: 1, borderRadius: 14, flexDirection: "row-reverse", alignItems: "center", justifyContent: "center", gap: 7 }, themeRow: { flexDirection: "row-reverse", gap: 9 }, theme: { flex: 1, minHeight: 70, borderWidth: 1, borderRadius: 17, alignItems: "center", justifyContent: "center", gap: 7 }, menu: { gap: 9, marginBottom: 12 }, row: { minHeight: 76, borderWidth: 1, borderRadius: 18, padding: 12, flexDirection: "row-reverse", alignItems: "center", gap: 12 }, rowIcon: { width: 46, height: 46, borderRadius: 15, alignItems: "center", justifyContent: "center" }, rowCopy: { flex: 1, alignItems: "flex-end" }, rowTitle: { fontSize: 13, fontWeight: "900" }, rowText: { fontSize: 9, marginTop: 4, textAlign: "right" }, logoutRow: { minHeight: 58, borderWidth: 1, borderRadius: 16, padding: 9, flexDirection: "row-reverse", alignItems: "center", gap: 10, marginBottom: 20 }, logoutIcon: { width: 38, height: 38, borderRadius: 12, alignItems: "center", justifyContent: "center" }, logoutTitle: { fontSize: 11, fontWeight: "900", textAlign: "right" },
});
