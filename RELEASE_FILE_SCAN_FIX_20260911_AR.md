# تسليم إصلاح المرفقات والتحقق — مراس العلم

**تاريخ التسليم: 11 سبتمبر 2026.**

هذه نسخة المصدر الكاملة المبنية على `maras-admin-completion-20260911.zip`، وليست باتشًا منفصلًا ولا ملف APK/IPA. تشمل الويب والخادم ومصدر تطبيقات الجوال وملفات التشغيل والترحيل والاختبارات. التقرير الحالي يخص هذه التعديلات؛ تقارير وبصمات الإصدارات الأقدم محفوظة للتاريخ ولا تصف محتوى هذه النسخة بعد تعديله.

## ما أُصلح ونُفّذ

1. **تعليق فحص المرفقات:** أضيف محرك ClamAV إلى إعداد Docker، ومسار نقل ملفات فعلي للمحرك، وعامل تلقائي وطابور دائم لكل من الطلبات والدعم وموارد التعلم وأدوات مراس. لم يتم تجاوز الحماية أو تحويل الملفات دون فحص إلى حالة سليمة.
2. **التزامن والتعثر:** أقفال فحص في قاعدة البيانات، ومحاولات مؤجلة تدريجيًا، واشتراط تطابق الملف والقفل عند حفظ النتيجة، وعدم السماح لنتيجة قديمة بإلغاء حجر جديد. حد محلي لمسارين متزامنين للفحص وحجم ووقت محدودان.
3. **التنزيل الفردي والمجمع:** تحقق من صلاحية الوصول قبل بدء الفحص، ورسائل عربية مفيدة وحالات HTTP صحيحة. توقف إنشاء ZIP صامت ناقص إذا فُقد أحد المرفقات؛ حد إجمالي الحجم والتحقق من تغير مجموعة الملفات أثناء الطلب.
4. **التطبيق:** استخدام مسار تنزيل مشترك للدعم، والتحقق من نتيجة HTTP، وإظهار خطأ الفحص الحقيقي بدل ادعاء حفظ الملف، وحذف ملفات الخطأ والتنزيل الفاشل، ومنع تضارب أسماء التنزيلات. أضيفت رؤوس هوية الجهاز والتحقق الإداري اللازمة. لا تُعرض معاينة المرفق المعلق كما لو كان سليماً.
5. **إدارة الأمان:** صفحة `/admin/files` وقسم مقابل في المركز التشغيلي بالتطبيق لعرض حالة المحرك والانتظار والمحاولات وأسباب التعثر واستئناف الدفعات.
6. **ضوابط إضافية:** حفظ مصدر التخزين مع المرفقات الجديدة، رفض تحويلات HTTP في نقل المستندات، رفض الرد المتناقض الذي يجمع تصريح سلامة وإصابة، منع إعادة إتاحة الملف المحجور عبر إجراء أدوات مراس أو إعادة فحص المورد العادية، وتصحيح التنظيف المرتبط بمصدر التخزين.
7. **أوامر التحقق:** `npm run verify:platform` لتشغيل بوابات جودة المصدر، و`npm run files:verify-live` للتحقق لاحقًا من محرك الفحص الفعلي. أضيف تشغيل اختبارات التطبيق إلى ملف CI الموجود.

## النتائج الفعلية داخل بيئة العمل

| الفحص | النتيجة | حدود النتيجة |
|---|---|---|
| الاختبارات الجديدة لإصلاح المرفقات | **96 من 96 ناجحة** | نقل HTTP/TCP وقراءة تخزين محلي فعلية، مع محاكيات صريحة لقاعدة البيانات وواجهات Expo في اختبارات السلوك. ليست شهادة كشف فيروسات. |
| مجموعة اختبارات الويب والخادم | **480 ناجحة من 490؛ 10 متعذرة/فاشلة** | تشمل الـ96 الجديدة؛ لا تُجمع مرتين. بعض الاختبارات سلوكية بوحدات معزولة وبعضها عقود مصدرية. |
| مجموعة اختبارات التطبيق الحالية | **48 ناجحة من 62؛ 14 متعذرة/فاشلة** | الحالات المتعذرة تحتاج React غير المثبتة. ليست اختبارات نقر على أجهزة. |
| فحص صياغة TypeScript/TSX لكل المصدر | **453 ملفًا، دون أخطاء صياغة** | Transpilation فقط باستخدام TypeScript 5.8.3 المتاح في البيئة، وليس فحص أنواع كاملًا ولا إصدار المشروع المثبّت من القفل. |
| فحص أنواع محدود | **نجح لثلاث وحدات: النقل الأمني والتخزين وZIP** | باستخدام أنواع Node المتاحة. لا يشمل تطبيق Next.js أو Expo كاملًا. |
| صياغة سكربتات تشغيل الخادم والمحرك | **نجحت** | `bash -n`، لا يعني بناء Docker أو تشغيل ClamAV. |
| تثبيت الحزم الدقيقة | **تعذر** | سجل `ENOTCACHED`، وفحص الشبكة فشل في حل اسم `registry.npmjs.org`. |
| بناء الويب الإنتاجي | **لم ينجح** | `next: not found`. |
| Lint للويب والتطبيق | **لم يُنفّذ بنجاح** | `eslint: not found`. |
| فحص أنواع التطبيق كاملًا | **لم ينجح** | غياب `expo/tsconfig.base` وأنواع Node، واختلاف مترجم النظام عن المترجم المطلوب. |

خط الأساس قبل التعديل بعد توفير المترجم المعزول: **384 نجاحًا من 394** للويب. بعد التعديل أصبحت **480 من 490**؛ بقيت حالات التعذر العشر نفسها. حالات الويب المتعذرة مرتبطة بغياب `tsx` و`jose` واعتماديات Next.js والبناء الإنتاجي. لم تُحذف الحالات لإظهار نتيجة ناجحة.

جميع بوابات `verify:platform` خرجت بفشل في هذه البيئة للأسباب المسجلة. **لا تستخدم نجاح الحالات المعزولة بديلًا عن اجتياز البناء وفحص الأنواع والاختبارات المتكاملة على بيئة مجهزة.**

## ما لم يُختبر حيًا

لم تُوصل قاعدة PostgreSQL فعلية، ولم تُطبق الترحيلات عليها هنا. اختبارات الأقفال والسجلات استخدمت محاكيًا صريحًا للنتائج وشروطها، لا قاعدة حقيقية. لم يُبنَ Docker، ولم يعمل محرك ClamAV أو تعريفاته فعليًا هنا؛ نجاح بروتوكول الاختبار لا يثبت فعالية محرك مكافحة الفيروسات.

لم تُختبر عمليات Tap المالية أو الاسترداد الفعلي، ولا تسليم البريد/WhatsApp/Push إلى حسابات حقيقية، ولا مفاتيح أدوات مراس الفعلية أو تشغيل الفيديو والتخزين السحابي على استضافتك. لم تُختبر كل شاشة من شاشات الإدارة والطلاب بالتفاعل اليدوي، ولم تُبنَ APK/IPA أو تُجرّب أجهزة Android وiOS. لذلك **لا يوجد اعتماد بأن كل وظائف المنصة تعمل 100% أو أنها خالية من جميع الثغرات**.

ملف `verification/20260911-files/coverage-inventory.json` يحصر مسارات API وصفحات الويب وشاشات التطبيق. الحصر ليس تغطية اختبار؛ لا يحتوي حكم نجاح وهميًا لكل مسار.

## السجلات المرفقة

- `verification/20260911-files/file-feature-tests.tap`: نتائج الحالات الجديدة الـ96.
- `verification/20260911-files/source-syntax.json`: قائمة ملفات الصياغة الـ453 ونتائجها.
- `verification/20260911-files/standalone-typecheck.log`: مخرجات فحص الأنواع المحدود؛ خلوه من الرسائل ترافق مع رمز خروج صفر.
- `verification/20260911-files/dependency-install-probe.log` و`network-probe.log`: سبب تعذر تثبيت الحزم.
- `verification/20260911-files/web-final.tap` و`mobile-final.tap`: نتائج إعادة التشغيل النهائية لمجموعتي الويب والتطبيق.
- `verification/local-gate-2026-09-11T03-37-14-220Z/`: سجلات بوابات البناء وLint وفحص أنواع التطبيق و`results.json`. تحتوي أيضًا تشغيل الاختبارات قبل إضافة حالة الاختبار الأخيرة؛ السجلان النهائيان أعلاه هما المرجع للعدد النهائي.
- `verification/20260911-files/summary.json`: نتائج هذا التسليم بصيغة قابلة للقراءة البرمجية.
- `SOURCE_CHECKSUMS_FILE_SCAN_20260911.sha256`: بصمات النسخة الحالية. الملفات ذات أسماء بصمات أقدم تاريخية فقط.

## قبل إطلاق النسخة

ابدأ بـ `FILE_SCANNING_AR.md`: يشرح المتغيرات ومصدر الملفات القديمة وذاكرة المحرك وتشخيص حالات الانتظار. لا تمسح قاعدة البيانات، ولا تغيّر مفاتيح تشفير المنصة الحالية، ولا تنقل مخزن الملفات دون مطابقة.

على بيئة مرحلية مماثلة للإنتاج، ثبّت الحزم الدقيقة في الجذر ثم `mobile`، وشغّل `npm run verify:platform`. طبّق كامل سلسلة الترحيلات حتى `0030` بعد أخذ نسخة احتياطية. شغّل محرك الفحص وتحديث تعريفاته، ثم نفّذ `npm run files:verify-live` واختبار رفع/انتظار/تنزيل فعلي من الطالب والإدارة والتطبيق.

اختبر وظائف الدفع والهوية والبريد والإشعارات والفيديو والأدوات بمفاتيح اختبار أو حسابات مخصصة، مع عدم تنفيذ دفعات حقيقية دون مراجعة. ابنِ نسخ التطبيق وجربها على الأجهزة، ثم راقب سجلات الخادم والذاكرة والانتظار بعد النشر. مصدر التطبيق المحدث لا يغيّر تطبيقًا مثبتًا على هواتف المستخدمين تلقائيًا؛ يحتاج إصدارًا موزعًا بالطريقة المعتمدة لديك.

إعداد الفحص الجديد لا يغني عن إدارة التحديثات ومراجعة صلاحيات المستخدمين والنسخ الاحتياطية واختبارات الأمان الدورية. عدم إتاحة ملف غير مفحوص إجراء مقصود؛ الإصلاح هو تشغيل المحرك والطابور وتوضيح السبب، وليس تعطيل الحماية.
